# eLearn Studio — Working Context

> **This file is the first thing to read at the start of every Claude Code session.**
> It is updated by Claude Code after every completed task block.

## 📅 Scheduled Reviews (check on every session-start)

_No pending reviews._

### Completed reviews

| Trigger date | Topic | Completed | Outcome |
|---|---|---|---|
| 2026-05-03 | Soak-window review of `no-unsafe-domain-cast` rule (Phase 1+2) and `verify:ci` health (silent-zero regression check) | **2026-07-17** (overdue — first session after trigger) | **Quiet.** (1) Zero new production casts since 2026-04-26 — only a *relocated* inline structural cast in `e2e/utils/simulation.ts:43` (out of rule scope; moved from `simulation-editor.spec.ts` during TD-013.5 helper factoring) and 2 `no-console` disables in the e2e campaign spec. No false-positive complaints. (2) CI push run green over `473067b` (2026-04-29); CodeQL weekly green through 2026-07-16; `verify:test` filter resolves **9 packages, all with test scripts** — no silent-zero regression. (3) Phase 2 deferred items unchanged (backend/api Mongo-Docker triage, Windows esbuild/AV lock, `RuleTester` cases — all still deferred, no production work since). Actionable finding: `fb53803` (TD-014.27 administrative close, 2026-05-04) was never pushed — resolved this same session (pushed 2026-07-17). |

**Why here and not elsewhere:** repo issues would dirty the tracker for personal operational triggers (owner directive, 2026-04-26). A Google Calendar event is paired as a personal reinforcement.

---
> Last updated: 2026-07-19 (night) — **TD-018 FIX SHIPPED ✅ (v0.5.71)** — done-button label revert on slide reload. One-line root cause exactly as investigated: T643.1's positive content-restore allowlist (`text`/`button`) omitted `done-button`, so edited labels saved to `properties.content` (published courses unaffected) but every editor reload fell back to the type default. Allowlist extended (safe by symmetry — plain-text node; label-change ADR consistent). TDD: unit RED→GREEN converters.test 91/91 (suite 1050/1050); E2E `@regression TD-018` in dedicated `button-widget.spec.ts` (live reflect + round-trip) 2/2; campaign refreshed `17-slide-5-final.png` → shows "Finish course" at last. Ops: Docker Desktop was down (machine restart) — daemon relaunched + clean dev-server restart (backend does not self-recover from a Mongo-less boot; known pattern). **Backlog restante**: TD-019 (name trait), TD-020 (select(null) mystery), TD-014-followup-1.
>
> Last updated: 2026-07-19 (evening) — **TD-017 CLOSED ✅ — documentation defect, not product gap (owner's fossil hypothesis CONFIRMED)** — verification: `EventDispatcher.fireSlideEvent` scans every widget's sequences for enterSlide/exitSlide by design (docstring explicit, unit-covered since T021, fired by renderSlide/goToSlide) → widget-hosted slide events ARE the shipped model; the campaign's §17 BranchingNote wiring was functional all along. Archaeology: `Slide.actions?` in the schema since the initial commit, read/written by NOTHING — fossil of a retired slide-level-storage intent; the v2 manual (1fdfb3c) was written against the intent, not the implementation, in FOUR places (§09 no-selection surface, §10 "attached to the slide itself", §11 recipes ×3 "On the slide", §17 "no block selected"). **Delivered**: `@deprecated` docstring on the fossil field; all four manual sites corrected to the widget-hosted model (host block named per recipe + model notes per docs-user voice); §17 slide-4 keeps the If/Else lesson on *BranchingNote* + tip cross-referencing the TD-021 checkbox as the simple path; ADR addendum records verification + archaeology + the not-needed verdict for a slide-scope surface (reopen only on real authoring demand). Campaign untouched (captures already show the correct pattern). **Backlog restante**: TD-018 (done-button label), TD-019 (name trait), TD-020 (select(null) mystery), TD-014-followup-1.
>
> Last updated: 2026-07-19 (later) — **TD-021 SHIPPED ✅ (v0.5.69) — correctness-gated navigation, SCORM-aligned** — owner directive: let the STANDARD arbitrate. Primary-source verdict (ADR `2026-07-19-conditional-navigation-scorm-alignment.md`): SCORM 2004 sequencing is inter-SCO only ("completely up to the content developer" for intra-SCO nav), SCORM 1.2 has no sequencing — single-SCO runtimes MUST gate internally (as Storyline/iSpring do); multi-SCO sequencing recorded as non-goal (absent in 1.2, LMS support inconsistent). **Delivered**: `scoring.requireCorrect` (shared-types) + runtime gate on the evaluator's correctness verdict with attempts-exhaustion unlock (no trapped learners) + **attempts enforcement at Submit** (previously unimplemented — button hard-disabled after first answer while default feedback said "Try again"; latent product gap folded in) + LMS mastery-threshold override (`cmi.scaled_passing_score`/`cmi.student_data.mastery_score` → packaged chain, resolved once into `state.passMark`; scormReport/finishCourse simplified to read it) + suspend v2 `c`/`t` fields with legacy inference + authoring `require-correct-checkbox` in the shared ScoringFeedbackForm. **TDD**: 13 runtime + 3 panel units RED→GREEN; E2E `@regression TD-021` (wrong → gate holds + Submit retryable; correct → unlocks) green first run. **Verification**: verify:test all-green (authoring 1049, runtime 278); E2E question-widget/preview-handshake/scorm-export 47/47; campaign refreshed §08 (both checkboxes; crop rect 251×220); user-guide §08 rewritten per docs-user skill; tsc 0; lint 0. **TD-017 downgraded** (authoring-power feature; §17 manual rewrite can now target the supported declarative gate). **Next**: TD-017 decision/manual rewrite, TD-018, TD-019, TD-020, TD-014-followup-1.
>
> Last updated: 2026-07-19 — **TD-016 FIX SHIPPED ✅ (was HIGH)** — nav-buttons broken editor render root-caused and fixed. Investigation probe proved model/DOM always correct (both children with labels at fresh/post-autosave/post-round-trip) and computed styles exposed `position:absolute` on BOTH child buttons → stacked at container origin, "Next →" painting over "← Previous". Culprit: `initEditor.ts` global `component:add` handler (T010.11/T012.6) absolutizing + force-dragging EVERY component incl. composite-widget children (re-applied on every loadData — why T634's clean re-injection re-broke). **Fix**: handler skips non-top-level components (`component.parent?.() !== editor.getWrapper()`); children keep their type's flex flow + `draggable:false`. Scope: nav-buttons is the only type with child components in defaults (grep-verified). **Verification**: unit TD-016.1 RED→GREEN + TD-016.2 control (authoring-ui **1046/1046**); E2E `@regression TD-016` in grapesjs-integration (labels + `static` positions + bbox no-overlap + slide-switch round-trip), 19/19 with widget-persistence; **campaign re-run refreshed the §17 manual captures** — "← Previous | Next →" now side by side (visual proof archived in issues-TD-016.md flow); tsc 0; lint 0 errors. Full doc: `docs/issues/issues-TD-016.md`. **Next**: TD-017 (owner decision: slide-level actions feature vs manual §17 correction), TD-018, TD-019, TD-020, TD-014-followup-1.
> Last updated: 2026-07-18 (evening) — **TD-015 FIX SHIPPED ✅ (severity downgraded from CRITICAL)** — same-day follow-through on the backlog's top item after the block close. **Root-cause found at the selection boundary**: `EditorCanvas component:selected` resolved the widget id via `getId()` (GrapesJS model id) while the course doc keys widgets by `attributes.id` (converters contract `attributes.id || getId()`); on a missed lookup the panel seeded `[]` AND `useActionsSave` treated that load as an edit (its `lastSavedRef = null` reset) — persisting the empty array and wiping the widget's saved actions on mere selection. **Two fixes (TDD)**: attribute-id-first resolution at the boundary + load-echo suppression in `useActionsSave` (loading NEVER saves; only real edits do — this closes the whole wipe class regardless of what seeds `[]`). **Tests**: `EditorCanvas.selection.test.tsx` (2) + `hooks/useActionsSave.test.ts` (3) — 3 reproduced the bug RED pre-fix; new E2E `@regression GAP-02.4` in `action-sequence.spec.ts` (wire → slide switch → return → re-select → cold reload → survives). authoring-ui **1044/1044**; E2E action-sequence 9/9 + widget-persistence pair green; tsc 0; lint 0 errors. **Honest residual documented in the TD-015 tasks entry**: GAP-02.4 passes pre-fix too (hard-restart verified) — the standard flow re-aligns ids on slide switch, so the E2E guards the flow but the §17-campaign loss episode remains un-pinned (revised hypothesis recorded; .5c's wire-at-capture already made the campaign deterministic). Reopen only on a real-flow divergence repro. **Ops note**: mid-session the Mongo/Garage containers were found down (only Moodle survived) — `docker compose up -d` + dev-server restart needed port cleanup (orphaned vite/tsx listeners on 3000-3002 after TaskStop; killed by PID). **Next**: TD-016 (nav-buttons editor render) or owner's pick of the remaining backlog (TD-017…TD-020, TD-014-followup-1).
>
> Last updated: 2026-07-18 (later) — **TD-013 BLOCK CLOSED ✅ (v0.5.67)** — subtasks .6→.10 delivered in one owner-batched stretch ("continuamos trabajando y comiteamos con push al cierre de bloque"). **.6**: `scripts/crop-screenshots.py` (Pillow, mtime-idempotent, `_`-keys ignored, exit 1/2 on entry/config errors — all five paths exercised) + `scripts/run-crop.cjs` interpreter-probing launcher (the project `.venv` pip hangs persistently installing Pillow — suspected AV, §4.1 pattern; system `py -3` carries Pillow 12.2.0) + chained as `docs:screenshots` post-step. **.7**: playbook T-13…T-18 added, T-6 superseded (Escape deselect), T-12 snippet corrected, Deferred table → Historical-deferrals, spec header rewritten. **.8**: 55/55 fresh + inventory cross-check; visual pass 11-by-hand + 44-by-vision-agent; the pass caught 2 MORE baseline defects, fixed in-block (`13-block-placeholder` visible-canvas clip + widget placed in view; `14-processflow-builder` now seeds the 3-node `PROCESS_FLOW_SCENE` its placeholder demands, deduplicated with `14-json-example`); 20 stale `-fullpage` nets removed (only 08's by-design net remains). **.9** (owner-picked candidates 1-4): NEW `e2e/utils/worked-example.ts` + `e2e/utils/actions.ts`; CSS neutralisation constants in `screenshot.ts`; spec 1763→1610 lines; campaign green post-refactor with control shots pixel-checked. **.10**: legacy v1 tooling retired (docs/scripts/ deleted, docs/package.json purged + lockfile regen, docs/assets/screenshots README-flagged, scope doc §capture rewritten); issues-TD-013.md self-review; CHANGELOG v0.5.67. **Backlog filed per owner directive**: TD-015 (CRITICAL — action sequences lost on slide switch, GAP-02 materialised), TD-016 (nav-buttons broken editor render), TD-017 (slide-level actions manual/product mismatch), TD-018 (done-button label), TD-019 (name-trait stripping), TD-020 (select(null) context-destruction root cause). **User Manual v2 phase: capture pipeline COMPLETE — 0 manual, 0 deferred.** **Next**: owner triage of TD-015…TD-020 priorities; TD-014-followup-1 (R-M2) still parked.
>
> Last updated: 2026-07-18 — **TD-013.5c CLOSED ✅** — campaign captures recomposed (owner-approved option 1 after the 2026-07-17 investigation showed the defects were in the committed baseline, not the regeneration). **(a) §17**: the campaign now builds the real "Capitals of Europe" worked example per `17-worked-example.md` — New Course dialog (Blank starts with 0 slides; ready-signal wait must come AFTER the first `addSlide`), 5 slides renamed via dblclick+Ctrl-A, widgets at explicit per-widget coordinates, MC fully configured through real Props inputs (question/4 options/Berlin correct/100pts/2 attempts/feedbacks — all survive the round-trip), and **actions wired live in each final's prep, immediately before its shot** — sequences wired earlier did NOT survive the intervening slide switches (ids regenerate, sequences orphan → the e2e skill's GAP-02 observed in practice, filed as product finding). Finals captured at 1600×900 so the 1024×768 canvas + sidebar fit unclipped. **(b)** `02-first-slide` captured right after course creation (1 slide, empty canvas — genuine §02 state). **(c)** `11-recipe-attempts` fills variable/expression/widget-id params → zero validation banners (nested action rows must be targeted by placeholder INSIDE the condition row — they don't carry their own `data-action-type`). **(d)** `01` callouts 5–9 dropped below the tab strip (`addCallouts` offset now per-axis optional). **Battle notes**: an `editor.select(null)` evaluate right after slide-4 placement died with "Execution context was destroyed" on EVERY run (no navigation/vite/pageerror per the now-permanent diagnostics; page provably survives) — replaced with Escape (real-UI deselect) + `retryOnDestroyedContext` wrapper; root cause unexplained, filed. **5 product/manual findings filed in `issues-TD-013.md`** for owner triage: no slide-level actions UI (manual §17 documents one — manual or product must change), action-sequence loss on slide switch (GAP-02), nav-buttons broken editor render (prev label missing/overlap — affects real authors too), done-button label edits not reflected in canvas, name-trait stripping on round-trip. **Verification**: e2e tsc 0; lint 0 errors; campaign green ×4 consecutive (~40s), 56 PNGs; visual pass on all 8 recomposed shots (17-slide-1/3/4/5 match their placeholders incl. Click→Navigate, scoring panel, Enter Slide→Score Quiz+If/Else with $score>=50 and filled Else params, Send to LMS). Stale `-fullpage` nets from failed iterations removed. **Next**: TD-013.6 (Python crop tool — Pillow install into `.venv` still pending, network hang).
>
> Last updated: 2026-07-17 (later) — **TD-013.5b CLOSED ✅** — `08-scoring-section.png` dual strategy shipped. **(a) Primary testid path**: `data-testid="scoring-section"` on the `ScoringFeedbackForm` section root (`QuestionPropertiesPanel.tsx` — shared by MC/TF/Fill panels, only one renders at a time); spec block captures the `-fullpage` net unconditionally FIRST (fresh same-render source each run so the Python fallback's target-vs-source mtime idempotence is well-defined), then `captureElement('[data-testid="scoring-section"]', { padding: 12 })` with a warn-only catch (final asset then falls to the Python post-crop). **Side-fix in `e2e/utils/screenshot.ts`**: `captureElement`'s padding path read `boundingBox()` without scrolling — the docstring promised scroll handling but only the non-padding `locator.screenshot()` path delivered it; `page.screenshot` clips viewport-relative coords, so a below-fold element produced an invalid clip. Added `loc.scrollIntoViewIfNeeded()` before the bbox read (benefits every padded caller). **(b) Fallback config**: NEW `scripts/screenshots-crop.json` — entry maps `08-scoring-section.png` → source `-fullpage` + crop `{x:1029, y:404, w:251, h:169}` (= live element bbox 1041,416,227×145 minus the 12px padding, clamped at the 1280 right edge); validated pixel-equivalent to the primary capture by cropping the fresh fullpage via System.Drawing and comparing. The `crop-screenshots.py` tool itself is TD-013.6 (underscore-prefixed JSON keys reserved as ignored comments). **Verification**: authoring-ui vitest **1039/1039**; authoring-ui `tsc -b --force` exit 0; e2e `tsc --noEmit` exit 0; `pnpm lint` 0 errors (2 pre-existing TD-004 warnings unchanged); E2E high-risk pair `question-widget.spec.ts` + `authoring-ui-layer.spec.ts` **52/52** (3.4m); full campaign run green → `08-scoring-section.png` emitted via the `(element)` path (251×169) and visually verified. **Infra note**: dev stack (docker dev compose + `pnpm dev`) brought up by agent this session and left running; Pillow install into the project `.venv` still pending (pip bootstrap via `ensurepip` done; `pip install Pillow` hung on network — retry at TD-013.6). Campaign side-effect PNG churn in `docs/user-guide/assets/screenshots/` remains uncommitted per TD-013 boundary (commit decision at block level). **Next**: TD-013.6 — `scripts/crop-screenshots.py` (Python + Pillow, reads `screenshots-crop.json`, mtime-idempotent, standalone + chainable).
>
> Last updated: 2026-07-17 — **Scheduled review 2026-05-03 completed (overdue) — quiet ✅** — soak-window review of `no-unsafe-domain-cast` (Phase 1+2) + `verify:ci` health executed per the steps codified in §Scheduled Reviews; full outcome recorded in the Completed-reviews table above. Zero new production casts, CI/CodeQL green, `verify:test` resolves 9 packages (no silent-zero), Phase 2 deferred items untouched. Side finding: `fb53803` was sitting unpushed since 2026-05-04 — pushed this session together with this update. **Next**: TD-013.5b (`08-scoring-section.png` dual strategy — testid in `QuestionPropertiesPanel.tsx` + Python post-crop fallback).
>
> Last updated: 2026-05-04 — **TD-014.27 ADMINISTRATIVE CLOSE + post-recovery re-validation ✅** — TD-014 block was functionally closed and pushed to master on 2026-04-26 in `c151f27` (with fix-forward `fec7d08` for 3 TS strict-mode errors and `2bbf3b7` for `CourseDoc` shape alignment); CI green over HEAD `473067b` since 2026-04-29 (run `Code Quality: Push on master`). The only loose end was administrative: `tasks.md` TD-014.27 was never ticked `[x]` and `WORKING_CONTEXT.md` still said "uncommitted WIP — commit + push pending". This session closes both. **Recovery context first**: session opened with `pnpm-lock.yaml` deleted from the worktree (root cause unclear — likely an aborted `pnpm install yn` left the package.json with a stray `yn` devDependency and the lockfile gone). Restored both files from HEAD via `git checkout HEAD -- package.json pnpm-lock.yaml` (no `yn` callers in the codebase, so reverting was safe). Fresh `pnpm install --frozen-lockfile` exit 0 in 16m26s — 1058 packages installed clean. **Re-validation pipeline** (mirrors `pnpm verify:ci` order): (a) `verify:types` (build shared-types) exit 0; (b) `verify:lint` exit 0 (only the 2 pre-existing TD-004 `react-hooks/exhaustive-deps` warnings, unchanged); (c) `verify:test` **1882/1882 across 7 packages** (authoring-ui 1039, backend 149, runtime-player 265, scorm-packager 156, phaser-simulations 125, simulation-engine 74, question-engine 74); (d) `tsc -b --force` exit 0 across 9 packages + `e2e tsc --noEmit` exit 0; (e) **F4 OpenAPI regen idempotent** — `gen:api-client` reproduced byte-identical `openapi.json` + `generated.ts` (committed `openapi.hash` `cb7d860059…` unchanged in `git status`); (f) **full local E2E suite**: 165 passed, 3 skipped, 1 flake (`T611.10` from the T611 block — passed cleanly in 24.6s on `--last-failed` re-run; CI absorbs this profile via `retries: 2`). TD-014's own specs `simulation-editor.spec.ts` (.22/.35) green; `simulation-recorder.spec.ts` (.23) skipped correctly without `SIMULATION_ENGINE_URL`. **Side-effects of the E2E run** (left in worktree, NOT included in the closure commit per TD-013 boundary): 8 modified PNGs + 2 untracked PNGs in `docs/user-guide/assets/screenshots/` produced by `docs-screenshots.spec.ts` re-rendering under `vite dev` (vs the `vite preview` baseline used in `c151f27`). The 2 untracked (`13-overview.png`, `13-hotspot-editor.png`) suggest TD-013.5's commit `473067b` may not have actually staged those files — flagged for TD-013 follow-up; not a regression of TD-014. **`.gitignore` updated** with owner-local work folders (`docs/CODEMAPS/`, `piezas-cursos/`, `video/`) and the `.claude/scheduled_tasks.lock` runtime file. **Outcome**: TD-014.27 ticked `[x]` in `tasks.md` with closure note; this `WORKING_CONTEXT.md` updated to remove the stale "uncommitted WIP" claim. **Next**: TD-013.5b — `08-scoring-section.png` dual strategy (testid in `QuestionPropertiesPanel.tsx` + Python post-crop fallback).
>
> Last updated: 2026-04-29 — **TD-013.5 CLOSED ✅** — §13 user-manual captures shipped. Both `13-overview.png` (full overlay, three-column layout) and `13-hotspot-editor.png` (canvas with drawn hotspot) emitted clean by the campaign run, no `-fullpage.png` safety-net fallback. **Pure real-UI flow** inheriting the helpers exercised end-to-end by `simulation-editor.spec.ts` (TD-014.22+.35) — zero store seeding, owner principle from TD-014 honoured ("todas las funcionalidades dadas por operativas tienen que poder usarse y reproducirse"). **Four file changes**: (1) **NEW** `e2e/utils/simulation.ts` — shared module with 4 helpers (`triggerSimDblClick`, `openSimEditor`, `addAndUploadStep`, `drawHotspotOnSelectedStep`) + `SIM_FIXTURE_IMG`. Single source of truth: `simulation-editor.spec.ts` refactored to import from it (-82/+8 LOC, behaviour preserved). (2) `SimulationEditor.tsx` — two additive `data-testid` anchors: `sim-editor-overlay` (overlay root) + `sim-canvas-area` (Konva stage wrapper). Pure additive, no rendering change. (3) `docs-screenshots.spec.ts` — §13 capture block: dblclick widget → addAndUploadStep ×2 → click step 0 → captureElement overlay → drawHotspotOnSelectedStep(0) → captureElement canvas-area → Cancel. Try/catch with `captureFullPage` fallback for robustness (campaign never aborts on a single missing crop, per existing pattern). (4) `simulation-editor.spec.ts` — refactored to use the shared helpers. **Verification**: lint 0 errors; e2e tsc --noEmit exit 0; authoring-ui tsc -b exit 0; authoring-ui vitest 1039/1039 (testids additive); owner-side `pnpm --filter @elearn-studio/e2e docs:screenshots` end-to-end → both PNGs emitted clean. .gitignore reorder + `.reports` entry already committed by owner as `c91012c feat: reorder` (separate concern). **Next**: TD-013.5b — `08-scoring-section.png` dual strategy (testid in QuestionPropertiesPanel.tsx + Python post-crop fallback in `scripts/crop-screenshots.py`).
>
> Last updated: 2026-04-28 — **TD-014.28 CLOSED ✅** — TD-013.5 unblocked. Pure documentation update in `tasks.md`: (1) **TD-014.28 marked complete** with the concrete confirmation that the real-UI path for §13 captures is feasible with zero store seeding, end-to-end proven by `e2e/tests/simulation-editor.spec.ts` (TD-014.22 + .35) during TD-014.27.e's 167/167 full-suite run. Six production surfaces enumerated (dblclick → overlay open via `model.getId()` post-TD-014.27.e fix; `+ Add step` `sim-add-step-btn`; `Upload…` `step-upload-btn` driven by `input[type=file].setInputFiles`; `step-description-input`; Konva hotspot draw via `.konvajs-content` + `page.mouse`; Save & Close / Cancel header buttons). (2) **TD-013.5 entry rewritten** to replace the original "Seed a session via direct store mutation" / "Hotspot drawing can be seeded programmatically" guidance with a 7-step real-UI recipe inheriting all selectors and the Konva drag-gesture pattern from `simulation-editor.spec.ts` (factor helpers into `e2e/utils/simulation.ts` preferred). Two new optional `data-testid` anchors recommended for capture stability (`sim-editor-overlay` overlay root + `sim-canvas-area` Konva stage container) — not blocking, but preferred over `boundingBox` math fallbacks. The `__simStore` window exposure (TD-014.29) is explicitly demarcated as read-only verification surface for asserts (e.g. polling `hotspot.width` for commit-lag), NOT a seeding mechanism. Zero production code changes. Owner principle reaffirmed: *"todas las funcionalidades dadas por operativas tienen que poder usarse y reproducirse"* — captures must exercise the same UI a user has to use; back-door seeding rejected. **TD-014 block now fully complete (10 + 4 = 14 subtasks all ✅)**. **Next**: TD-013.5 — add the §13 capture block to `e2e/tests/docs-screenshots.spec.ts` per the rewritten recipe; then continue TD-013.5b/.6/.7/.8/.9/.10.
>
> Last updated: 2026-04-26 — **TD-014 BLOCK CLOSED** 🟢 ✅ — full closure delivered through .25 (docs), .26 (reviewer pass), .27 (verification). **.25 docs**: `13-software-walkthrough.md` updated to match shipped UX (4-button toolbar `Record… / Import… / Save & Close / Cancel`, `+ Add step`, `Upload…` + `Asset Library`, drag + ↑/↓ + Alt+↑/↓ reorder, Step fields table extended with screenshot/hotspot rows, `Type` → `Type text`); `12-simulations-overview.md` one-liner added on the three build paths (record / import / manual upload); NEW `developer-guide/11-simulation-recorder-architecture.md` (~340 LOC, 5-phase mermaid sequence + stores + data shapes + HTTP surface table + lifecycle + threading + failure modes + naming-asymmetry callout for `RawSimStep` alias); `docs/issues/issues-TD-014.md` cross-cutting sweep section recorded (with reverts on `18-troubleshooting.md` + `20-glossary.md` after owner caught vocabulary leak — saved feedback memory `feedback_user_manual_vocabulary.md`: user-manual files describe author-observable behaviour only; engineering concepts (CORS, ports, process names, store internals) belong in developer-guide). `AUDIT_BASELINE_SYMPTOMS.md` moved to `docs/`, referenced from TD-014 header in `tasks.md`. **.26 reviewer pass**: `docs/issues/issues-TD-014.md` reviewer section appended with severity-ranked findings + dispositions; R-H1 fixed (3 hardcoded `'#6c7086'` literals in `SimulationEditor.tsx:293/306/318` → `colors.textMuted` — caught by .24's theme refactor sweep); R-M1 fixed (architecture doc invented `RawSimStep` type name that doesn't exist outside `backend/api`; renamed primary references to `SimStep` in 8 sites + added Naming-asymmetry callout); R-M2 (spread-with-null pattern) deferred to follow-up `TD-014-followup-1`. **.27 verification + block closure**: (a) `pnpm -r test` green (authoring-ui 1039/1039, backend/api 149/149 — Mongo via Docker after owner started containers); (b) `pnpm -r lint` exit 0; (c) `npx tsc -b` exit 0 across all packages; (d) F4 OpenAPI regen — swagger `AuthoredSimStep` + `SimHotspot` schemas added to `backend/api/src/lib/swagger.ts` (5 required hotspot fields + 13 required + `expectedText` optional on step), `SimConfig.steps.items` switched from `{type:'object'}` to `$ref`, stale `sessionId` removed; regen via `gen-openapi.ts` clean (only `openapi.hash` tracked); (e) **full E2E 167/167 pass** including `simulation-editor.spec.ts` (TD-014.22) + `simulation-recorder.spec.ts` (TD-014.23) — first end-to-end run of the latter, surfaced 5 inherited bugs all fixed: (1) `registerSimBlock.ts:73` `model.id` (Backbone-plain, undefined on GrapesJS components) → `model.getId()` (GrapesJS public API; pre-existing, never surfaced because unit tests called `openPanel('comp-1')` directly bypassing `onDblClick`); (2) `apiClient.ts` hardcoded `credentials: 'include'` → `credentials: init.credentials ?? 'include'` (recorder on :3002 has `credentials: false` by design — paired with `recorderApi.ts` `RECORDER_FETCH_INIT = { credentials: 'omit' }` spread into all 5 endpoints); (3) `phaser-simulations` rollup TS warning at `__tests__/PhaserSimWidget.test.ts:150` — applied owner-picked Option C: tsconfig excludes `**/__tests__/**` + `*.test.*` + `*.spec.*` from build (matching authoring-ui pattern) + per-property type assertion at the offending line (TS narrow-on-callback false positive); (4) `e2e/**/*.spec.ts` ESLint override disabling `no-empty-pattern` (Playwright runtime requires `({}, testInfo)` destructure pattern — owner-picked Option C after rejecting unilateral suppression); (5) `simulation-editor.spec.ts:153` `find('[data-widget="screenshot-sim"]')` returned null → walk `getWrapper().components()` filtered by `c.get('type')` (GrapesJS doesn't auto-attach `data-widget` to component model attributes — same pattern as `docs-screenshots.spec.ts:798-802`); (6) `simulation-recorder.spec.ts` snapshot `activeSessionId` BEFORE Stop & Import click (`handleStopAndImport` calls `reset()` per design, clears the id post-success); (f) `CHANGELOG.md` v0.5.66 entry written covering full TD-014 block (Added / Changed / Fixed / Refactored / Verified sections); (g) WORKING_CONTEXT.md (this update); (h) batch commit + push pending owner confirmation per shared-state action protocol. **Owner directive applied throughout**: pre-existing errors are NEVER an excuse for deferral — every inherited bug surfaced by the full-suite verification was fixed in-scope rather than deferred. **Memory artefacts**: `feedback_user_manual_vocabulary.md` (NEW). **Next**: .28 unblock TD-013.5 (confirm §13 captures now feasible with zero store seeding via real-UI overlay).
>
> Last updated: 2026-04-25 (later×4) — **TD-014 PHASE B STARTED** 🟢 .24 done. Refactor + theme module (`simulationTheme.ts`) + 3 project-wide conventions surfaced (lint suppression policy → AGENTS.md §11.8; widget-vs-app-chrome label-change → own ADR; flat testID naming `<scope>-<action>[-<id>]` → row #13 of consistency ADR). 5 files refactored (SimulationEditor, RecorderLauncherDialog, RecorderLiveView, SessionsPickerDialog, StepForm); label-change removed from 4 app-chrome buttons; error format unified to `'X failed: ${msg}'`; SessionsPicker testID prefix reconciled. Verification: vitest **1039/1039**, tsc 0, lint 0 errors. Pending: **.25 docs** (user-guide §13 + developer-guide §11 + cross-cutting sweep), **.26 reviewer**, **.27 verification + block closure** (incl OpenAPI regen F4 + CHANGELOG v0.5.66 + commit), **.28 unblock TD-013.5**.
>
> Last updated: 2026-04-25 (later×3) — **TD-014 AUDIT SWEEP PHASE A COMPLETE** 🟢 All 10 findings closed (F1, F2, F3, F5, F6, F7, F8, F9, F10, F11; F4 folded into .27.d). **TD-014.38** delivered as a 3-test addition at `__tests__/simulation/RecorderLauncherDialog.test.tsx` (zero production code) — pins the two inline JSX guards at `RecorderLauncherDialog.tsx:86` (Escape + !isBusy) and `:94` (overlay onMouseDown with `target===currentTarget` + !isBusy); closes the docstring-promise vs delivered-coverage gap that has been open since TD-014.19. Verification: authoring-ui vitest **1039/1039** (was 1036, +3 new); `tsc --noEmit` exit 0; `pnpm lint` 0 errors. **Next: Phase B = original tail .24 (refine + theme tokens) → .25 (docs §13/§11) → .26 (reviewer issues file) → .27 (verification + block closure incl. F4 OpenAPI regen + CHANGELOG v0.5.66 + commit + push) → .28 (unblock TD-013.5)**.
>
> Last updated: 2026-04-25 (later×2) — **TD-014 AUDIT SWEEP** 🟡 Subtasks .29–.37 done (9 of 10 findings closed: F7, F5, F6, F11, F1, F2, F10, F3, F8). **TD-014.37** delivered as a 1-test addition at `__tests__/simulation/RecorderLauncherDialog.test.tsx` (zero production code) — pins the `>` vs `>=` boundary at `RecorderLauncherDialog.tsx:62` against silent flip drift; matches the limit number in the error copy rather than the full message so the test is stable across copy rewords. Verification: authoring-ui vitest **1036/1036** (was 1035, +1 new); `tsc --noEmit` exit 0; `pnpm lint` 0 errors. **Pending**: .38 F9 LOW (click-outside + isBusy guards, three tests in the same file). Then original tail .24–.28.
>
> Last updated: 2026-04-25 (later) — **TD-014 AUDIT SWEEP** 🟡 Subtasks .29–.36 done (8 of 10 findings closed: F7, F5, F6, F11, F1, F2, F10, F3). **TD-014.36** delivered as a pure docstring rewrite at `recorderApi.ts:85-93` — corrects the false claim that `no-store` makes the rotating cache-buster unnecessary; documents the two independent reasons it IS required (MITM proxies that ignore `no-store` in the wild + React's `<img>` src-identity network skip across renders); corrected the example from `t=Date.now()` to `t=${tick}` to match the real call site. Zero behavioural change, no test impact. Verification: authoring-ui `tsc --noEmit` exit 0; `pnpm lint` 0 errors. **Pending**: .37 F8 LOW URL length test, .38 F9 LOW click-outside + isBusy guards. Then original tail .24–.28.
>
> Last updated: 2026-04-25 — **TD-014 AUDIT SWEEP** 🟡 Subtasks .29–.35 done (7 of 10 findings closed: F7, F5, F6, F11, F1, F2, F10; F6 partial-scope deferral from .31 fully closed by .35's E2E hotspot-persistence assertion). **Pending**: .36 F3 docstring (Option B), .37 F8 URL length test, .38 F9 click-outside + isBusy guards. Then back to original tail .24–.28. **TD-014.35** delivered as a single-file E2E change in `e2e/tests/simulation-editor.spec.ts` — zero production code edited; adds a Konva draw-mode gesture (`.konvajs-content` locator, `mouse.move → down → move → up` at 0.15/0.85 inset, ~448×252 rect on a 640×360 canvas) between step creation and reorder, plus an extension to the post-reload `persisted` block that asserts the drawn hotspot survives reload (range bounds — > 100 / > 50 / >= 0 — to absorb DPR drift across runners). **Adjacent lint cleanup** folded in (Option A by owner pick after surfacing alternatives B/C/D with trade-offs — initial unilateral `eslint-disable-next-line` proposal withdrawn as protocol breach): `simulation-recorder.spec.ts:31` `no-empty-pattern` resolved by renaming `{}` → `_args` (1-token diff; `argsIgnorePattern: '^_'` in `.eslintrc.cjs` exempts from `no-unused-vars`, zero rule disable). Verification: e2e `tsc --noEmit` exit 0; authoring-ui vitest **1035/1035** (no regression — production untouched); `pnpm lint` **0 errors** (down from 1), 2 pre-existing TD-004 warnings unchanged. All TD-014 work remains uncommitted.
>
> Last updated: 2026-04-24 (later session) — **TD-014 AUDIT SWEEP IN PROGRESS** 🟡 Subtasks .29–.34 done (6 of 10 findings from audit externo 2026-04-24 closed: F7 store exposure, F5 drag-drop real assertion, F6 content persistence [partial scope — hotspot extension pinned to .35], F11 handleSave hardening, F1 interactionType contract + mock-lie cleanup, F2 recorder stop semantics via Option C split). Pending in the sweep: **TD-014.35 (F10 MEDIUM) — E2E draw-mode gesture + inherited hotspot-persistence from .31**; .36 F3 docstring (Option B confirmed); .37 F8 URL length test; .38 F9 click-outside + isBusy guards. Then back to the pre-audit TD-014 tail: .24 refine, .25 docs, .26 reviewer, .27 verification, .28 unblock TD-013.5. New files since last context: `decisions/2026-04-24-recorder-stop-semantics.md` (TD-014.34 Option C ADR, delivered), `packages/authoring-ui/src/__tests__/simulation/RecorderLiveView.test.tsx` (10 tests pinning the 4 Implementation Notes). Verification state: authoring-ui vitest **1035/1035**, backend vitest **149/149**, `tsc --noEmit` exit 0 across both packages, `pnpm lint` exit 0 (2 pre-existing TD-004 warnings only). **All TD-014 work remains uncommitted** in the working tree — when resuming, either commit the sweep in one batch at the end of TD-014.38 or break into per-subtask commits; decision deferred to owner. Also still uncommitted from morning: TD-013.1–.4 (screenshot campaign WIP).
>
> Last updated: 2026-04-24 — **TD-013 IN PROGRESS** 🟡 Subtasks .1–.4 done (§01 9-callout overlay, §02 dashboard New Course dialog, §09 NameField component + capture, §09 widget-dropdown-names via Zustand direct-sync + ensureClickEvent group-role fix). Pending: .5 §13 Sim Editor overlay captures, .5b §08 scoring-section testid + Python fallback, .6 Python crop tool, .7 playbook update, .8 verification, .9 code refinement, .10 block closure + legacy retirement. Current state: `09-widget-dropdown-names.png` emits clean "Show" listbox with named options (Text, Image, Button, Rectangle, HintButton); `09-widget-name-field.png` emits the NameField input row. Work NOT yet committed — next session should start by marking TD-013.4 complete in tasks.md (already done), then commit and proceed to TD-013.5. See 2026-04-23 for TD-012 closure details.
>
> Last updated: 2026-04-23 — **TD-012 CLOSED** ✅ Typed `window.__elearn_editor` boundary + docs-screenshots playbook. Resolved 109 pre-existing TypeScript errors in the `e2e/` package via a single typed augmentation (`e2e/types/elearn-window.d.ts` declaring `__elearn_editor?: E2EEditor` with a minimal surface covering only the methods specs invoke). Every `(window as unknown as Record<string, unknown>).__elearn_editor as { … }` escape-hatch double cast across 17 specs + 2 utils + 1 POM collapsed to `window.__elearn_editor` — same "narrow once at a boundary" discipline TD-004 (v0.5.56) codified on the production side. `EditorPage.ts` made `page` public (standard Playwright POM; was `private readonly page`, triggering TS2341 in 35 sites). `preview-handshake.spec.ts` spec-local `__previewSpy` / `__openerSpy` declared inline via `declare global` to keep the test-harness globals out of the cross-spec surface. `docs-screenshots.spec.ts:138` `Node.remove()` fixed with `instanceof Element` guard (pattern already in use at lines 526/794). Paired with new `docs/developer-guide/10-docs-screenshots-playbook.md` documenting 12 techniques (T-1…T-12) used in the screenshot spec — defensive fallback, tall-panel capture, native `<select>` size-expand, `ensureWidgetIsCentered`, re-locate after slide switches, `editor.select(null)` before slide captures, clean-filename bypass, `pasteSceneDef` sequencing, event-menu close, Moodle context, category-by-index, `ensureClickEvent` guard — plus 6 deferred placeholders with structural reason each. Structural lesson: e2e package was outside TD-004's sweep because its CI path doesn't trip TS errors (Playwright uses its own transformer, and `pnpm -r lint`/`pnpm -r test` skip the e2e typecheck) — future type-safety sweeps must add `pnpm --filter e2e exec tsc --noEmit` to their grep. Verification: `npx tsc --noEmit` in `e2e/` → exit 0 (was 109 errors); `grep 'as unknown as' / 'Record<string, unknown>'` in `e2e/` → 0 matches; `authoring-ui-layer.spec.ts` 22/22 pass in 1m 42s (no runtime regression). Commits: `b55d139` playbook, `43ed28e` typing refactor. v0.5.65. **Previous line (2026-04-23):** TD-010.6 CLOSED ✅ Three E2E tests in `e2e/tests/authoring-ui-layer.spec.ts` (T608.2 "Props tab selected", T608.5 "empty state before selection", T608.5 "returns to empty state after deselection") still used `getByText(/Select a question widget/i)` — pre-TD-010 per-panel copy that the centralised `<PropsEmptyState>` no longer renders ("Select a widget on the canvas…" under `data-testid="props-empty-state"`). Unit tests were migrated at TD-010.3 but the E2E lag surfaced on master `c1792f9` once Playwright re-ran behind the unrelated lint fix. Migrated the 3 assertions to `getByTestId('props-empty-state')` — the same contract pinned by `PropsEmptyState.test.tsx` (6), `PropsTabRouting.test.tsx` (101), and the 2026-04-19 Playwright-MCP live QA pass. Also added `docs:screenshots` script to `e2e/package.json` for canonical invocation of the screenshot campaign. Zero production change. **Previous line (2026-04-19):** TD-011 CLOSED ✅ `registerQuestionBlocks(editor)` moved from inside `registerMediaPlayerWidget` (L448) to the top-level `registerBlocks()` dispatcher (L46) in `packages/authoring-ui/src/editor/registerBlocks.ts`. Before the fix, the entire question-widget family (MC, TF, Fill) was registered only as a side effect of media-player registration — a future refactor that dropped `registerMediaPlayerWidget` from the dispatcher would have silently removed MC/TF/Fill from the block sidebar with no test red, no type error, no runtime warning. 1 file, 2 lines, zero functional change. Discovered via `/graphify packages/authoring-ui/src --mode deep` (first documented case where the tool surfaced a latent coupling that the 769 unit tests + 165 E2E tests could not express). Verification: `tsc -b` exit 0, authoring-ui 769/769 pass (unchanged), E2E `question-widget.spec.ts` 30/30 pass in 3m19s. Commit `3fbb519`. v0.5.64. **Previous line (2026-04-18):** TD-009 + TD-010 CLOSED ✅ Widget persistence on rapid slide switches, and Props-tab empty-state cleanup. TD-009 root-caused three overlapping races: (1) React 18 StrictMode double-invoking `EditorCanvas` Effect 2 and firing two concurrent `editor.load()` calls whose stale `loadData()` cleared the canvas; (2) autosave `setTimeout` callback not re-checking `getEditorLoading()`, firing mid-load with empty widgets and PATCHing the current slide; (3) `data-editor-ready` attribute staying `"true"` after `setIsReady(false)` because React hadn't re-rendered — any observer racing on the old slide's ready signal ended up saving stale content into the NEW slide. Fix in two files: `EditorCanvas.tsx` adds `lastLoadContextRef` + `lastLoadPromiseRef` for race #1 and an imperative `containerRef.current.setAttribute('data-editor-ready','false')` for race #3; `initEditor.ts` adds the `getEditorLoading()` recheck inside the timer callback for race #2. TD-010 eliminated the stack of 6 "Select a X widget" placeholders below every Props panel: 6 panels now `return null`, `AppLayout` renders a single centralised `<PropsEmptyState>` from the new `propsEmptyState.tsx` module (kept separate from `AppLayout.tsx` to avoid pulling `konva` into unit tests). New E2E `widget-persistence-across-slides.spec.ts` (2 scenarios) + `PropsEmptyState.test.tsx` (6 tests); `SidebarPanels.test.tsx` updated for the null-return contract. Verification: `tsc -b` exit 0, authoring-ui 769/769, runtime-player 265/265, E2E persistence guard 2/2 pass. v0.5.63.

---

## Current State

| Field | Value |
|---|---|
| **Latest release** | v0.0.1-beta (2026-03-31) |
| **Current version** | v0.5.67 (TD-013 block closed 2026-07-18 — screenshot campaign 100% automated; commit + push at block closure per owner authorization). |
| **Active phase** | Backlog remediation. TD-015/016/018/021 shipped; TD-017 closed (doc defect). Remaining: TD-019 (MEDIUM), TD-020 (LOW), TD-014-followup-1. |
| **Active blocks** | **TD-013 🟢 BLOCK CLOSED 2026-07-18** (11/11 incl. owner-approved .5c). **TD-014 🟢 BLOCK CLOSED** (14/14). Backlog pending triage: TD-015 (CRITICAL)…TD-020. |
| **Unit test counts** | authoring-ui **1049/1049**; runtime-player **278/278** (+13 TD-021) (no change from .27 — verification only, no new tests in this subtask); backend/api **149/149** (no change). |
| **E2E test count** | **167 tests, 167 passing (full-suite green during .27.e)** — `simulation-editor.spec.ts` (TD-014.22) + `simulation-recorder.spec.ts` (TD-014.23) ran end-to-end for the first time during this verification. Recorder spec exercised against live simulation-engine on :3002 (Docker containers brought up by owner). Surfaced 5 inherited bugs that had never run before — all fixed in-scope per owner directive (see Last-updated entry). |

---

## What Was Last Done

- **TD-014.27 — Verification + block closure ✅ (2026-04-26 functional close, 2026-05-04 administrative close + post-recovery re-validation)** — Closes TD-014 block. Functional work shipped in `c151f27` (single-batch commit covering Phase A .29–.38 + Phase B .24–.27); fix-forwards `fec7d08` (3 TS strict-mode errors) + `2bbf3b7` (`CourseDoc` shape alignment) followed before CI went green over HEAD `473067b` (2026-04-29). The 2026-05-04 session re-validated everything from a fresh install (lockfile recovery — see Last updated entry) and ticked the administrative checkbox in `tasks.md`. **Sub-steps** (as committed):
  - **(a–c) Baseline verification** — `pnpm -r test` green (authoring-ui 1039/1039, backend/api 149/149 — Mongo provided by Docker containers brought up by owner mid-session); `pnpm -r lint` exit 0 (only the 2 pre-existing TD-004 `react-hooks/exhaustive-deps` warnings in `useComponentProperty.ts`, unchanged); `npx tsc -b` exit 0 across all packages.
  - **(d) F4 OpenAPI regen** — extended `backend/api/src/lib/swagger.ts` with full `SimHotspot` schema (5 required fields: `x, y, width, height, tolerance`) and `AuthoredSimStep` schema (13 required fields + `expectedText` optional = 14 total fields). Switched `SimConfig.steps.items` from the placeholder `{type:'object'}` to `{$ref:'#/components/schemas/AuthoredSimStep'}`. Removed the stale `sessionId` from `SimConfig` required + properties (legacy field — never emitted by the server). Regenerated via `gen-openapi.ts` → `openapi.json` (gitignored) → `openapi-typescript` → `generated.ts` (gitignored); only `openapi.hash` is tracked. Re-ran (a–c) post-regen — all clean.
  - **(e) Full E2E 167/167** — `simulation-editor.spec.ts` (TD-014.22) and `simulation-recorder.spec.ts` (TD-014.23) ran end-to-end for the **first time** in this run (recorder spec was previously gated and never executed; editor spec was added in TD-014.22 but only proven via unit/integration coverage). The full-suite execution surfaced 5 inherited bugs that the per-subtask vitest verification could not reach — all fixed in-scope per owner directive ("que sean problemas pre-existentes nunca debe ser argumento para posponer su resolución"). The five fixes (in causal order):
    1. **`registerSimBlock.ts:73` — `model.id` → `model.getId()`** (CRITICAL): pre-existing latent bug. `model.id` is the Backbone-plain default ID accessor; on GrapesJS Component models it's `undefined` because GrapesJS overrides `idAttribute` to `cid`-derived. The GrapesJS public API is `getId()`. Why never surfaced before: every unit test for the recorder/editor block called `openPanel('comp-1')` directly with a hardcoded id — the call path through `onDblClick` (used at runtime) was only exercised in E2E. The bug presented as "Save & Close doesn't close the overlay" because `editingComponentId` came back undefined and `handleSave` bailed out at the new TD-014.32 invariant guard.
    2. **`apiClient.ts` — hardcoded `credentials: 'include'` → `credentials: init.credentials ?? 'include'`** (CRITICAL): the elearn backend on :3001 uses cookie-based refresh-token handshake (`include` correct). The simulation-engine on :3002 has `credentials: false` by design — sending cookies triggers a browser CORS preflight that demands `Access-Control-Allow-Credentials: true` which the recorder service does NOT send (and shouldn't, per its security model). Result: every recorder API call from the browser surfaced as `TypeError: Failed to fetch` / `net::ERR_FAILED`. Companion edit in `recorderApi.ts`: added `RECORDER_FETCH_INIT: Pick<RequestInit, 'credentials'> = { credentials: 'omit' }` and spread it into all 5 recorder endpoints (`startRecording`, `captureStep`, `stopRecording`, `listSessions`, `getSession`, `deleteSession`). Documented the CORS contract inline in both files.
    3. **`phaser-simulations` rollup TS warning at `__tests__/PhaserSimWidget.test.ts:150`** (PRE-EXISTING, surfaced by `pnpm dev`): rollup pulled the test file into the build because tsconfig didn't exclude it. Inside, `expect(receivedDetail?.widgetId).toBe('w-quiz-99')` failed type narrowing because `receivedDetail` was assigned inside a callback and TS infers `never` after the assignment branch closes. **Owner directive applied**: full-test scope explicitly demanded zero pre-existing errors propagating through; surfaced 3 options, owner picked **Option C** (strictly best — fix both root causes, not just symptom) — (a) tsconfig excludes `**/__tests__/**`, `*.test.*`, `*.spec.*` from the build (matching authoring-ui's pattern, prevents recurrence on any new test file); (b) cast at the offending line `expect((receivedDetail as { widgetId: string } | null)?.widgetId).toBe('w-quiz-99')` (preserves IDE type-check behaviour, doesn't hide the bug from future maintainers).
    4. **`e2e/**/*.spec.ts` — ESLint `no-empty-pattern` override** (PRE-EXISTING from TD-014.35): Playwright's `test.beforeAll(async ({}, testInfo) => …)` requires the empty destructure pattern as the first arg — runtime validates it. TD-014.35 had previously renamed `{}` to `_args` (Option A) which broke Playwright at runtime. **Owner directive on retry**: justified rule disable rather than further refactor. Surfaced trade-offs B/C/E, owner picked **Option C** ("ok adelante con C esta justificado esta vez") — added a `.eslintrc.cjs` override scoped to `e2e/**/*.spec.ts` only, disabling `no-empty-pattern`, and reverted `_args` to `{}`. The override is annotated with the Playwright-runtime requirement so the suppression is auditable.
    5. **`simulation-editor.spec.ts:153` — `find('[data-widget="..."]')` returned null** (PRE-EXISTING): GrapesJS does NOT auto-attach `data-widget` to component model attributes — only `data-gjs-type` on the rendered DOM (different attribute) and `c.get('type')` on the model. `registerSimBlock` never sets `data-widget`. The fix walks `getWrapper().components()` and filters by `c.get('type') === 'screenshot-sim'` — same fallback pattern documented in `docs-screenshots.spec.ts:798-802` (TD-013-era). Companion edit in `simulation-recorder.spec.ts`: snapshot `activeSessionId` BEFORE the `recorder-live-stop` click, because `handleStopAndImport` calls `reset()` per design (TD-014.34 invariant `c`) which clears the id post-success.
  - **(f) `CHANGELOG.md` v0.5.66 entry** — full block coverage in 5 sections: Added (authoring surface for Software Walkthrough recorder UI; backend recorder endpoints; E2E coverage `simulation-editor.spec.ts` + `simulation-recorder.spec.ts`; documentation `developer-guide/11-simulation-recorder-architecture.md`); Changed (TD-014.24 style consistency sweep + `simulationTheme.ts` extraction + 3 project-wide conventions surfaced as ADRs); Fixed (F1–F11 audit-externo findings + 5 inherited bugs surfaced during .27.e); Refactored (R-H1 + R-M1 from .26 reviewer pass); Verified (test counts, E2E 167/167, lint 0, tsc 0). R-M2 deferred to follow-up TD-014-followup-1 with explicit cross-reference.
  - **(g) `WORKING_CONTEXT.md` updated** — this entry + Last-updated header line + Current State table + Next Steps `.27` flipped to ✅ Done.
  - **(h) Commit + push + CI** — done. Single-batch commit `c151f27` (2026-04-26) covered TD-014 Phase A (.29–.38) + Phase B (.24–.27); fix-forwards `fec7d08` and `2bbf3b7` resolved 3 TS strict-mode build errors and `CourseDoc.id`→`._id` test stub drift respectively. CI green over HEAD `473067b` (run 2026-04-29 `Code Quality: Push on master` + `CI` both `completed/success`). CodeQL clean. Re-validated 2026-05-04 from a fresh install after `pnpm-lock.yaml` recovery (see Last updated entry for the full pipeline).
  - **Verification deltas vs pre-.27**: vitest unchanged (1039 + 149 — .27 added zero tests); E2E **167/167** (was 165/165 + 2 not-run-end-to-end); all gates green (test/lint/tsc/OpenAPI regen). **The TD-014 block is functionally closed; only the commit + push step remains.**

- **TD-014.26 — Reviewer pass ✅ (2026-04-25 → 2026-04-26, uncommitted WIP)** — Generated `docs/issues/issues-TD-014.md` reviewer section with severity-ranked findings + dispositions. **R-H1 (HIGH)** fixed: 3 hardcoded colour literals `'#6c7086'` in `SimulationEditor.tsx:293/306/318` that the .24 theme refactor had missed (caught by post-refactor grep) — replaced with `colors.textMuted` from `simulationTheme.ts`. **R-M1 (MEDIUM)** fixed: `developer-guide/11-simulation-recorder-architecture.md` invented a `RawSimStep` type name that doesn't exist outside `backend/api`'s internal Mongoose layer; renamed primary references to `SimStep` in 8 sites + added a "Naming asymmetry" callout block explaining that `RawSimStep` is a `backend/api`-local alias for the persisted shape, not part of the cross-package contract. **R-M2 (MEDIUM, deferred)**: spread-with-null pattern in legacy step-derivation code — non-blocking for TD-014 closure; tracked as `TD-014-followup-1` with file pointer + reproducer recipe in `issues-TD-014.md`. Reviewer dispositions explicitly call out: zero CRITICAL findings, zero HIGH findings remained open at .26 close, zero functional regressions introduced. **Verification**: authoring-ui vitest 1039/1039 (theme refactor + 3 colour-literal substitutions = no test impact); `tsc --noEmit` exit 0; `pnpm lint` 0 errors.

- **TD-014.25 — Documentation block ✅ (2026-04-25 → 2026-04-26, uncommitted WIP — initial draft included two vocabulary leaks; reverted after owner caught them)** — Four sub-deliverables, with one owner-pick scope correction mid-flight:
  - **(a) `docs/user-guide/13-software-walkthrough.md`** updated to match the shipped UX exactly: header description lists 4 toolbar buttons (`Record… / Import… / Save & Close / Cancel`); `Add step` → `+ Add step`; screenshot field split into `Upload…` + `Asset Library` buttons; reorder line per spec lists drag-drop + ↑/↓ buttons + Alt+↑/↓ keyboard shortcut; Tip on `Record…`; note on `Clear hotspot`; Step fields table extended with `Step screenshot`, `Hotspot tolerance (px)`, `Hotspot` rows; corrected `Type` → `Type text` in interaction type list (matches `SimInteractionType` union member shipped in TD-014.33).
  - **(b) Image placeholders** `13-overview.png` + `13-hotspot-editor.png` cross-referenced into TD-013.5 (same forward-reference policy as the rest of the User Manual v2 placeholders); explicit gating noted in `tasks.md` so TD-013.5 picks them up after TD-014.28.
  - **(c) NEW `docs/developer-guide/11-simulation-recorder-architecture.md`** (~340 LOC): 5-phase Mermaid sequenceDiagram covering authoring-ui ↔ backend/api ↔ simulation-engine ↔ Garage; "Stores" section covering `simStore` (authoring) + `recorderStore` (live session) + `editorStore` ownership boundaries; "Data shapes" section with `SimConfig` / `SimStep` / `SimHotspot` / `Session` shapes + cross-link to OpenAPI generated types; HTTP surface table with all 8 recorder endpoints + their authoring-ui callers; Lifecycle section with start → capture → stop → import flow + the 4 invariants from `decisions/2026-04-24-recorder-stop-semantics.md`; Threading section explaining Playwright-CDP browser pool inside simulation-engine; Configuration section listing the env vars (`VITE_SIMULATION_ENGINE_URL`, `RECORDER_HEADLESS`, etc.); Failure modes with the silent-stop / orphan-session / 404-on-DELETE cases; "Naming asymmetry" callout explaining `RawSimStep` as a backend-local alias. Cross-linked from `developer-guide/index.md` + `developer-guide.md`.
  - **(d) Cross-cutting user-manual sweep** — grepped `docs/user-guide/*.md` + `docs/user-manual-v2-scope.md` for simulation/recording/recorder/screenshot-sim/session/hotspot/Software Walkthrough; documented affected files in `docs/issues/issues-TD-014.md` § "TD-014.25d sweep". Initial draft included two **vocabulary leaks** that the owner caught: §10 of `18-troubleshooting.md` referenced CORS / process names / browser console, and `20-glossary.md` added a `Session ID` entry mentioning "persisted recording manifest". Owner pulled the alarm with: "¿Has añadido información de bugs y fixes en ficheros de un manual de usuario?". Surfaced 4 options; owner picked custom **Option C** (revert §18 + §20 to baseline; keep §12 + §13 changes which describe author-observable behaviour only). Reverts applied — `18-troubleshooting.md` baseline restored verbatim (the original "ten problems" intro + original §10), `20-glossary.md` Hotspot definition restored to its original Interactive-Diagram-only scope. Saved `feedback_user_manual_vocabulary.md` to memory codifying the rule for future docs work: **user-manual files describe author-observable behaviour only; engineering concepts (CORS, ports, process names, store internals, console traces) belong in developer-guide, never user-guide**. `AUDIT_BASELINE_SYMPTOMS.md` moved from repo root to `docs/`; `tasks.md` TD-014 header references the new path.

- **TD-014.24 — Refine the generated code ✅ (2026-04-25, refactor + 3 project-wide conventions surfaced — uncommitted WIP)** — 13 sub-decisions captured in `decisions/2026-04-25-simulation-style-consistency.md` (full ADR with alternatives, reasoning, trade-offs per sub-decision); two **project-wide conventions** surfaced and codified in their own ADRs: `decisions/2026-04-25-button-label-change-convention.md` (widget vs app-chrome label-change rule) and the **flat testID naming convention** documented inline in row #13 of the consistency ADR. **Scope correction vs spec**: AssetPickerModal.tsx does not exist (asset library uses GrapesJS `editor.AssetManager.open()`); StepForm.tsx added to scope (omission from spec, shares 6 colour tokens with the listed files). Final scope: 5 production files refactored. **Deliverables**: (1) new `simulationTheme.ts` with 5 sections (colors / typography / spacing / buttons / surfaces) using pragmatic-extraction philosophy (consensus tokens + outliers stay inline with explanatory comments). (2) Theme refactor across SimulationEditor, RecorderLauncherDialog, RecorderLiveView, SessionsPickerDialog, StepForm. (3) Reconciliations: dialog heading 15→16, input fontSize 13→12, button vertical padding 7→6, error-banner horizontal padding 10→16. (4) Naming collision fix: green `btnPrimary` → `buttons.success`. (5) `btnDanger` migrated to theme (closes 2026-04-24-recorder-stop-semantics ADR commitment). (6) **Label-change removed universally from app-chrome buttons** per convention ADR — 4 sites: `Starting…` (RecorderLauncher), `Uploading…` (StepForm), `Loading…` (SessionsPicker.Refresh), `Importing…` (SessionsPicker.Import). The two SessionsPicker cases were not in my original survey — surfaced during execution; convention application confirmed by owner. (7) Error message format unified to `'X failed: ${msg}'`: StepForm + RecorderLiveView (×2 sites) + 2 test assertions in `RecorderLiveView.test.tsx` updated. (8) `SessionsPickerDialog` testID prefix reconciled to flat convention `<scope>-<action>[-<id>]`: `session-row-${id}` → `sessions-picker-row-${id}`, `session-import-${id}` → `sessions-picker-import-${id}`. An interim hierarchical attempt (`sessions-picker-row-${id}-import`) was corrected after owner pointed out the deviation from the codebase pattern observed in `sim-step-item-${i}`, `recorder-live-discard`, etc. 7 test references updated; 0 E2E references (verified). (9) Action 3 outcome: production simulation namespace had 0 hits of `any`/`as any`/`as unknown as`; tests have 5 legitimate `as unknown as Editor` casts (test scaffolding, not production discipline scope). **Verification**: authoring-ui vitest **1039/1039** (no regression); `tsc --noEmit` exit 0; `pnpm lint` 0 errors (2 pre-existing TD-004 warnings unchanged). **Methodological observation**: this subtask exposed three project-wide conventions previously undocumented — (1) lint suppression policy (codified in `AGENTS.md` §11.8 by owner during .35), (2) widget vs app-chrome label-change rule (own ADR), (3) flat testID naming `<scope>-<action>[-<id>]` (documented in row #13 of the consistency ADR). Future refactors that touch UI surfaces should grep `/decisions/` first.

- **TD-014.38 — Click-outside + isBusy guard tests ✅ (2026-04-25, F9 LOW resolved — uncommitted WIP). AUDIT-EXTERNO SWEEP PHASE A COMPLETE (10/10 findings closed).** Pure test addition in `packages/authoring-ui/src/__tests__/simulation/RecorderLauncherDialog.test.tsx`, zero production code changes. New describe block `RecorderLauncherDialog — click-outside + isBusy guards (TD-014.38 / F9)` with 3 tests pinning two inline JSX guards: (1) `closes on mouseDown on overlay backdrop (click-outside happy path)` — exercises `RecorderLauncherDialog.tsx:94` (`onMouseDown={e => { if (e.target === e.currentTarget && !isBusy) onClose() }}`); accesses the overlay via `container.firstChild` (the role=dialog form is its child, so the only top-level rendered node is the overlay); fires `mouseDown` directly on it making `target === currentTarget`. (2) `does NOT close on Escape while isBusy=true` — sets `useRecorderStore.setState({ isBusy: true })` BEFORE render so the dialog mounts already subscribed to the busy state (avoids any subscription-update timing question between setState and fireEvent in the same tick); fires Escape on `getByRole('dialog')`; asserts `onClose` NOT called. (3) `does NOT close on click-outside while isBusy=true` — same setState-before-render pattern + `mouseDown` on overlay; asserts `onClose` NOT called. **Why a new dedicated describe**: the click-outside happy-path test was previously absent from the file altogether (only Escape happy-path existed in render+lifecycle), so 1 of the 3 tests had to be net new regardless; folding the busy-state negation tests into render+lifecycle would split F9 coverage across two locations. New dedicated block keeps F9 traceability in one place — matches the F8/TD-014.37 grouping pattern. Updated the file's top-level docstring bullet from `Escape + click-outside close unless isBusy` to `Escape + click-outside close unless isBusy (TD-014.38 — completes coverage promised in this docstring since TD-014.19)` so the audit trail of "promise → late delivery" is visible from the header. **Verification**: file-scoped `vitest run` 22/22 (was 19, +3 new); full authoring-ui vitest **1039/1039** (was 1036, +3 new); `tsc --noEmit` exit 0; `pnpm lint` 0 errors (2 pre-existing TD-004 warnings unchanged).

- **TD-014.37 — URL length limit test ✅ (2026-04-25, F8 LOW resolved — uncommitted WIP)** — Pure test addition in `packages/authoring-ui/src/__tests__/simulation/RecorderLauncherDialog.test.tsx`, zero production code changes. Added a new `RecorderLauncherDialog — URL length limit (TD-014.37 / F8)` describe block parallel to the existing `title length limit` block (rejected merging both into a generic "length limits" — they cover independent concerns and parallel grouping matches the per-concern pattern the rest of the file uses). Single test pins the boundary at `RecorderLauncherDialog.tsx:62` (`url.length > MAX_RECORDER_URL_LENGTH`): builds an overlong URL of length `MAX + 20` (= `'https://example.com/' + 'a'.repeat(MAX_RECORDER_URL_LENGTH)`), submits, awaits `recorder-dialog-error`, asserts the error text contains the literal limit (regex matches `2048` inside the actual copy `"url must not exceed 2048 characters"`) and asserts `mockStart` was NOT called. **Guards against `>` vs `>=` flip**: with `>=`, `length === MAX` would no longer be allowed and the error message would fire on a boundary the rest of the codebase + the backend mirror in `simulation-engine/src/routes/recorder.ts` don't share — silent semantic drift before this test. **Why match the limit number, not the full message**: keeps the test stable across copy rewords; the contract is "the limit appears in the error", not "the message uses this exact wording". Updated the file's top-level docstring to list "URL length limit (TD-014.37 — guards against `>` vs `>=` flips)". Imported `MAX_RECORDER_URL_LENGTH` alongside the existing `MAX_RECORDER_TITLE_LENGTH` import — single statement, no module path duplication. **Verification**: file-scoped `vitest run` 19/19 passing (was 18, +1 new); full authoring-ui vitest **1036/1036** (was 1035, +1 new); `tsc --noEmit` exit 0; `pnpm lint` 0 errors (2 pre-existing TD-004 warnings unchanged).

- **TD-014.36 — `getLiveScreenshotUrl` docstring corrected ✅ (2026-04-25, F3 MEDIUM resolved — uncommitted WIP)** — Pure docstring rewrite in `packages/authoring-ui/src/api/recorderApi.ts:85-93`, zero behavioural change, no lint/type/test impact. The previous text claimed *"`Cache-Control: no-store`, so a rotating `t=Date.now()` query is NOT needed to defeat cache"* — directly contradicted by the actual call site at `RecorderLiveView.tsx:160` (`const liveUrl = \`${getLiveScreenshotUrl(activeSessionId)}?t=${tick}\`` where `tick` increments on a `POLL_INTERVAL_MS = 500ms` timer). New docstring documents the two independent reasons the rotating cache-buster IS necessary, neither solved by `no-store`: (a) **network layer** — corporate proxies performing MITM TLS inspection ignore `Cache-Control: no-store` in the wild and serve stale responses; rotating the URL makes each poll distinct so the proxy can't key on a single URL. (b) **React layer** — `<img>` only triggers a network fetch when its `src` attribute changes by string identity across renders; re-rendering with the SAME `src` is a no-op regardless of server cache headers, so the rotating query is what forces a fresh fetch on every poll tick by mutating `src`. Also corrected the example in the docstring from `t=Date.now()` to `t=${tick}` to match the actual call site (the call uses a poll counter, not a timestamp — relevant because `Date.now()` would change every render not every poll). Cross-references to `RecorderLiveView.tsx:160` + `POLL_INTERVAL_MS` + `tick` state added so future maintainers can trace the contract back to the consumer without searching. Original task spec referenced lines 80-93 + `RecorderLiveView.tsx:100`; actual coordinates are 85-93 (docstring) + 160 (call site) — same content, line drift from intermediate edits. **Verification**: authoring-ui `tsc --noEmit` exit 0; `pnpm lint` 0 errors (2 pre-existing TD-004 warnings unchanged); no test changes needed (docstrings have no runtime effect).

- **TD-014.35 — E2E draw-mode gesture + hotspot persistence ✅ (2026-04-25, F10 MEDIUM resolved + F6 partial-scope closure from TD-014.31 — uncommitted WIP)** — Pure E2E change, single-file diff in `e2e/tests/simulation-editor.spec.ts`. **Block 1** (between step #2 creation and the drag-reorder phase): `simStore.addStep` sets `selectedStepIndex` to the newly-added step (=1 after Step 2), so the block first clicks `sim-step-item-0` to bring Step 1 into focus and force HotspotCanvas to mount in draw-mode (zero-size sentinel from `addStep` triggers `isDrawModeHotspot()` true). Locates the Konva stage via `page.locator('.konvajs-content').first()` (only stable selector — Konva renders no testid surface; sim editor mounts exactly one stage so `.first()` is unambiguous). Drags from `(stageX + 0.15·width, stageY + 0.15·height)` to `(stageX + 0.85·width, stageY + 0.85·height)` via `page.mouse.move → down → move({steps:10}) → up`. The 0.15/0.85 inset (a) keeps both corners away from `clampPoint`'s borders so neither axis is squashed by `rectFromPoints`, (b) yields a ~448×252 rect in canvas-units (CANVAS_W/H = 640×360 in `HotspotCanvas.tsx:25-26`) — comfortably above the > 100 / > 50 thresholds. Immediate assertion uses `expect.poll` on `steps[0].hotspot.width > 100` to absorb React-commit lag between `mouseUp` and `updateStep` landing in the store, then snapshots the full hotspot and asserts the four range bounds individually (`width > 100`, `height > 50`, `x >= 0`, `y >= 0`). **Block 2** (closing TD-014.31's deferred F6 hotspot-persistence): replaced the trailing comment that said "Hotspot-content persistence is verified in TD-014.35" (forward-reference now obsolete) with a concrete `persistedHotspots` assertion — reads `__simStore?.getState().config?.steps.map(s => s.hotspot)` after reload + re-open, asserts `length === 2`; index 0 (Step 2 — never drawn) `toMatchObject({ width: 0, height: 0 })` to confirm the sentinel survives, index 1 (Step 1 — drawn pre-reorder, swapped to index 1 by the drag) gets the same four range bounds. Belt-and-braces against `handleSave` regressions that would silently drop hotspot fields + end-to-end validation of the backend round-trip on `SimHotspot` shape. **Why ranges, not exact equality**: `rectFromPoints` clamps + rounds, and DPR shifts integer values across runners (CI vs local Win/Mac). **Adjacent pre-existing lint resolution** (folded in per owner directive 2026-04-25 — Option A picked by owner after rejecting an initial `eslint-disable-next-line` proposal as unilateral protocol breach; alternatives B/C/D surfaced with trade-offs before owner pick): `simulation-recorder.spec.ts:31` `no-empty-pattern` on Playwright's `test.beforeAll(async ({}, testInfo) => …)` (latent since TD-014.23) resolved by renaming empty destructure `{}` → `_args` (1-token diff). `argsIgnorePattern: '^_'` already configured in `.eslintrc.cjs:31` exempts the rename from `no-unused-vars` — zero rule disable, zero precedent for suppression. **Verification**: e2e `tsc --noEmit` exit 0; authoring-ui vitest **1035/1035** passing (no regression — production code untouched); `pnpm lint` **0 errors** (down from 1); 2 pre-existing TD-004 warnings in `useComponentProperty.ts` remain (unchanged count, tracked separately under TD-004's exhaustive-deps follow-up). Full E2E run deferred to TD-014.27 per WORKING_CONTEXT batch policy. TD-014.31's `⚠️ PARTIAL SCOPE` marker left in place for historical traceability — the F6 coverage gap is now closed end-to-end.

> **▶ RESUME POINTER (for next session):** **TD-013.5 closed; TD-013.5b is next.** Goal: produce `08-scoring-section.png` via the **dual strategy** (owner decision D-3, 2026-04-23) — (a) primary path: add `data-testid="scoring-section"` to the Scoring heading/fieldset root inside `QuestionPropertiesPanel.tsx`, replace the current `captureFullPage(page, '08-scoring-section.png')` at `docs-screenshots.spec.ts:428` with `captureElement(page, '[data-testid="scoring-section"]', { filename: '08-scoring-section.png', padding: 12 })`; (b) fallback path: add a `08-scoring-section` entry to `scripts/screenshots-crop.json` (source: `08-scoring-section-fullpage.png`, deterministic crop rect) and have the Python post-crop tool (TD-013.6) consume it. The fallback is idempotent — checks file mtime vs source and skips if the testid path succeeded. Single filename, two redundant paths. After .5b: TD-013.6 (Python tool itself), .7 (playbook update), .8 (verification), .9 (refine), .10 (block closure + legacy retirement). **Current TD-013.5 batch uncommitted in working tree**: 4 files (`e2e/utils/simulation.ts` NEW + 3 modified in `e2e/tests/` + `packages/authoring-ui/src/components/simulation/SimulationEditor.tsx`). Suggested commit message: `feat(TD-013.5): §13 sim-editor captures via real-UI helpers + 2 data-testid anchors`. Pre-existing untracked artefacts (`docs/CODEMAPS/`, `piezas-cursos/`, `video/`, `.claude/scheduled_tasks.lock`) are explicitly out of scope per owner directive 2026-04-28.

- **TD-014.34 — Recorder stop semantics: 3-action split (Stop & import / Stop / Discard) ✅ (2026-04-24, F2 HIGH resolved via Option C — uncommitted WIP)** — Closes audit-externo F2. Owner decision 2026-04-24: rejected Options A (single destructive Cancel) and B (single preserving Cancel + rename copy) because both leave the "Cancel" button semantically overloaded; adopted Option C — three distinct actions with single-responsibility semantics. Full ADR in `decisions/2026-04-24-recorder-stop-semantics.md` (Accepted/delivered 2026-04-24). Changes in `RecorderLiveView.tsx`: removed `handleCancel`; added `handleStopPreserve` (stop → reset with count-aware toast `N step(s) saved to Sessions`) and `handleDiscard` (`window.confirm` → stop → `deleteSession` → reset). Four **owner-agreed implementation invariants** pinned: (a) `stop()` failure MUST NOT call `reset()` — `recorderStore` deliberately keeps `recording:true` on failure for retry, calling `reset()` blindly would wipe `activeSessionId` and orphan the backend session (zombie); (b) `deleteSession` 404 is silent success (resource already gone) — same `toast.info('Recording discarded')` as happy path; (c) Stop-preserve toast reports real captured-step count including the zero case; (d) Discard button is leftmost, separated from the stop-group by a flex-1 `actionSpacer` — final left-to-right layout `[Discard] · [Capture] [Stop] [Stop & import]` removes the "rightmost = close" misclick vector. Esc key routes to `handleDiscard` (retains its own confirm). New `btnDanger` style token (red-accent border #f38ba8, neutral fill) — moves to `simulationTheme.ts` at TD-014.24. Test IDs: `recorder-live-cancel` removed; `recorder-live-discard` + `recorder-live-preserve` added; `recorder-live-stop` re-semantics to "Stop & import" (E2E `simulation-recorder.spec.ts:71` already uses it for that flow — no E2E change). **Tests added (+12)**: new `RecorderLiveView.test.tsx` (10 tests): preserve happy-path, zero-step, stop-fail (invariant a); discard confirm-cancel, happy-path, 404-silent (invariant b), non-404 warning + reset, stop-fail (invariant a); Esc routing; DOM ordering contract (invariant d). `SimulationEditor.integration.test.tsx` (+2): Stop preserve + Discard happy-path through the full overlay stack. **Verification**: authoring-ui vitest 1023 → **1035/1035**; `tsc --noEmit` exit 0; `pnpm lint` exit 0 (only the 2 pre-existing TD-004 warnings, unchanged).

- **TD-014.33 — Backend `interactionType` contract + frontend mock-lie cleanup ✅ (2026-04-24, F1 HIGH resolved — uncommitted WIP)** — Closes audit-externo F1. **Scope expansion beyond the raw plan**: the backend's `AuthoredSimStep` in `backend/api/src/types/simulation.ts` was missing `interactionType` + `expectedText` entirely — not just the emission, the TYPE itself. The fix had to start at the type definition (otherwise the emission would fail strict TS checks), not just at the route. Added `SimInteractionType = 'click' | 'hover' | 'type'` + required `interactionType` + optional `expectedText` to the backend type, mirroring the authoring-ui union with a docstring flagging the lockstep contract. Route `backend/api/src/routes/simulations.ts` step-derivation emits `interactionType: 'click'` on every step; `expectedText` stays absent (recorder doesn't produce 'type' steps). New backend contract test `"seeds interactionType='click' on every imported step (no expectedText)"` asserting both fields across every step in the import response — placed as a dedicated `it` block (not folded into the hotspot one) so a regression surfaces as a distinct signal. **Frontend mock cleanup** in `SimulationEditor.integration.test.tsx:165-193`: removed the two pre-seeded `interactionType: 'click'` lines from `mockImportSimulation.mockResolvedValue`, replaced with a 7-line comment documenting the "mock that lies" anti-pattern. The mock is now deliberately minimal — if the backend regresses, the backend contract test fails upstream, not the integration test. **Verification**: backend vitest 148 → **149/149**; authoring-ui vitest **1023/1023** (no regression — StepForm's `step.interactionType ?? 'click'` select fallback handles the missing field); `tsc` exit 0 in both packages; eslint clean on all 4 changed files.

- **TD-014.32 — `handleSave` hardening in SimulationEditor ✅ (2026-04-24, F11 CRITICAL resolved — uncommitted WIP)** — Closes audit-externo F11. Rewrote `SimulationEditor.handleSave` as async with four explicit paths: (1) `!editor || !editingComponentId` → silent no-op (invariant violation — bail without side effects); (2) `editor.getWrapper()?.find(...)[0]` returns falsy → `toast.error('Cannot save: widget no longer exists on canvas')` + abort (panel stays open); (3) `requestSave?.()` rejects → `toast.error(\`Save failed: ${msg}\`)` + abort; (4) happy path → `closePanel()` only AFTER `await requestSave?.()` resolves. Wired `useToast()`. Inline comment explains why the overlay must stay open on failure (SaveErrorBanner is covered by the overlay; toast z-index 9999 is the only visible channel). **Tests added**: new `SimulationEditor — TD-014.32 handleSave (F11)` describe block with 4 tests + `makeMockEditor` / `makeMockComponent` helpers. Mocks mirror real `useEditorStore` shape (`editor: Editor | null`, `requestSave: (() => Promise<void>) | null`) so drift on either side surfaces here first. **Verification**: authoring-ui vitest 1019 → **1023/1023**; `tsc --noEmit` exit 0; `pnpm lint` exit 0.

- **TD-014.31 — E2E persistence content verification (F6 CRITICAL, partial scope) ✅ (2026-04-24, uncommitted WIP)** — Closes audit-externo F6 for steps content; hotspot-persistence is **deferred to TD-014.35** via an explicit `⚠️ PARTIAL SCOPE` marker in `tasks.md` and a cross-referencing comment inline in `simulation-editor.spec.ts`. After reload + re-open, asserts content across three independent surfaces of truth: (a) step-list DOM (`[data-field="description"]` textContent order = `['Step 2', 'Step 1']`), (b) StepForm inputs (click step-0 → `step-description-input` value + first textarea value), (c) `__simStore` full shape per step via `getState().config?.steps.map(s => ({description, instruction, hasScreenshot, hasScreenshotKey, interactionType}))` with `toEqual` for exact-match — catches silent field drops (e.g. a `handleSave` bug that copies only some fields). Screenshot URL check uses `Boolean(s.screenshotUrl && s.screenshotUrl.length > 0)` because presigned URL signatures are non-stable. TD-014.35 MUST extend the post-reload `persisted` block to also return `hotspot` and assert `{ width > 100, height > 50, x >= 0, y >= 0 }` — not a zero-size sentinel. Without .35's extension, F6 coverage stays partial.

- **TD-014.30 — Rewrite drag-drop E2E assertion (F5 CRITICAL) ✅ (2026-04-24, uncommitted WIP)** — Closes audit-externo F5. The original spec asserted `toContain(expect.stringContaining(''))` — a no-op that passed for any array. **Scope correction**: the planned assertion `toEqual(['Step two: enter password', 'Step one: click login'])` would have read empty strings because the helper only filled Instruction, not Description (the step-list display div renders `step.description`). Fix: (a) helper signature widened to `addAndUploadStep(page, description, instruction)` filling both fields; (b) new `data-testid="step-description-input"` on Description input in `StepForm.tsx:114` so the helper doesn't rely on input ordering; (c) `data-field="description"` added to the step-list display div in `SimulationEditor.tsx:215`; (d) E2E assertion replaced with a pair — DOM-level (`[data-field="description"]` textContent per step item → `toEqual(['Step 2', 'Step 1'])`) AND store-level (`window.__simStore?.getState().config.steps.map(s => s.description) → toEqual(['Step 2', 'Step 1'])`). Belt-and-braces guards against a DOM render that accidentally matches the old order by coincidence or a store update that doesn't reach the DOM.

- **TD-014.29 — Expose `__simStore` + `__recorderStore` on window for E2E (F7 HIGH) ✅ (2026-04-24, uncommitted WIP)** — Closes audit-externo F7. Pattern mirrors the `__elearn_editor` / `__elearn_store` exposure from `EditorCanvas.tsx:93-96` (same `import.meta.env.DEV || import.meta.env.VITE_E2E_MODE === 'true'` gate + `typeof window !== 'undefined'` guard). Changes: (1) tail block in `simStore.ts` + `recorderStore.ts` assigns the store hooks to window; (2) `src/types/globals.d.ts` adds `__simStore?` + `__recorderStore?` reusing the existing `ElearnStoreBind` shape (loose `unknown` so no Zustand types leak into the ambient file); (3) `e2e/types/elearn-window.d.ts` adds narrow E2E-specific bind types `E2ESimStoreBind` + `E2ERecorderStoreBind` with the state-shape subset specs actually read — policy inherited from the TD-012 "narrow once at the boundary" discipline; (4) `simulation-recorder.spec.ts:81-92` replaced the fictitious `window.__recorderSessionId` (API that never existed; every CI run before this fix accumulated orphan sessions in Garage) with `window.__recorderStore?.getState().activeSessionId` + `expect(sessionId).not.toBeNull()` belt-and-braces assertion.

- **TD-013.1–.4 — Screenshot campaign automation, first 4 subtasks (2026-04-24, uncommitted WIP)** — Owner directive (2026-04-23): zero manual captures, zero manual post-processing; all 55 placeholders produced by `pnpm --filter @elearn-studio/e2e docs:screenshots`. **.1 §01 `01-full-ui-annotated.png`**: 9-callout overlay via existing `addCallouts(page, [...])` with markers for (1) top toolbar (testid added to `TopToolbar.tsx`), (2) left sidebar, (3) canvas, (4) right sidebar container, (5) Layers tab, (6) Styles tab, (7) Props tab, (8) Actions tab, (9) Anim tab; callout 1 offset `{x: 400, y: 24}` to land in the toolbar gap instead of overlapping the "New Course" button. Captured with `page.screenshot({ fullPage: true })` to a clean filename (not `captureFullPage` which suffixes `-fullpage`), `removeCallouts(page)` in `finally`. Caption in `docs/user-guide/01-welcome.md` updated "1-4" → "1-9". **.2 §02 `02-create-course.png`**: pre-fixture capture at end of campaign — navigates back to `/`, clicks dashboard New Course button, `captureElement(page, '[role="dialog"][aria-label="New Course"]', { padding: 10 })` then Cancel to restore state. **.3 §09 `09-widget-name-field.png`**: the original capture attempted `input[name="name"]` but the field did not exist in the DOM (pre-TD-013.3 state). New `packages/authoring-ui/src/components/sidebar/NameField.tsx` mounts at the top of the Props tab for EVERY widget via `<NameField>` in `AppLayout.tsx`, wrapped in `PanelErrorBoundary`; uses `useComponentProperty<string>(selected, 'name', '')` (T648 canonical pattern). Owner guidance: `name` property must exist for ALL widget types uniformly — it is what the NameField maps to. `[data-testid="widget-name-input"]` filled with `HintButton`, `captureElement(page, '[data-testid="widget-name-field"]', { padding: 12 })`. 19 new tests in `PropsTabRouting.test.tsx` cover the contract (17 widget types renders-check + null-selection + null-editor). **.4 §09 `09-widget-dropdown-names.png`**: reload-based approach in the original plan was rejected — the GrapesJS → course round-trip subtly strips the `name` trait for unpersisted widgets, so the `<select>` rendered bare ids (itllc, ioj4v, ifkxf, irq3q, irz8s) instead of names. **Pivot: Zustand direct-sync (T-15 in the playbook, still to be documented)**. `EditorCanvas.tsx` onReady now exposes `window.__elearn_store = useEditorStore` alongside `__elearn_editor` (DEV or `VITE_E2E_MODE=true` only). Ambient declarations mirrored in `packages/authoring-ui/src/types/globals.d.ts` and `e2e/types/elearn-window.d.ts` (no `as any` at call sites — same narrow-once-at-a-boundary discipline TD-004/TD-012 enforced). Spec reads the live GrapesJS component tree (whose `name` traits are authoritative — NameField wrote them there), derives `{id, name, type, bounds, layer}` for each top-level component, calls `store.setState({ course: { ...c, slides: slides.map(…widgets) } })`. WidgetIdParam re-renders the named-option `<select>` branch, T-3 (native `<select>` size-expand) applied, clean screenshot emitted. **Side-fix in the same block**: `ensureClickEvent()` previously queried `page.getByRole('tab', { name: /^Click$/i })` — EventSelector does NOT use `role="tab"`; each event is a `<div role="group" aria-label="Click">` with a toggle button (see `EventSelector.tsx:33-45`). Old query always returned 0 hits, so the SECOND invocation (from the dropdown-names block) re-opened the `+ Event` menu, failed to find a "Click" menuitem (already added), and left the menu open — visually overlaying the widget-target `<select>` and polluting the clip with event-menu items (questionAnswered, enterSlide, …). Fixed to `getByRole('group', { name: /^Click$/i })` + defensive menu-close fallback. **Verification**: `npx tsc -b packages/authoring-ui` exit 0; `e2e/` `tsc --noEmit` exit 0; `docs:screenshots` spec green in 32.4s; `09-widget-dropdown-names.png` renders "Show" label + listbox with "— select widget —, Text, Image, Button, Rectangle, HintButton". **Status**: WIP, uncommitted at end of 2026-04-24 session — next session commits TD-013.1–.4 + settings / playbook edits, then moves to .5 (Sim Editor) / .5b (scoring-section testid) / .6 (Python crop tool). `tasks.md` TD-013.4 subtask already marked `[x]` with full implementation notes.

- **TD-012 — e2e/ typed `window.__elearn_editor` boundary + docs-screenshots playbook (2026-04-23, v0.5.65)** — Two paired tasks closed together. **(1) Typing refactor:** resolved 109 pre-existing TypeScript errors in the `e2e/` package. Two root-cause patterns, both the exact escape hatches TD-004 (v0.5.56) erradicated on the production side: (a) `(window as unknown as Record<string, unknown>).__elearn_editor as { … }` scattered across 20 specs + utils + POM (~70 sites); (b) `EditorPage.ts:36` declared `private readonly page: Page` but 10 specs accessed `editorPage.page` directly for `waitForResponse` / `evaluate` / `waitForTimeout` (~35 TS2341 sites). Fix: new `e2e/types/elearn-window.d.ts` with ambient `Window` augmentation declaring `__elearn_editor?: E2EEditor` — a minimal typed surface (addComponents, select, getSelected, getWrapper, getComponents, runCommand, store, BlockManager + typed Component/Components/Wrapper) covering only the methods specs invoke. Pulling `grapesjs` as a devDep would add ~400 kB of types for test-only typing; the minimal interface documents the actual contract and keeps e2e self-contained. Every `(window as … Record<string, unknown>).__elearn_editor as { … }` collapsed to `window.__elearn_editor`. `EditorPage.ts` made `page` public (standard Playwright POM). `preview-handshake.spec.ts` spec-local `__previewSpy` / `__openerSpy` declared inline via `declare global { interface Window { … } }` at top of file (kept out of cross-spec ambient surface). `docs-screenshots.spec.ts:138` `Node.remove()` fixed with `instanceof Element` guard (pattern already at lines 526/794). **(2) Paired documentation:** new `docs/developer-guide/10-docs-screenshots-playbook.md` — 12 techniques (T-1…T-12) used in the screenshot spec, each with failing naive approach + fix + affected manual section. Covers defensive fallback per capture, tall-panel capture (viewport resize + CSS neutralisation), native `<select>` size-expand, `ensureWidgetIsCentered`, widget re-locate after slide switches (GrapesJS regenerates IDs), `editor.select(null)` before slide captures, clean-filename bypass for `page.screenshot`/`popup.screenshot`, `pasteSceneDef` + `selectOption` sequencing for §14 builders, event-menu toggle close, Moodle context in §16, category-by-index crops, `ensureClickEvent` guard. Tabulates the 6 deferred placeholders (02-create-course, 09-widget-name-field, 09-widget-dropdown-names, 13-overview, 13-hotspot-editor, 01-full-ui-annotated) with structural reason each. Spec header updated to point at playbook first. Cross-linked from `developer-guide/index.md` + `developer-guide.md`. **Structural lesson**: e2e was outside the TD-004 sweep because its CI path doesn't trip TS errors (Playwright uses its own transformer; `pnpm -r lint`/`pnpm -r test` skip the e2e typecheck). Any future type-safety sweep must add `pnpm --filter e2e exec tsc --noEmit` to its grep. Follow-up I-01 in `issues-TD-012.md` tracks a proposed CI step. **Verification**: `npx tsc --noEmit` in `e2e/` exit 0 (was 109 errors); `grep 'as unknown as' / 'Record<string, unknown>'` in `e2e/` → 0 matches; `authoring-ui-layer.spec.ts` 22/22 pass in 1m 42s. Commits: `b55d139` (playbook), `43ed28e` (typing refactor). Self-review: `docs/issues/issues-TD-012.md` (0 CRITICAL/HIGH/MEDIUM/LOW; 2 INFO, both non-blocking).

- **TD-010.6 — E2E assertions migrated to centralised PropsEmptyState testid (2026-04-23)** — CI on master `c1792f9` stayed red in the E2E job after the `addCallouts`/`removeCallouts` lint fix landed. Three tests in `e2e/tests/authoring-ui-layer.spec.ts` — `T608.2 › clicking Props tab makes it selected`, `T608.5 › Props panel shows empty state before any widget is selected`, `T608.5 › switching away from MC widget to no selection returns to empty state` — failed with `element(s) not found` on `getByText(/Select a question widget/i)`, the per-panel copy that TD-010 (v0.5.63, 2026-04-18) removed when it replaced 6 `*PropertiesPanel` fallback divs with a single centralised `<PropsEmptyState>` rendering `"Select a widget on the canvas to edit its properties."` under `data-testid="props-empty-state"`. Unit tests had been migrated at TD-010.3 — the 3 E2E assertions were the residual lag that only surfaced when Playwright was re-triggered by the unrelated `c1792f9` lint fix. **Fix**: 1 file, 3 assertions — `getByText(/Select a question widget/i)` → `getByTestId('props-empty-state')` at lines 78, 170, 208. Same contract already pinned by `PropsEmptyState.test.tsx` (6), `PropsTabRouting.test.tsx` (101), and the 2026-04-19 Playwright-MCP live-browser QA (`qa-01-initial.png`), so the full test stack now asserts the same observable. **Bundle**: `e2e/package.json` also gains a `docs:screenshots` script (`playwright test tests/docs-screenshots.spec.ts --headed`) so the screenshot campaign has the same canonical entry point already present in `authoring-ui/package.json`. Zero production change — the UI already renders the testid-marked node. **Structural lesson**: when UI copy moves under a `data-testid`, the migration must sweep every locator across unit + integration + E2E in the same PR; otherwise the E2E tripwire surfaces two blocks later behind an unrelated CI re-trigger.

- **Mermaid syntax compatibility fix for PyCharm preview (2026-04-19, `e8c15c5`)** — Two diagrams in `docs/developer-guide/09-authoring-ui-architecture.md` ("GrapesJS instance lifecycle" + "Unified persistence") failed to render in PyCharm's Markdown preview. Root causes: (1) `;` inside sequence-diagram message labels — Mermaid treats `;` as a statement separator so `A->>B: foo(); bar()` produces `Expecting 'SOLID_OPEN_ARROW' ... got 'NEWLINE'`; fixed by `;` → `,` in 6 message labels. (2) 12 graph subgraph titles and node labels containing `—` (em-dash), `·` (middle dot), or `→` (arrow) were unquoted — PyCharm's parser rejects these with `ALIAS or CLOSE_SQUARE expected`; fixed by wrapping in `"..."`. Also replaced 4 `&lpar;`/`&rpar;` HTML entities with literal parens inside quoted labels. Sequence-diagram message text and Note labels are free-form until end-of-line so em-dashes there stay unquoted. **Rule bake-in**: added new "Renderer compatibility" subsection + 3 checklist items to `.claude/skills/elearn-docs-technical/SKILL.md` so future diagrams follow the PyCharm-compatible syntax by construction. GitHub preview parity preserved — the quoted-label syntax is supported in every mermaid version that handles `:::class`.

- **Developer-guide §09 authoring-ui architecture (2026-04-19, `398bb38`)** — New page `docs/developer-guide/09-authoring-ui-architecture.md` (522 lines, 7 Mermaid diagrams) documenting the internal architecture of `packages/authoring-ui`: stack, package map, UI composition, GrapesJS lifecycle (Effect 1/2 + autosave + cleanup with the 4 race-fix pins from TD-009/T639.8), Zustand-vs-Backbone source-of-truth split with the T648 canonical pattern, unified-persistence sequence (requestSave Layer 1/2 + 5 callers) with the parallel requestCourseMutation mini-diagram for course-meta ops, stores table, hooks table, preview postMessage handshake, Props empty-state router (TD-010), key-files table. Content verified against current source of EditorCanvas, initEditor, storageManager, useComponentProperty, editorStore, AppLayout, propsEmptyState. Cross-links wired in `developer-guide/index.md` + `developer-guide.md` index; `01-architecture.md` pointer after "GrapesJS Storage Integration"; `08-persistence-flow.md` header hand-off to §09 Unified persistence to avoid server/React duplication.

- **TD-010.5 + T651.5 backlog cleanup (2026-04-19)** — Two stale unchecked items in `tasks.md` closed. **T651.5** ("Run full test suite + push + verify CI green") had been effectively done — T651.4 recorded the 1533/1533 local pass and T1000.E2E recorded CI run `24582182042` green in 17m02s — but the box was never ticked; marked `[x]` with the cross-reference. **TD-010.5** (visual QA of the centralised Props empty-state across 17 widget types) had only partial coverage: the 2026-04-19 Playwright-MCP live-browser pass exercised just 2 states (button-selected, nothing-selected). Gap closed deterministically by new file `packages/authoring-ui/src/__tests__/layout/PropsTabRouting.test.tsx` (101 tests, all green): pins the null-return contract for the 5 previously-unpinned custom panels (Button / MediaPlayer / AudioNarration / ProgressBar / VolumeControl) across every non-matching type in the 17-type grid, and renders an AppLayout-mirror `PropsTabFragment` for each of the 17 types + null to assert exactly one `[data-testid="props-empty-state"]` for the 6 Styles-tab types (+ null) and zero for the 11 custom-panel types. The invariant "only one panel visible" is now provable by composition: `hasCustomPropsPanel(type)` routing is pinned by the pre-existing `PropsEmptyState.test.tsx` (6 tests), each of the 7 panels' null-return contract is pinned by its own suite, and the new file covers the composed Props-tab fragment. Verification: authoring-ui 769 → **870/870 pass** (+101); `npx tsc --noEmit` exit 0. No production code change.

- **TD-011 — `registerQuestionBlocks` hidden inside `registerMediaPlayerWidget` ✅ (TD-011 CLOSED, v0.5.64, 2026-04-19)** — `/graphify packages/authoring-ui/src --mode deep` surfaced `registerBlocks()` as the top god node (degree 13, 423 nodes / 656 edges / 20 communities across `src/`). Source inspection of the dispatcher revealed L448 of `packages/authoring-ui/src/editor/registerBlocks.ts` called `registerQuestionBlocks(editor)` from inside `registerMediaPlayerWidget()` instead of from the top-level `registerBlocks()` dispatcher alongside `registerSimBlock`/`registerPhaserSimBlock`. Consequence: the entire question-widget family (MC, TF, Fill) was registered only as a side effect of media-player registration — if a future refactor dropped `registerMediaPlayerWidget` from the dispatcher, MC/TF/Fill would silently disappear from the block sidebar with no test red, no type error, no runtime warning. Neither the 769 unit tests nor the 24-spec / 165-test Playwright suite caught this because `registerMediaPlayerWidget` was still being called, so the misplaced line kept working by accident. **Fix**: 1 file, 2 lines — cut L448 from `registerMediaPlayerWidget`, add `registerQuestionBlocks(editor)` to `registerBlocks()` L46. Zero functional change — only the registration-call location moved. **Verification**: `npx tsc -b` exit 0; authoring-ui vitest **769/769 pass** (unchanged — the functional contract is identical); existing E2E `question-widget.spec.ts` **30/30 pass** in 3m 19s on chromium covering T601.0a/0b/0c (block discovery in sidebar) + T601.1 (drag-to-canvas for MC) + T601.5/6 (TF/Fill variants) + persistence + T611 mandatory-gate + T620.5 + T621.5 + T631.3/4/6 + T639. **Methodological note**: TD-011 is the first documented case in this project where the graphify structural-analysis tool surfaced a latent coupling that test coverage alone cannot express. The bug is about structural cleanliness, not behaviour — a dispatcher that gains a hidden side effect inside one of its branches passes every existing test until the branch itself is removed or reordered. Structural properties like "every widget-family registrar is called from the top-level dispatcher" are better expressed as graph queries than as Playwright assertions. Commit: `3fbb519`.

- **Live Browser QA pass (2026-04-19, head = `b3fc646`, no code change)** — Validated TD-009 + TD-010 + `ensureWidgetIsCentered` in a real Chrome session via Playwright MCP bridge against `http://localhost:3000`. Smoke: 0 console errors, 2 pre-existing Zustand-devtools warnings, all business endpoints 200/201. TD-010 verified on both paths: 1 `[data-testid="props-empty-state"]` node when nothing is selected (correct "Select a widget" copy); 0 empty-state nodes + 1 `[data-testid="button-properties-panel"]` when a Button is selected — no stacking in either direction. TD-009 verified: added button `irux` on slide 1 at (400, 250) via `__elearn_editor.addComponents` + `addStyle`, rapid hop slide 1 → slide 2 → slide 1, `ed.getWrapper().find('#irux').length === 1`, tree exactly `[{id:'irux', type:'button', name:'Button'}]`. Autosave traffic healthy: 6 `PATCH /courses/:id/slides/:sid` requests all 200 OK across the 2 slides, no widget-loss symptoms. Two `ERR_ABORTED` network entries on CSS-snippet URLs (`body { margin: 0; ... }` and `* { box-sizing: border-box; }` fetched as `href` targets) are a pre-existing GrapesJS quirk unrelated to this work — carried forward unchanged, not a regression. Screenshots `qa-01-initial.png` + `qa-02-button-selected.png` saved under `.playwright-mcp/`. Verdict: SHIP, 0 blockers.

- **Screenshot-campaign tooling: `ensureWidgetIsCentered` helper (2026-04-18, `698caf5`, test-only)** — During a docs-screenshots run on v0.5.63 it became obvious that every widget added via `__elearn_editor.addComponents(...)` lands at the canvas origin (0,0), producing top-left-clipped thumbnails across `04-*-props.png`, `17-slide-*-final-fullpage.png`, etc. (recurrence of the T600/T630 drag-position pattern — `addComponents` has no coord arg, unlike the real `block:drag:stop` path). Instead of rewriting the drag engine, added a tiny helper `ensureWidgetIsCentered(page, blockId, x=400, y=250)` in `e2e/utils/screenshot.ts` that calls `component.addStyle({position:'absolute', left, top})` on the created block — same mutation `initEditor.ts::block:drag:stop` performs for real drag-drops. Wired after all 18 `addBlockById` call sites in `docs-screenshots.spec.ts`. Verification: regenerated full campaign (exit 0, 42.1s); `04-button-props.png` panel renders cleanly; `17-slide-1-final-fullpage.png` shows Button + Rectangle centered on slide 1; `17-slide-5-final-fullpage.png` shows the Phaser sim widget centered and visible. `e2e/tsc --noEmit` clean for the changed files (pre-existing warnings in `volume-control-widget.spec.ts` unrelated).

- **TD-009 — Widgets lost when switching slides rapidly ✅ (TD-009 CLOSED, v0.5.63)** — Three overlapping races closed. (1) React 18 StrictMode double-invoke of `EditorCanvas` Effect 2 on mount ran two `editor.load()` concurrently; the cancelled first run's `loadData()` still fired synchronously after resolve, wiping the canvas after the test/user added a widget. Fix: `lastLoadContextRef` + `lastLoadPromiseRef` in `EditorCanvas.tsx` — the StrictMode twin short-circuits and awaits the in-flight load's promise instead of starting its own. Lifecycle correction: Effect 1's cleanup clears those refs so when the editor is destroyed+recreated (StrictMode or course switch) the FRESH editor still gets its initial load — without this reset 9 reload-based E2E tests failed until the fix landed. (2) Autosave `setTimeout` callback in `initEditor.ts` did not re-check `getEditorLoading()`; a timer scheduled before a slide switch could fire during `editor.load()` and PATCH an empty widget list to the current slide. Fix: `if (getEditorLoading()) return` inside the callback — the explicit `requestSave` inside Effect 2 already persisted any pending edits before load, so the timer-driven save is redundant and unsafe. (3) **Discovered during TD-010 verification**: `setIsReady(false)` at the top of Effect 2 only scheduled a re-render — the `data-editor-ready` DOM attribute didn't flip to `"false"` in the same tick. Any observer (Playwright's `waitFor`, user code reacting to "ready") could see the previous slide's stale `"true"` and race ahead. In the worst case, a widget observed during that window was serialised INTO the new slide's PATCH by the flush-before-switch save, because the editor tree still held the old slide's content at attribute-check time. Fix: imperatively `containerRef.current.setAttribute('data-editor-ready','false')` right after `setIsReady(false)` so every observer sees the load-in-progress state synchronously. **Verification**: 2 new `initEditor.test.ts` tests (autosave-during-load guard, control case); new E2E `widget-persistence-across-slides.spec.ts` — single-hop + multi-hop (5-slide round-trip), 2/2 pass (100% failure rate before fix). Authoring-ui 763 → **769/769**; runtime-player **265/265**; `tsc -b` exit 0; `docs-screenshots` campaign still green. **Discovery chain was instructive**: the spec was green after the first two fixes, then regressed 3/3 the moment TD-010 moved `selectedComponentType` into AppLayout (extra re-renders surfaced race #3). Forwarding browser console logs + a temporary `[STORE]` log in `storageManager.ts` showed the editor tree still held the OLD slide's components at `readySignal` attach — pointing at the attribute-flip race.

- **TD-010 — PropertiesPanels: stacked empty-state placeholders → single centralised empty-state ✅ (TD-010 CLOSED, v0.5.63)** — Before the fix, selecting any widget caused all 7 PropertiesPanels to mount; 6 of them rendered their own "Select a X widget to edit its properties." fallback below the actually-matching panel, producing up to 6 pieces of dead copy the author had to scroll past. Fix: the 6 previously-stacking panels (`Question`, `Button`, `MediaPlayer`, `AudioNarration`, `ProgressBar`, `VolumeControl`) now `return null` when they don't apply (`PhaserSim` already did). Extracted `hasCustomPropsPanel(type)` and `<PropsEmptyState selectedType />` into a new `packages/authoring-ui/src/components/layout/propsEmptyState.tsx` module (separate from `AppLayout.tsx` so unit tests import the helpers without pulling `SimulationEditor → react-konva → konva` into the Node vitest run — konva needs the native `canvas` module). `AppLayout.tsx` now renders the 7 `PanelErrorBoundary`-wrapped panels only when `hasCustomPropsPanel(selectedComponentType)` is true; otherwise a single `<PropsEmptyState>` explains either "Select a widget" (nothing selected) or "Use the Styles tab" (selected widget has no custom Props panel — text/image/rectangle/score-*). Tests: 6 new tests in `PropsEmptyState.test.tsx` pin the contract (11 widget families → custom panel, 6 → Styles-tab copy, null → select-a-widget, single-node invariant); 7 panel suites in `SidebarPanels.test.tsx` updated to assert `container.firstChild === null` instead of looking for the vanished "Select a X widget" text. **Verification**: `tsc -b` exit 0; authoring-ui 763 → **769/769 pass** across 34 files (+1 file).

- **TD-008 — Manual-audit pass: 4 minor UI/UX bugs fixed before User Manual v2 ✅ (TD-008 CLOSED, v0.5.62)** — Pre-requisite cleanup ahead of the user-manual v2 rewrite (`docs/user-manual-v2-scope.md`). Functional review of v0.5.61 surfaced 4 inconsistencies. Fixed in two commits, both CI-green. **Bug #1 (LOW — `62153ca`)**: `PhaserSimPreviewModal` captured `window.innerWidth`/`Height` once at mount; resize left stale dimensions. Added `useState(viewport)` + `useEffect` resize listener with cleanup. **Bug #2 (LOW — `62153ca`)**: `properties: {}` vs `[]` inconsistency across 14 widget defaults. GrapesJS's Style Manager `PropertyComposite` internally calls `this.get('properties') || []` — object shape is crash bait despite `GENERATED_CONTENT_TYPES` omit-on-load already masking it. Standardised to `[]` uniformly across `registerBlocks.ts`, `registerQuestionBlocks.ts`, `registerSimBlock.ts`, `registerPhaserSimBlock.ts`, and `converters.ts::NavButtonChildDef`. Test `registerBlocks.test.ts` updated: `toEqual({})` → `toEqual([])` for all 14 types. **Bug #3 (LOW — `62153ca`)**: Phaser placeholder `sim-complete` only fired in `mode === 'demo'`; practice/assessment modes never emitted completion → courses with Phaser sims could not progress. Confirmed T036 (per-simType scene builders) was NOT in immediate backlog (`grep T036 tasks.md WORKING_CONTEXT.md` → 0 matches), so fixed directly: removed mode guard; all modes auto-complete at 2 s with score `100` (demo/practice) or `config.passingScore` (assessment). T036 will replace the placeholder entirely. **Bug #4 (HIGH UX — `de0ad2e`)**: Actions Editor widget-target dropdown rendered `{w.id}` (cryptic GrapesJS IDs like `c32kq3`) instead of the human-readable `name` trait set in Props → Name. Fixed in four places: (a) `BaseWidget.name?: string` added to `@elearn-studio/shared-types` (optional, backward-compatible); (b) `widgetsFromGrapesjs` reads `c.get('name') ?? attributes.name ?? ''` (trimmed, empty → undefined) and populates top-level `widget.name`; (c) `grapesjsFromWidgets` restores `attributes.name = widget.name` on reload; (d) `ActionItemEditor.tsx` `<option>` label changed to `{w.name || w.id}` while `value` stays `w.id` (technical routing key untouched). Backward-compat: legacy courses with `name` only in `widget.properties` continue to work via the T611 attribute restoration loop — verified by dedicated regression test. **Test coverage**: 6 new round-trip tests in `converters.test.ts` under `Bug #4 — name trait round-trip for Actions Editor dropdown`. Authoring-ui suite 755 → **761/761 pass** across 33 files; runtime-player **265/265**; `pnpm -r lint` 0 errors; `npx tsc -b` exit 0. **CI**: runs `24608241954` (bugs #1-#3) and `24608814942` (bug #4) both success with full E2E. Self-review `docs/issues/issues-TD-008.md` (0 CRITICAL/HIGH/MEDIUM/LOW). Unblocks user-manual v2 §3 (Actions Editor) — authors name widgets and the dropdown shows those names.

- **TD-006 — `_isEditorLoading` audit: native events confirmed insufficient ✅ (TD-006 CLOSED, v0.5.61)** — TD-006 asked whether GrapesJS's native `storage:start:load` / `storage:end:load` events could replace the `_isEditorLoading` module flag in `initEditor.ts`. The 2026-04-17 ADR (T646.5) had already documented the answer was no, but flagged it as "future work" pending re-verification. The 2026-04-18 audit closes that loop empirically against the currently-installed `grapesjs@0.21.13`. **Audit method**: static inspection of `node_modules/.../grapesjs/dist/grapes.mjs` (the compiled bundle IS the runtime behaviour for the question "does Storage.load yield before loadData runs?"), backed by a permanent vitest regression test that reads the same file and asserts the structural call order. Live `grapesjs.init()` was tried first but hangs in vitest jsdom — grapesjs uses iframe canvases / DOM measurement APIs jsdom does not implement; static inspection is equivalent and stable across CI runs. **Findings**: `EditorModel.prototype.load` (line 61352 of `grapes.mjs`) `await`s `this.Storage.load(options)` THEN calls `this.loadData(result)` synchronously. `StorageManager.prototype.onEnd` (line 42110) fires `storage:end:load` inside `Storage.load()` — before its promise resolves. So the timeline of `editor.load()` is: `storage:start:load` → custom storage resolves → `storage:end:load` → `EditorModel.load` awakens → `loadData(result)` → cascade of `component:add × N`. Using `storage:end:load` to clear the gate would lift it three steps before the `component:add` cascade arrives → `triggerAutosave` would start the debounce timer incorrectly → exactly the bug the gate exists to prevent. **No production-code change**: `_isEditorLoading` flag, `setEditorLoading`/`getEditorLoading` accessors, `EditorCanvas.tsx` set/clear sites, `triggerAutosave` reading `getEditorLoading()` — all unchanged. **Added**: (1) `decisions/2026-04-18-editor-loading-flag.md` recording the audit method, evidence (with exact line numbers and source snippets from `grapes.mjs`), and reopen criteria. Extends `decisions/2026-04-17-editor-loading-flag.md`. (2) `packages/authoring-ui/src/__tests__/editor/grapesEventOrder.test.ts` with 5 structural assertions: source loads (sanity), `Storage.load(options)` precedes `loadData(result)` textually in `EditorModel.prototype.load`, `StorageManager.onEnd` references `endLoad` for type `'load'`, `StorageEvents.endLoad === 'storage:end:load'` (event name pinned), regex regression-guard `yield.*?Storage\.load[\s\S]*?loadData\(result\)`. Runs in 17 ms (no editor init). (3) `docs/issues/issues-TD-006.md` self-review (0 CRITICAL/HIGH/MEDIUM/LOW). **Verification**: `npx tsc -b` exit 0; new audit file 5/5 pass; full authoring-ui suite 750 → **755/755 pass** (32 → 33 files); `pnpm -r lint` 0 errors; `_isEditorLoading` references unchanged in 5 files (1 production: `initEditor.ts`; 1 React caller: `EditorCanvas.tsx`; 2 test files; 1 audit doc). **Reopen criteria**: the regression test fails — happens iff a future grapesjs release inverts the call order. Until then, TD-006 is closed with rationale "Native events timing insufficient".

- **TD-005 — `useExtendedProperty` shallow-merge contract documented + lost-key detector ✅ (TD-005 CLOSED, v0.5.60)** — Audit (2026-04-18) of the codebase invalidated the original ticket premise: `extendedProperties` is **not** flat today and never has been since question widgets shipped. `BaseQuestionExtendedProps.scoring` is `{ weight, attempts, mandatory? }`; `MCExtendedProps.options` is `MCOption[]` of `{ id, text, isCorrect }`; `PhaserSimExtendedProps.sceneDef` carries nested `nodes`/`edges`/`steps`. So the suggested "Option B literal" (`FlatExtendedProperties` type + warn-on-any-object) would have broken typecheck and spammed warnings on every keystroke during question editing. Deep-merge was rejected on four grounds: (1) YAGNI — no caller needs it; the T639 `getLatest()` + spread pattern at the call site already solves partial nested updates correctly. (2) Array semantics ambiguity — deep-merge libraries treat arrays inconsistently (concat vs replace vs index-merge); the current wholesale-replace contract is what every caller assumes. (3) Bundle cost — lodash is not a dependency, hand-rolled deep-merge would carry the same risks without the library's coverage. (4) Debuggability — shallow-replace is transparent; readers see exactly what happens. **Resolution reframed**: document the actual contract + add a dev-only diagnostic for the bug pattern that matters (silent loss of sibling keys when shallow-replacing a nested object with a partial-shape one). **Implementation**: (a) JSDoc on `useExtendedProperty.update()` explicitly stating that `newValue` REPLACES nested objects entirely, with the canonical T639 `updateEp({ ...getLatest(), [subKey]: { ...getLatest()[subKey], ...patch } })` pattern in the docblock as the way to do partial updates. (b) Dev-only `console.warn` gated by `process.env.NODE_ENV !== 'production'`, fires only when both `prev` and `newValue` are plain objects AND `newValue` omits keys present in `prev` — message includes the subKey name, the lost keys list, and the literal fix snippet. (c) Local `isPlainObject(v)` helper that excludes arrays on purpose (wholesale array replacement is intentional for options lists, scene-def nodes, etc.). (d) 4 regression tests in `useComponentProperty.test.ts` under a new `TD-005 shallow-replace contract + lost-key warning` describe: legitimate full-shape replace works without warning, warning fires when partial-shape loses keys (with full message inspection — `[TD-005]`, subKey, lost keys, `getLatest()` suggestion all asserted), no-warning when same-shape, no-warning for array-of-objects replacement. All four use `vi.spyOn(console, 'warn')` with `mockRestore()` in `finally` so no other test sees the spy. **No type changes** — `extendedProperties` stays `Record<string, unknown>` so the heterogeneous shipped shapes remain legal. **No production cost** — the warning branch is tree-shaken by Vite/esbuild in prod bundles. **Verification**: `npx tsc --noEmit` exit 0; `useComponentProperty.test.ts` 40 → **44/44 pass**; full authoring-ui suite 746 → **750/750 pass** (32 files); ESLint unchanged (2 TD-004 historical warnings, no new ones). Self-review `docs/issues/issues-TD-005.md` (0 CRITICAL/HIGH/MEDIUM/LOW). Commit: `d6b54f5`.

- **TD-007 — Unified course meta-operations via `requestCourseMutation` ✅ (TD-007 CLOSED, v0.5.59)** — Closes the drift T651 explicitly scoped out. **Two-layer design adapted from T651 (not mirrored — see post-mortem below)**: Layer 1 `packages/authoring-ui/src/lib/courseMutation.ts` exports pure `performCourseMutation<R>(apiCall, hooks)` — narrows thrown errors to a message string, returns `R | undefined`, zero Zustand/React dependency. **Layer 2 lives as a plain always-available store action inside `editorStore.ts`** (non-nullable field, no setter, no `EditorCanvas` registration) — wires `setIsSaving`/`setSaveError`/`bumpCacheVersion` with `bumpCache: true` as default invariant (escape hatch `{ bumpCache: false }` kept for future callers; no current site uses it). **Migrated 8 call sites**: `TopToolbar.tsx` → `handleNewSlide`, `handleDeleteSlide`, `handleSaveSettings`; `SlideList.tsx` → `handleAddSlide`, `handleDuplicate`, `handleDelete`, `commitRename`, `handleDrop`. Pattern: `const updated = await useEditorStore.getState().requestCourseMutation(() => courseApi.X(...)); if (!updated) { toast.error(...); return }; setCourse(updated); /* op-specific side effect */`. **Bug fixed as side effect**: `commitRename` (`updateSlide`) and `handleDrop` (`reorderSlides`) did not invoke `bumpCacheVersion()` → storageManager `courseCache` held pre-mutation slide list → stale title/order could surface on next `editor.load()`. Both paths now bump cache automatically via the default. **Local flags deleted**: `isAdding` (1 site) and `isProcessing` (3 sites) collapsed into global `useEditorStore(s => s.isSaving)` selector; child `SlideItem` prop renamed `isProcessing` → `isBusy`. `SaveErrorBanner` and "Saving…" toolbar badge now surface for all 8 mutations (previously only TopToolbar's 3). **Toast unified**: 5 SlideList sites upgraded from `toast.warning` to `toast.error`. **Design decision**: `setCourse(updated)` stays at caller (forgetting produces immediately-visible bug → review catches it); `bumpCacheVersion` goes inside the helper (forgetting was invisible until cache hit → helper enforces). **Post-mortem**: the first push `769a12a` placed Layer 2 inside `initEditor.ts` mirroring T651 literally, with a nullable store field and `if (!rcm) return` caller guards. That created a null window between app mount and EditorCanvas Effect 1 during which E2E fixture `editorPage.addSlide()` (which clicks Add Slide immediately on page load) produced a silent no-op → dozens of tests timed out at 30s × 3 retries → CI E2E step cancelled at 27 min (run `24601830271`). Fix-forward `4cd6bb8` moved Layer 2 into the store directly because `requestCourseMutation` has no editor dependency. Key guardrail extracted: **Layer 2 placement follows the Layer 1 primitive's dependencies, not file-layout symmetry with T651.** **Test coverage**: 8 tests for `performCourseMutation` (success, error, non-Error narrowing, hook ordering, optional hooks) + 5 tests for the store action (success + cacheVersion bump, `bumpCache:false`, error path, **null-window regression guard** verifying `typeof rcm === 'function'`, non-Error narrowing). `SlideList.test.tsx` + `sidebar/SidebarPanels.test.tsx` no longer install mock closures — they exercise the live store action against mocked `courseApi`. **Verification**: `grep "isAdding\|isProcessing"` → 0 matches; `grep "bumpCacheVersion"` → only store definition/action body + test files + caller comments (zero direct call-site invocations); `npx tsc --noEmit` exit 0; authoring-ui suite 733 → **746/746 pass** across 32 files; `pnpm -r lint` 0 errors. **CI**: run `24602663078` completed success in 17m 08s — all 27 steps green including full E2E. ADR: `decisions/2026-04-18-course-mutation.md`. Self-review: `docs/issues/issues-TD-007.md` (0 CRITICAL/HIGH/MEDIUM; includes full post-mortem).

- **TD-004 micro-polish — ESLint warnings reduced + defaultValue stability tests ✅ (v0.5.58)** — When TD-004 first landed, the `const comp = component as ELearnComponent | null` narrowing lived at function-body scope so it could be shared by `useComponentProperty`'s effect, `useState` initializer, and `update()` writer. That layout expanded the pre-existing `react-hooks/exhaustive-deps` warning set from 2 (the historical `defaultValue` / `readValue`) to 4 — both effects gained `comp` as a missing closure dependency. Fix: moved the narrowing INSIDE each `useEffect` (`if (!component) return; const comp = component as ELearnComponent; …`). Since the cast is type-only, `comp` is identity-equivalent to `component` at runtime — no deps-array semantics change, zero runtime behaviour change. Outer-scope narrowing kept only where it was still used by the `useState` initializer / `update()` writer / `useExtendedProperty.readValue` / `update`. Both `eslint-disable-next-line` comments refined to state the contract explicitly: `defaultValue` / `readValue` are fallback-only constants for the panel lifetime; adding them to deps would `off`+`on` the Backbone listener on every render of the panel (keystroke-level churn with zero benefit) since the change handler reads the live model via `comp.get(key)` / `readValue()` — never a captured closure of those values. Added 2 regression tests documenting the contract: `useComponentProperty — defaultValue stability contract (TD-004)` and `useExtendedProperty — defaultValue stability contract (TD-004)` — both rerender with a different `defaultValue` after mount and assert (a) the emitted value stays on the GrapesJS model value and (b) the Backbone listener count does not change. Verification: `npx eslint useComponentProperty.ts` → 2 warnings (was 4); `npx tsc --noEmit` → exit 0; useComponentProperty suite 38 → **40/40**; full authoring-ui suite 731 → **733/733**; `pnpm -r lint` → 0 errors across the monorepo. Commit: `ea86279`.

- **TD-003 — T642 (FLAKE-03) confirmed resolved + T643 runtime forEach guards ✅ (TD-003 CLOSED)** — Bundled resolution of two independent tech-debt items. **T642 (FLAKE-03)**: audit confirms the per-test course isolation was already delivered by the prior T642.2 fixture fix — `e2e/global-setup.ts:15-18` no longer calls `POST /courses` (comment explicitly documents the removal); `e2e/fixtures/auth.ts:56-86` creates a fresh course per test in the `editorPage` fixture setup phase and deletes it in teardown with a re-fetched token. Every CI run since T642.2 has passed FLAKE-03; no code change was required. **T643 runtime extension**: authoring-side was already guarded in T643.1/T643.2 (`validateSequence.ts` with `?.forEach`, `converters.ts` with hard-coded `actions: []`), but the runtime player had 6 unguarded iteration sites over legacy MongoDB data. Fix: `packages/runtime-player/src/actions/executor.ts` → `for (const action of actions ?? [])` at the load-bearing recursion point (transparently protects `condition.then`/`else`, `loop.body`, and `call-sequence` via delegation); `packages/runtime-player/src/actions/dispatcher.ts` → `attachWidget(sequences ?? [])`, `fireSlideEvent` outer+inner (`allWidgetSequences ?? []` + `(sequences ?? []).filter(…)`), `fireWidgetEvent((sequences ?? []).filter(…))`; `packages/runtime-player/src/actions/builtins/callSequence.ts` → `run(shared.actions ?? [])` (belt-and-suspenders). Deliberate non-fixes: `condition.ts` and `loop.ts` left untouched (executor guard covers them via `this.run()` recursion; adding local guards would be dead defensive code no test can distinguish); `querySelectorAll().forEach()` sites (9 of them across `index.ts`, `handlers.ts`, `visibility.ts`) left unguarded — NodeList is never undefined, adding guards would create a false impression of risk. Schema-level migration of legacy documents deferred — proper long-term fix but carries data-loss risk without dry-run audit; runtime guards remain as cheap safety net. Coverage: 9 new regression tests in `actions.test.ts` (new describe `TD-003 — forEach guards on legacy MongoDB data`) feed `@ts-expect-error` legacy shapes (undefined arrays) to every affected entry point and assert no-op resolution instead of TypeError. Runtime-player suite: 256 → **265/265 pass**. Authoring-ui: **731/731 pass** (no regression). `npx tsc --noEmit` both packages: exit 0. `docs/issues/issues-TD-003.md` generated. Version bumped to v0.5.57.

- **TD-004 — `ELearnComponent` typed view over GrapesJS `Component` ✅ (TD-004 CLOSED)** — Pure type-safety refactor. Added `packages/authoring-ui/src/types/ELearnComponent.ts` exporting `ELearnComponent = Component & { get(key: string): unknown; set(key: string, value: unknown): void; on(event: string, handler: () => void): void; off(event: string, handler: () => void): void }`. Replaces the private `GjsComponent` type previously defined inline at `useComponentProperty.ts:4-9`. The interface loosens only the four methods that hooks use with arbitrary string keys/events (because GrapesJS's `Component.get<A extends keyof ComponentProperties>` rejects elearn custom fields like `extendedProperties`, `actions`, `questionText`, `options`, `sceneDef`); every other typed method stays strongly-typed via the base `Component` class. Return type of `get` is `unknown` (not `any`) so callers still narrow at the use site. `useComponentProperty.ts` now narrows `component as ELearnComponent | null` **exactly once per hook** at function entry instead of at every method call — 6 scattered casts collapsed to 2 authoritative narrowings. `GRAPESJS_REACT_PATTERNS.md` code example updated with a leading comment explaining the rename. Scope deliberately narrow: property panels (Button/PhaserSim/Question/Animation/etc.) were NOT refactored because they compile clean against grapesjs's existing typings — their method usage stays within `ComponentProperties`. Test mocks retain `as unknown as Component` casts by design (mock-construction lives at a different type-safety boundary). Production code `grep "as GjsComponent"` and `grep "as unknown as Component"` both return 0 matches. `npx tsc --noEmit`: exit 0. 731/731 authoring-ui unit tests pass unmodified. Version bumped to v0.5.56.

- **TD-002 — Preview feature E2E test ✅ (TD-002 CLOSED)** — Added `e2e/tests/preview-handshake.spec.ts` with one `@integration` test exercising the full T641 preview postMessage handshake end-to-end: click Preview → popup opens at `/preview.html` → popup signals `'elearn-preview-ready'` to correct origin (not `'*'`) → opener receives it → opener posts `{ type:'elearn-preview-data', course, slideIndex }` back → popup's `ELearnPlayer.init('player', course, …)` renders content into `#player` (no `.preview-error` fallback). Critical Rule 5 asserted via `Object.keys(popup.localStorage).length === 0`. Popup spy implementation detail: `context.addInitScript()` wraps `window.opener` via `Object.defineProperty` to record every `postMessage()` call AND registers a `'message'` listener to record every inbound event — all before `preview.html`'s inline script runs (the only way to observe the outbound ready signal since `addInitScript` fires before document scripts). Opener spy installed via `page.evaluate` after the opener page has loaded. CI run 24586873603 completed with success in 17m03s — all 163 E2E tests green including the new one. TD-002 marked DONE in `tasks.md`. Commit: `583b229`.

- **TD-001 — Backend export routes: shared `runExport()` helper ✅ (TD-001 CLOSED)** — See `docs/issues/issues-TD-001.md`. Shared pipeline extracted to `backend/api/src/lib/export/runExport.ts` with a `PACKERS` registry; route layer in `courses.ts` reduced from 3× ~35-line handlers to a single `buildExportHandler(format)` factory + 3 one-line registrations. 17 new tests; backend 148/148 pass. **xAPI marginal cost: 70 LOC → 2 LOC (35×).** CI run 24584714505 success.

- **T651 — Unified persistence via `requestSave()` ✅ (T651 CLOSED — Phase 10 CLOSED)** — Eighth and final task of Phase 10. Five pre-T651 call sites duplicated the save recipe (`setIsSaving` + `setSaveError` + try/catch around `editor.store()`); three of them had silent-failure bugs from drift. T651 collapses them to one entry point via a two-layer design: **Layer 1** `performSave(editor, hooks)` pure primitive in `storageManager.ts` (no Zustand, no React — preserves T645 DI invariant); **Layer 2** Zustand-bound `requestSave(opts?)` closure in `initEditor.ts` wiring `performSave`'s `onStart`/`onSuccess`/`onError` hooks into `setIsSaving`/`setSaveError`; **Layer 3** `requestSave` field in `editorStore.ts` set via `setRequestSave(requestSave)` in `EditorCanvas` Effect 1. Bonus migrations (ADR-mandated to prevent immediate drift): `SaveErrorBanner.handleRetry`, `useActionsSave` subscribe, `SimulationEditor.handleSave` — all three **silent-failure bugs auto-fixed** (failures now surface in `SaveErrorBanner`; `SaveErrorBanner.handleRetry` regains the missing `setIsSaving(true)` via `onStart`; regression test `T651.3: sets isSaving(true) during retry` added). `grep "editor.store()"` is green everywhere except `storageManager.ts:68` (the single remaining invocation). Seven ADR guardrails honoured: DI pure, race guard stays in triggerAutosave, stopCommand flush stays in saveAndLoad, T650 unload untouched, timeout scoped to pre-nav only, null-safe at all callers, GrapesJS StorageManager contract unchanged. Non-`Error` fallback message harmonised to `'Save failed'` across callers (was 3 different strings). Course meta-ops (addSlide/deleteSlide/updateCourse) explicitly out of scope — candidate TD-007. 1533/1533 unit+integration tests pass. CI run `24582182042`: success (17m02s). Code review: APPROVED (0 findings above INFO). `docs/issues/issues-T651.md` generated. ADR: `decisions/2026-04-17-request-save.md`. Commits: `501c6aa` (feature + migration + tests), Phase 10 closure commit (refinements + docs + CHANGELOG + WORKING_CONTEXT).

- **T650 — beforeunload dirty-state warning ✅ (T650 CLOSED)** — Phase 10 seventh task. Closes the 2-second data-loss window during autosave debounce: if the user closes the tab or navigates away from the domain mid-debounce, the browser now shows its native "Leave site? Changes you made may not be saved" warning. T650.1: `initEditor()` return type extended from `{editor, cleanup}` to `{editor, cleanup, hasPendingChanges}`; `hasPendingChanges = () => autosaveTimer !== null` is a pure closure accessor — no new flag, no parallel state, zero drift risk. T650.2: `beforeunload` listener registered in `EditorCanvas.tsx` Effect 1 (same lifetime as the editor — NOT Effect 2, which would churn on every slide switch); handler does only `e.preventDefault()` + `e.returnValue = ''` when dirty, no async work. T650.3: 3 unit tests (null state → false; `component:update` → true; post-debounce → false AND `store()` called once) using `vi.useFakeTimers()` + `vi.advanceTimersByTimeAsync(2001)`. T650.4 absorbed into T650.3 — the original task text said "verify `store()` called inside beforeunload", which contradicts the T650 design (no forced save); real intent was "verify warning fires when dirty", which reduces to the timer-state tests. Deliberately rejected alternatives (documented in `issues-T650.md`): `await store()` (can't await in `beforeunload`), sync XHR (deprecated), `sendBeacon` (64 KB limit, slide JSON exceeds), Web Locks (doesn't cross unload), SW sync (excessive scope). Environment repair done in parallel during T650.5: `@rollup/rollup-win32-x64-msvc@4.60.1` and `es-abstract@1.24.1` were corrupted in the local pnpm store (missing subdirectories / `package.json`); fixed via `pnpm store prune` + targeted rm + `pnpm install` after killing a stale `esbuild.exe` holding a file lock — CI unaffected (runs in clean container). 1532/1532 unit+integration tests pass (authoring-ui 730, backend 131, runtime-player 256, scorm-packager 156, phaser-simulations 125, question-engine 74, simulation-engine 60). CI run `24576886118`: success (17m04s). Code review: APPROVED (0 findings above INFO). `docs/issues/issues-T650.md` generated. Commits: `04e6121` (feature + env repair), `7849ba6` (docs + task closure).

- **T649 — Stale-closure fix in array mutations: synchronous latestRef update ✅ (T649 CLOSED)** — Phase 10 sixth task. Root cause: `latestRef.current` was only assigned at React render time (`latestRef.current = value` in render body of hook). Two consecutive `update()` calls within the same render cycle both read the same stale ref — second call silently discarded the first update. Fix: added `latestRef.current = newValue` synchronously inside `update()` in both `useComponentProperty` and `useExtendedProperty` (before `comp.set(key, newValue)`). Panel fix: 9 stale-closure sites in `QuestionPropertiesPanel` patched — `updateOption`, `addOption`, `removeOption`, radio `onChange`, `addAnswer`, `removeAnswer`, `updateAnswer` all now call `const current = getLatest()` first. Preventive audit of ButtonPropertiesPanel and 4 other panels: no array mutation patterns found — no changes needed. T649.4 regression: two consecutive array-mutation calls in same `act()` block both apply. T649.5: `getLatest()` reflects external `comp.set()` immediately. 727/727 unit tests pass. Code review: APPROVED. `docs/issues/issues-T649.md` generated. Commit: `74befb8`.

- **T648 — Fix Zustand/Backbone duality in all PropertiesPanel components ✅ (T648 CLOSED)** — Phase 10 fifth task. All panels migrated to canonical `useComponentProperty` pattern. `useComponentProperty` made null-safe (accepts `Component | null`). Zustand `selectedComponentType` = render gate only; within-panel routing reads `selected.get('type')` from Backbone synchronously. Eliminated keystroke-level global re-renders. 724/724 tests pass.

- **T647 — Fix EditorCanvas pre-navigation store(): add UI state update ✅ (T647 CLOSED)** — Phase 10 fourth task. The `saveAndLoad()` pre-navigation save block in `EditorCanvas.tsx` silently caught errors via `console.error` but never updated Zustand, so `SaveErrorBanner` never appeared on slide-switch failures. Fix: added `setIsSaving(true)` + `setSaveError(null)` at block entry, `setSaveError(msg)` in catch with `err instanceof Error` narrowing (fallback: `'Pre-navigation save failed'`), and `finally { setIsSaving(false) }` — guarantees reset on success, error, and timeout. `useEditorStore.getState()` called synchronously at block entry (avoids stale closure). Verified no race with `triggerAutosave`: CRITICAL-01 guard in `initEditor.ts` returns early before touching `setIsSaving` when context has changed at debounce fire time. New file: `src/__tests__/EditorCanvas.test.tsx` — 4 regression tests using real Zustand store (not mocked) + mocked `initEditor`. All 4/4 pass. 716/716 unit tests green. Code review: APPROVED (0 issues). `docs/issues/issues-T647.md` generated.

- **T646 — Fix initEditor leaks: dragstart listener + autosaveTimer guard ✅ (T646 CLOSED)** — Phase 10 third task. Three accumulated resource leaks fixed in `initEditor.ts`. T646.3: `dragstart` listener extracted as named `dragstartHandler` const — registered once on `blockContainer`, removed by exact same reference in `cleanup()`. Prior to fix, each `initEditor()` call added a new handler without removing the previous; after 3–4 course changes, multiple handlers mutated the drag ghost simultaneously. T646.4: replaced `try/catch` DOM removal with `ghost.isConnected` guard; added `isUnmounted` flag (set to `true` in `cleanup()`) that blocks the `requestAnimationFrame` callback from touching `document.body` after `editor.destroy()`. T646.1/T646.2: `cleanup()` function composes all teardown — `isUnmounted = true` → `clearTimeout(autosaveTimer)` → `removeEventListener('dragstart', dragstartHandler)` → `unsubscribeCacheInvalidate()`. Called by `EditorCanvas` Effect 1 before `editor.destroy()`. T646.5: `_isEditorLoading` module-level flag kept; decision documented in `decisions/2026-04-17-editor-loading-flag.md` (GrapesJS fires `component:add` synchronously during `loadData()` — no React state update can propagate in time). T646.6: 4 new unit tests using `vi.useFakeTimers()` — clearTimeout guard, no-accumulation across 3 cycles, isUnmounted blocks removeChild in rAF; key afterEach pattern: `querySelectorSpy.mockRestore()` NOT `vi.restoreAllMocks()` (restoreAllMocks resets all vi.fn() module mocks). T646.8: task-ref comments removed from dragstart block; tsc clean. Code review: APPROVED (0 issues). `docs/issues/issues-T646.md` generated. 712/712 tests pass. Commits: `00dce16`, `e843512`.

- **T645 — Fix storageManager singletons: StorageContextProvider DI ✅ (T645 CLOSED)** — Phase 10 second task. Eliminated three module-level singletons (`storageContext`, `updateStorageContext`/`getStorageContext`, `invalidateCourseCache`) that bypassed React's lifecycle. Replacement: `StorageContextProvider` interface (T645.3.1) with `getContext()` (reads Zustand `getState()` synchronously at call time — T645.3.4) and `onCacheInvalidate(cb)` (Zustand plain `subscribe` on `cacheVersion` — T645.4). `storageManager.ts` has no Zustand import — DI only. New `registerStorageManager(editor, provider): () => void` replaces old init pattern. `bumpCacheVersion()` Zustand action replaces `invalidateCourseCache()`. `courseCache` lifecycle strictly private to module, reset only via provider callbacks (T645.3.5). Callers updated: `EditorCanvas.tsx`, `TopToolbar.tsx`, `SlideList.tsx` (T645.5). `initEditor` return type changed to `{ editor, cleanup }` — cleanup wraps `unsubscribeCacheInvalidate()` and is designed for T646 extensibility (T645.7). `storageManager.test.ts` rewritten with `makeProvider()` helper; 708/708 tests pass. Old API grep: 0 live calls remain. Code review: APPROVED (0 issues). `docs/issues/issues-T645.md` generated. Commits: `505de96`, `6752076`.

- **T644 — Fix PhaserSimPropertiesPanel: align with panel pattern ✅ (T644 CLOSED)** — Phase 10 first task. `PhaserSimPropertiesPanel` was the only panel that bypassed the established `useComponentProperty` pattern. T644.1: replaced direct `getExtendedProps(selected)` read with `useComponentProperty<PhaserSimExtendedProps>` subscription — undo/redo now re-renders the panel correctly. T644.2: removed `editor.store()` call from `update()` — saves route through `comp.set()` → `component:update` → debounced autosave in `initEditor.ts`. T644.3: replaced `onBlur` sync with `useEffect([ep.sceneDef])` so the textarea reflects any external Backbone mutation (undo/redo, remote). T644.7 refinements: `PhaserSimSceneDefEditor` converted to pure controlled component (no internal state); `GjsComponent` type exported from `useComponentProperty.ts`; optimistic `setValue()` added to `useExtendedProperty.update()` (parity with `useComponentProperty`); `Handler` → `ChangeHandler` rename + `ComponentMock` interface in hook test. 16 panel regression tests + all 712 suite tests green. Code review: APPROVED (0 issues). `docs/issues/issues-T644.md` generated. Commits: `9fcaf6d`, `df52fdf`, `b5e2a20`.

- **T643 — Fix forEach crashes: GrapesJS loadData + validateSequence ✅ (T643 CLOSED)** — Two independent bugs sharing the same `TypeError: Cannot read properties of undefined (reading 'forEach')` symptom. T643.1: `phaser-sim` and `screenshot-sim` were absent from `GENERATED_CONTENT_TYPES` in `converters.ts`; on reload `grapesjsFromWidgets()` set `def.content` to the saved `PLACEHOLDER_HTML` string, GrapesJS parsed it into auto-generated child defs without `actions: []` → crash. Also added guard: `text`/`button` widgets skip setting `def.content` when the stored value is HTML markup (detected via leading `<`). T643.2: three `.forEach()` calls in `validateSequence.ts` (lines 90, 102, 129) had no optional-chaining guard; old MongoDB documents saved before field types were required could have `condition.then`, `loop.body`, or `sequence.actions` absent at runtime → crash when user selected a widget (ActionsPanel render → validateAllSequences). Fixed with `?.forEach()`. 7 converter regression tests + 3 validateSequence regression tests added. 696 unit tests green. CI ✅ (run 24311195340).

- **T641.1 — Preview popup + T611.10 pass ✅ (T641 CLOSED)** — Root bug: `EditorPage.closeCourseSettings()` matched the Cancel button via regex `/close|cancel/i`, so the `navigationMode` selected in the Course Settings dialog was never saved to the backend. `course.settings.navigationMode` in the Zustand store remained `'free'`, causing `slideIsComplete()` in the runtime player to return `true` immediately (no gating), and the Next button stayed enabled. Fix: added `saveCourseSettings()` to `EditorPage.ts` that explicitly clicks `data-testid="course-settings-save"` and waits for the dialog to close. Updated T611.10 in `question-widget.spec.ts`: (1) calls `saveCourseSettings()` instead of `closeCourseSettings()` after setting linear-strict mode, (2) waits for the PATCH response via `waitForResponse()`, (3) removed all DIAGNOSTIC console.log / evaluate blocks from Step 4. Prior-session fix also contributed: `renderMCQuestion` / `evalMC` in `runtime-player/src/index.ts` now handle `MCOption[]` objects (`{ id, text, isCorrect }`) in addition to plain strings. **T611.10 passes (30.4s). 30/30 question-widget E2E tests green (was 29/30 + 1 skipped). SKIP-01 resolved.**

- **T640.11 — Code-review issue resolution ✅ (T640 CLOSED)** — All 7 issues from T640.7 reviewer report resolved: (H-01) Added inline comment to `storageManager.ts` cache-update block documenting JS single-threaded guarantee and Worker Threads caveat; (H-02) Added `Array.isArray(courseCache.doc.slides)` guard before `.map()` — sets `courseCache = null` on corrupt data instead of throwing TypeError; (M-01) Expanded `autoload: false` comment in `initEditor.ts` with full 6-step race sequence (init fires load before context set → blank canvas → EditorCanvas load → correct widgets → race → blank overwrites); (M-02) Added clarifying comment that `courseCache.doc` is non-null by outer if-guard and TypeScript type declaration; (M-03) Moved `invalidateCourseCache()` to outer `beforeEach` fixture in `storageManager.test.ts` for consistent test isolation; (L-01) Applied in previous session — "failed PATCH request (network error or 4xx/5xx)"; (L-02) `autosave:false` comment now says "every command including every keystroke in text widgets and every component add/remove." Verdict updated to APPROVED. **686 unit tests green.**

- **T640.6 — Persistence flow doc corrections ✅** — Manual read of `storageManager.ts`, `initEditor.ts`, and `docs/developer-guide/08-persistence-flow.md` identified 5 inaccuracies vs. actual code. All corrected: (1) Step 4 snippet now shows `editor.getComponents().toArray()` — the `store()` callback is a closure capturing `editor`, not reading `gjsData.components`; (2) New Step 5 documents thumbnail generation (`generateThumbnail(editor)`) with isolated try-catch, and PATCH payload corrected to `{ widgets, thumbnail }`; (3) Load pseudo-code now includes the required `{ pages: [{ id, component: { actions: [], components } }], styles: [] }` GrapesJS wrapper; (4) Key Files table: `widgetConverters.ts` → `converters.ts` (actual import); (5) Source-of-truth rule nuanced — `SaveErrorBanner` retry is an intentional exception to "never call `editor.store()` from React". Steps renumbered (old 5→6→7→8 to accommodate new thumbnail step). Sequence diagram updated. `storageManager.ts` and `initEditor.ts` needed no changes.

- **T640.4 — Persistence flow documentation ✅** — Created `docs/developer-guide/08-persistence-flow.md` covering the full edit→save→cache→load pipeline. Includes: source-of-truth boundary table, detailed step-by-step walk-through, `autoload:false`/`autosave:false` rationale, cache lifecycle table, ASCII sequence diagram, failure modes table, and a key files reference. Linked from `docs/developer-guide/index.md`.

- **T640.1–T640.3 — StorageManager: cache update on successful store() ✅** — `store()` success path no longer invalidates `courseCache = null`. Instead it updates the cached slide's widget list in-place via immutable spread. `courseCache = null` kept only in the `catch` block (failure path). Result: `load()` after a successful save hits the in-memory cache; no redundant `GET /courses/:id` round-trip. Two new unit tests: (1) `getCourse` called only once across load → store → load sequence; (2) `grapesjsFromWidgets` is called with the fresh saved widgets (not stale pre-store data) — regression guard against BUG-T640. 20/20 storageManager unit tests green.

- **T639 — Stale-closure fix: `useComponentProperty` / `getLatest()` (T639.1–T639.11) ✅ CLOSED** — Root cause: `useExtendedProperties.update(patch)` spread over `ep` from the React closure (value at last render). Rapid consecutive updates caused silent data loss. Fix: `useComponentProperty` exposes third return element `getLatest()` (reads `latestRef.current`, always fresh). T639.2: `QuestionPropertiesPanel` update() uses `getLatest()`. T639.4: `AnimationPropertiesPanel` save() uses `getLatestEp()`. T639.5: rule in CLAUDE.md + SKILL.md. T639.6: developer guide updated. T639.7: 6 unit tests added (680 green). T639.8: GrapesJS destroy/load race fixed in `initEditor.ts` (`em.loadData` monkey-patch checks `em.destroyed`); E2E regression test passes 11.1s. T639.9: CI green. T639.10: scoring sub-patch callbacks in all three question forms use `getLatest().scoring`. T639.11 (code review): 13 issues resolved — `UsePropertyReturn<T>` shared labeled tuple type (HIGH-01); `useExtendedProperty` now also returns `getLatest()` (HIGH-02); `AnimationExtendedProps` typed interface replaces `Record<string, unknown>` (MEDIUM-01); block scoping around `em.loadData` removed (MEDIUM-02); 4 new `useExtendedProperty — getLatest()` unit tests (MEDIUM-03); `ep.scoring` defaults comment (MEDIUM-04); plus LOW-01–LOW-06 (comments, `try/catch` on `removeChild`, ESLint annotations). **684 unit tests + 162 E2E tests green. APPROVED.**

- **T638 — Fix typography changes not affecting Score Widgets / v0.5.43** — Style Manager `font-size`/`color` changes had no visual effect on `quiz-score` and `score-field` canvas previews because `onRender()` injected hardcoded inline styles with higher CSS specificity than the GrapesJS CSS rule. Initial fix (d22fb16): `change:style` Backbone listener + `model.getStyle()` re-render — worked in dev but failed in CI production build (minified Backbone event not firing). Final fix (7bae6ec): removed inline `font-size`/`color` from `onRender()` entirely; GrapesJS applies `setStyle()` to `el` via CSS rule, inner elements inherit automatically. Also added `quizTitle`/`scorePrefix` editable traits. 5 `@regression` E2E tests in `score-widgets.spec.ts` (T638.5a–5e) — all pass in CI. 673 unit tests pass. Code review: APPROVED. `docs/issues/issues-T638.md` generated.

- **T637 — Text Widget Editing: RTE Cursor Loss / v0.5.43** — 5 cursor-loss root causes identified and fixed. (1) `Commands.isActive('text-edit')` always false → replaced with `isRteActive` closure flag (T637.2); (2) `elearn:paste` fires via keymap contenteditable gap → `if (isRteActive) return` guard (T637.1); (3) `elearn:copy` overwrites native clipboard → same guard (T637.1); (4) `component:update` on every keystroke → covered by isRteActive autosave guard; (5) `fromMove:true` suppresses `rte:disable` → documented, not fixed (low impact edge case). T637.3+T637.4: explicit `richTextEditor: { actions: ['bold','italic','underline','strikethrough','link'] }` in `grapesjs.init()`. T637.5: 4 regression E2E tests in `text-widget-rte.spec.ts` (cursor, paste suppression, toolbar, autosave) — all pass (20.7s). T637.6: full suite 154 pass, 3 skipped (FLAKE-02 pre-existing). T637.7: removed T637.1 diagnostic console.debug block + deleted `t637-diagnostic.spec.ts`. T637.8: code review APPROVED (0 issues), `docs/issues/issues-T637.md` updated. 673 unit tests pass. Commits: see git log.

- **T636 — Cross-slide copy/paste / v0.5.42** — Module-level clipboard in `clipboard.ts` (`let _clipboard`) survives GrapesJS `editor.load()` calls during slide navigation (editor is NOT recreated). `elearn:copy` command captures selected component's `style + definition`; `elearn:paste` command adds the definition to the new slide's canvas and restores `left/top/width/height` via `addStyle()`. Keymaps: `ctrl+c` / `ctrl+v`. 3 `@regression` E2E tests in `copy-paste-widget.spec.ts` use `runCommand` directly (bypasses DOM focus issues after `slidesTab.click()`). Unit tests: 672 pass. Full E2E suite: 153 pass (150+3 skipped). Code review: APPROVE (0 issues). Commits: `209805c` (implementation) + `cba6d28` (wrap-up).

- **T611/T612/T634 issues closure / v0.5.41** — Final two code fixes applied to `runtime-player/src/index.ts`: T611 M-01 garbled JSDoc in `slideIsComplete()` cleaned up + inline comment at Map-absence check site (`// Missing entry means unanswered`); T611 M-02 inconsistent type cast in `handleSubmit()` replaced with `ep?.scoring as QuestionScoringInfo | undefined`. All items in `issues-T611.md` (7 items), `issues-T612.md` (6 items), and `issues-T634.md` (M-01, L-01, L-02) marked RESOLVED. 256 runtime-player tests pass. Commit: `7313cc7`.

- **T635 — SCORM format selector / v0.5.40 / 6b6a9da** — `ExportFormat = 'scorm12' | 'scorm2004' | 'aicc'` exported from `PublishDialog.tsx`. Radio group with per-format descriptions added to publish dialog (SCORM 1.2 selected by default; confirm button label updates dynamically). `onConfirm` signature updated to `(format: ExportFormat) => void`. `triggerZipDownload` helper refactors shared blob download logic; `exportSCORM2004` + `exportAICC` added to `courseApi.ts`. Backend: `POST /courses/:id/export/scorm2004` + `/aicc` routes added (same rate-limit/asset-rewriting pattern as scorm12). 5 `@regression T635` E2E tests. All 1266 unit tests + 15 SCORM E2E tests pass. Code review: APPROVE (0 issues). CI run: ✅ green.

- **T631.3 waitForCanvas hang fix / 0db0248** — `waitForCanvas()` Phase 1 used `page.waitForFunction()` with a 500ms timeout to detect the `data-editor-ready="false"` transition. When the browser JS thread was busy post-load (GrapesJS component reconstruction), Playwright's CDP queue backed up — the 500ms timeout never fired, causing ~59s hangs in all 14 `beforeEach` hooks of `question-widget.spec.ts`. Fix: replaced `waitForFunction()` with `waitForTimeout(300)` — a pure Node.js timer. Phase 2 still gates on `data-editor-ready="true"`. All diagnostic TEMP DIAG T631.3 code removed from EditorPage.ts, question-widget.spec.ts, EditorCanvas.tsx. 142/145 E2E pass (3 pre-existing skips). T635 now unblocked.

- **T634 — Fix nav-buttons "missing child buttons" error / v0.5.39** — Root cause: `onRender()` HTML injection meant `component.components().at(0/1)` returned `undefined` in `NavButtonsPropertiesForm`, showing the "corrupted" error. Fix: replaced `onRender()` with `defaults.components` — two proper GrapesJS child button objects with `actions: []` (prevents loadData forEach crash). `widgetsFromGrapesjs` saves child labels as `prevLabel`/`nextLabel`; `grapesjsFromWidgets` restores them as child `content` on load with fallback defaults for backward compat. 6 unit tests + 3 E2E tests. Code review: APPROVE (0 CRITICAL/HIGH, 1 MEDIUM: loose type on `GrapesJsComponentDef.components`). Commit: `c7d123f`.

- **T633 — Fix button background image / v0.5.38** — `openBackgroundImagePicker` in `ButtonPropertiesPanel.tsx` switched from `component.setStyle()` (replace) to `component.addStyle()` (merge) so `left/top/width/height` are preserved on background assignment. Added `background-size: cover`, `background-repeat: no-repeat`, `background-position: center`. "Remove Image" clears all 4 background properties. `@regression T633.4` E2E test in `button-widget.spec.ts`. Commits: `d4a6055`.

- **T632 — Fix asset picker type for media/audio widgets / v0.5.37** — `detectAssetType(filename)` helper added to `assetManager.ts`: maps file extension to `'video'`, `'audio'`, or `'image'` — all uploads now tagged with correct GrapesJS type. `MediaPlayerPropertiesPanel` reads `mediaType` from component model and opens AM with `['audio','image']` or `['video','image']`. `AudioNarrationPropertiesPanel` changed from `['image']` to `['audio','image']`. Commit: `ece0142`. T632.4 regression test added to `media-player-widget.spec.ts`: smoke-level test verifies "Choose from Asset Library…" button opens GrapesJS AM modal; all 7 media-player tests pass. T632.5: CI run 24007832902 ✅ PASSED. T632 fully closed.

- **T631 — MC correct-answer regression test / v0.5.36** — Confirmed T621 fix (reads `comp.get('extendedProperties')` in `useExtendedProperties.update`) already covers MC/TF/Fill stale-closure issue. Added `@regression T631.6` E2E test in `e2e/tests/question-widget.spec.ts`: marks Option B correct, waits for autosave PATCH, reloads page, navigates back, asserts Option B is still the correct answer via `window.__elearn_editor`. Commit: `7d08f9c`.

- **Phase 2.8 Authoring UI Hardening / v0.5.35** — Four CRÍTICO fixes from Gemini code audit. T620: `useComponentProperty.ts` applies optimistic `setValue(newValue)` before `comp.set()` so controlled inputs never freeze; `latestRef = useRef(value)` tracks latest value to prevent stale closures. T621: `useExtendedProperties` wrapper reads `comp.get('extendedProperties')` directly (always current via Backbone.set) rather than stale closure `ep` — eliminates cascade-edit data loss. T622: `SaveErrorBanner.tsx` persistent red banner below TopToolbar; reads `saveError` from Zustand; Retry calls `editor.store()`, clears optimistically on success, updates message on failure; non-dismissible by design; SlideList navigation blocked when `saveError !== null`; TopToolbar "Save failed" badge added. T623: `extendFnView: ['initialize']` added to image widget view — removes `Object.getPrototypeOf(Object.getPrototypeOf(this))` prototype chain hack. 649 unit tests pass. Commit: `64a2ed9`.

- **C-03 SCORM export asset bundling / v0.5.34** — Verified the full 4-step asset pipeline in `backend/api/src/routes/courses.ts` was already implemented: `collectAssetSrcs()` extracts `/assets/<uuid>.ext` srcs from all slide widgets, `downloadAssets()` streams each object from Garage S3 to a tmpdir, `rewriteAssetSrcs()` deep-clones the course replacing `/assets/` with `assets/` (relative), `packSCORM12(rewrittenCourse, tmpDir, { assetPaths })` bundles assets into the ZIP. Added E2E gate tests to `packages/scorm-packager/src/__tests__/index.test.ts`: C-03a verifies asset file appears in ZIP at `assets/<uuid>.png`; C-03b verifies `index.html` contains relative `assets/<uuid>.png` and NOT `/assets/<uuid>.png`. 46 scorm-packager tests pass. Commit: included in v0.5.34.

- **C-02 sharedSequences end-to-end / v0.5.33** — Three-layer fix: (1) `packages/shared-types/src/course.ts` — `sharedSequences?: SharedActionSequence[]` added to `CourseDoc` (part of D-01 v0.5.31); (2) `packages/runtime-player/src/index.ts` — `course.sharedSequences ?? []` passed into actions context; (3) `backend/api/src/models/Course.ts` — `SharedActionSequenceSchema` + `sharedSequences` field added to Mongoose schema, PUT allowlist updated. Regression test in `courses.test.ts`. `call-sequence` action verified end-to-end via C-01.3 E2E test. Commit: `5c59d6d`.

- **C-01 actions engine E2E gate / v0.5.32** — Wired actions engine to runtime player with full E2E verification. Root bug: `renderWidget()` in `packages/runtime-player/src/index.ts` was returning `''` for `visible: false` widgets, making `show` actions impossible (no DOM element to operate on). Fix: render invisible widgets with `style="display:none"` + `data-hidden="true"` so `executeShow()` can find and reveal them. `slideRenderer.test.ts` updated to assert new behavior. New E2E spec `e2e/tests/runtime-player-actions.spec.ts` with 3 tests (C-01.1 hide, C-01.2 show, C-01.3 call-sequence) — all green at 259–481ms. Commit: `df7af97`.

- **D-01 shared-types refactor / v0.5.31** — Introduced `packages/shared-types/` as single source of truth for all widget/course/action/question types across the monorepo. Migrated `WidgetType`, `BaseWidget`, `Bounds`, `CourseDoc`, `Slide`, `SlideTemplate`, `Resource`, `CourseSettings`, `SCORMMetadata`, `NavigationMode`, `ActionSequence`, `SharedActionSequence`, and all question types out of `authoring-ui/src/types/` into `@elearn-studio/shared-types`. Added dual CJS + ESM build (`dist/index.js` + `dist/esm/index.js`) so Vite/Rollup consumers (authoring-ui) can statically analyse named exports and Node.js consumers (backend, scorm-packager) get CJS. Made `CourseDoc.templates?`, `resources?`, `sharedSequences?`, `deletedAt?`, `createdAt?`, `updatedAt?` optional and `passingScore?`/`masteryScore?` optional to match real fixtures and allow intentional fallback tests. Fixed `courseHasPhaserSim.test.ts` `makeWidget(type: WidgetType)` narrowing issue. 24 files changed, 790 insertions, 850 deletions. All 1444 tests across 8 packages pass. Commit: `f73576e`.

- **GrapesJS-React refactor / v0.5.30** — Fully eliminated the `isLocalRef` / `useRef(false)` anti-pattern from all 6 property panels. `AnimationPropertiesPanel` split into outer (null checks) + inner (`AnimationPanelContent`, receives guaranteed non-null `Component`) to fix null-safety crash from `component as never` cast. `QuestionPropertiesPanel` local `useExtendedProperties<T>` hook (49 lines with `isLocalRef`) replaced with a 7-line thin wrapper delegating to `useComponentProperty`. All panels now use shared `useComponentProperty<T>` / `useExtendedProperty<T>` from `hooks/useComponentProperty.ts`. 644 unit tests pass. Pending: commit.
- **Audit consolidado closure / v0.5.29** — Closed all remaining open ALTO and DEUDA TÉCNICA issues. A-06: replaced misleading "atomic single write" comment with accurate race-condition note in `PATCH /slides/reorder`. D-03: deleted 9 debug/test files from repo root (PHP, JS — untracked, already in .gitignore). D-05: deleted 4 `rollup.config-*.mjs` temp files from `packages/runtime-player/` (untracked, already in .gitignore). Confirmed A-01, A-05, A-07 already fixed in T600-T608. Confirmed D-04 untracked and in .gitignore. 131/131 backend API tests pass.
- **Phase 2.7 closure / v0.5.28** — Resolved all open MEDIUM and LOW issues from T611 and T612, and completed T270.DOCS. Code fixes: `QuestionScoringInfo` interface replaces ad-hoc casts; `mandatory: false` added to all three question default scoring configs; `goNext()` clarifying comment; `slideIsComplete()` JSDoc expanded; `finishCourse()` wraps navigation in try-catch; `restoreSuspendData()` warns when out-of-bounds visited indices are dropped; module-level `_noNavNextWarned` flag prevents repeated console.warn per session. Docs updated: `09-publishing.md` (Navigation Mode section), `scorm2004.md` (Sequencing per mode section), `05-questions.md` (Mandatory questions section). T613.6 marked deferred (Moodle E2E, opt-in via `E2E_MOODLE=1`). All 256 runtime-player + 628 authoring-ui tests pass.
- **T613 / v0.5.27** — SCORM 2004 conditional sequencing based on `navigationMode`. `buildManifest2004()` in `scorm-packager/src/index.ts` now branches on `course.settings?.navigationMode`: `'free'`/undefined → `choice="true" flow="true"` (unchanged); `'linear-strict'` → `choice="false" choiceExit="false" flow="true"` (LMS TOC navigation blocked). Single-SCO architecture: SCORM preConditionRule/objectiveProgressStatus sequencing rules are not applicable. 3 new unit tests; 27 scorm2004 tests pass. Reviewer: 0 issues. T613.6 (Moodle integration) deferred/opt-in. Commit: `5c9b8d8`.
- **TA608.6 fix / v0.5.26** — Fixed pre-existing GrapesJS Style Manager forEach crash for GENERATED_CONTENT_TYPES (progress-bar, audio-narration, volume-control). `model.defaults.properties` changed from `{}` to `[]` in `registerBlocks.ts`. GrapesJS's PropertyComposite calls `.forEach()` on `this.get('properties')`; `{}` is truthy so `|| []` doesn't activate, but `{}.forEach` is undefined → crash. Empty array `[]` fixes it. Also: T612.9 E2E uses `waitForReady()` (not `waitForReloadComplete()`) since no canvas exists without a slide; cleanup unchecks `requireAllSlides` BEFORE switching mode to 'free' since the checkbox is conditionally rendered only under `linear-strict`. T611.10 skipped (Preview button not yet implemented — shows "coming soon" toast). Commit: `cd82dbe`.
- **T612 / v0.5.25** — Phase 2.7 SCORM Navigation: `finishCourse()` now checks `requireAllSlides` and navigates to the first unvisited slide instead of completing (T612.6). Legacy `lesson_location` fallback seeds `visitedSlides` with `[0..restoredSlide]` (T612.7). T612.8: 2 unit tests for requireAllSlides gate. T612.9: E2E regression test for courseSettings persistence across reload. H-01 (optional chaining on `extendedProperties?.scoring`) and H-02 (`console.warn` for missing nav-next buttons) fixed. 256 unit tests + 128 E2E tests. Commit: `a65b01e`.
- **T610 + T611 / v0.5.24** — Phase 2.7 SCORM Navigation: T610 added `navigationMode`/`requireAllSlides` fields across all 4 packages and the Course Settings UI. T611 implemented mandatory question gating: `mandatory?: boolean` in `QuestionScoring`, mandatory checkbox in `QuestionPropertiesPanel`, `slideIsComplete()` + `updateNavButtons()` in runtime player, `data-nav-next` attribute on Next button. E2E regression test T611.10 added. Commit: `f85b27a`.
- **TA608 refinements / v0.5.22** — CRITICAL fix: `visitedSlides` was reset to empty `Set` on every player resume, causing progress bar to show 0% after returning to a course. Fixed by extending `suspend_data` schema from v:1 to v:2 to persist `visitedSlides` as a number array. `SuspendableState` interface extended with `visitedSlides: Set<number>`. `restoreSuspendData()` reconstructs the set with bounds checking; v:1 payloads seeded with `[currentSlide]` for backward compat. Also fixed: `updateProgressBars` percent scoping (now uses `.closest('.el-progress-bar')`); height input clamping in ProgressBarPropertiesPanel. Added TA608.6 persistence E2E test. All suspend unit tests updated + 2 new tests. `docs/issues/issues-TA608.md` generated by reviewer.
- **T601.8 fix / v0.5.21** — Fixed CI failure: `tests/question-widget.spec.ts:339:7 — T601.8 MC user-edited question text survives page reload`. Root cause: GrapesJS Backbone `component.set('extendedProperties', next)` (called by `useExtendedProperties` hook) places extendedProperties in the Backbone attributes hash; `c.getAttributes()` returns it; it was not excluded by `INTERNAL_GJS_ATTRS`; it leaked into `widget.properties`; on reload `grapesjsFromWidgets` placed it in the GrapesJS component def `attributes` sub-object; `loadData()` crashed with `TypeError: Cannot read properties of undefined (reading 'forEach')`. Fix: extended both `INTERNAL_GJS_ATTRS` and the `grapesjsFromWidgets` skip list to exclude `extendedProperties`, `elearnActions`, `actions`, `properties`.
- **T609 / v0.5.20** — Implemented MISSING-02: Global Volume Control widget. New `volume-control` GrapesJS block (Media category). `VolumeControlPropertiesPanel` with volume range/number input and showMute checkbox. Runtime player: module-level `_globalVolume`/`_globalMuted`, `applyVolumeToSlide()`, mute button SVG icon swap. 5 new E2E tests.
- **T608 / v0.5.19** — Implemented MISSING-03: Course Progress Bar widget. New `progress-bar` GrapesJS block (Navigation category). `ProgressBarPropertiesPanel` with color picker, height input, showPercent checkbox. Runtime player: `visitedSlides: Set<number>` in PlayerState, `updateProgressBars()` updates fill width and percent text on every slide nav. 5 new E2E tests.
- **T607 / v0.5.18** — Implemented MISSING-01: Audio narration widget. New `audio-narration` GrapesJS block + component with speaker-wave canvas preview. `AudioNarrationPropertiesPanel` with Audio Source URL (+ AM picker) and Playback Options (controls/autoplay checkboxes). Runtime player renders `<audio>` element. Fixed converter bug (WIDGETS_WITH_SRC_TRAIT whitelist) that caused `media-player` and `audio-narration` to lose `src` on reload. Fixed backend Widget.ts missing `audio-narration` from WIDGET_TYPES enum (caused 500 on PATCH). Added AM audio extension validation. 6 new E2E tests; 115 tests pass.
- **T606 / v0.5.17** — Fixed BETA-14: SCORM export loading feedback. `PublishDialog` now shows a status section (spinner → "Generating SCORM package…" → "Download ready" / error message inline). Error state shows in dialog with `role="alert"`. Cancel relabels to "Close" after export. 3 new E2E tests; 10 SCORM tests pass.
- **T605 / v0.5.16** — Fixed BETA-15: image widget placeholder via SVG data URI on `img.gjs-plh-image` (camera icon + "Click to choose image" text). Changed `click` → `dblclick` for Asset Manager open; tooltip `title="Double-click to open image selector"` set in `onRender()`. 4 new E2E tests; full 106-test suite passes.
- **T604 / v0.5.15** — Fixed BETA-10: new `MediaPlayerPropertiesPanel` with Media Source URL (+ AM picker), Media Type selector, and Playback Options (autoplay/controls/loop checkboxes). `useTrait` and `useExtendedBool` hooks with `isLocalRef` loop prevention. Props tab auto-opens on widget select. 6 new E2E tests; full 102-test suite passes.
- **T603 / v0.5.14** — Fixed BETA-04/05/11: new `ButtonPropertiesPanel` component for `button`, `done-button`, `nav-buttons`. Caption editable via `component.get/set('content')`; background image via Asset Manager + `component.setStyle()`. Nav buttons: separate prev/next caption fields writing to child components. Props tab auto-opens on widget select. 2 new E2E tests; all 15 grapesjs-integration tests pass.
- **T602 / v0.5.13** — Fixed BETA-01/02/03/08/09/13: all question property forms (MC, TF, Fill) now correctly persist text edits, correct-answer selections, and feedback fields. Root cause: forms read `extendedProperties` as a plain variable with no `useState` — React never re-rendered. Fix: `useExtendedProperties<T>` hook (useState + GrapesJS model subscription + isLocalRef loop prevention). All 23 question-widget E2E tests pass.
- **T601 / v0.5.12** — Fixed BETA-07 (AM thumbnail: generic icon → presigned URL) and BETA-12 (AM filename: UUID → original filename). `customFetch` in `assetManager.ts` now resolves presigned URL post-upload and passes `{ src, name: originalName, type: 'image' }` to GrapesJS. Added T601 E2E regression test; all 4 image-upload tests pass.
- **T600 / v0.5.11** — Fixed BETA-06: `done-button`, `question-tf`, `question-fill`, `media-player` now land at the correct position on drag (not at canvas origin 0,0). Added 4 E2E regression tests; all 13 grapesjs-integration tests pass.
- **Beta Review Round 1** — Full manual authoring test by project owner. 15 bugs found, 3 missing features identified. Full details: `docs/issues/issues-BETA-R1.md`
- **v0.5.10** — Defensive guards for missing `w.bounds` in `grapesjsFromWidgets`
- **v0.5.9** — E2E suite expanded 73 → 90 tests; Moodle SCORM integration tests
- **v0.5.8** — Four persistence race condition fixes (BUG-T800-01 through BUG-T800-04)
- **Audit consolidado** — 4 auditorías integradas en `docs/issues/audit-consolidado.md`;
  todos los issues ALTO y DEUDA TÉCNICA cerrados (v0.5.29).
  bugs sistémicos: C-01 ✅ v0.5.32, C-02 ✅ v0.5.33, C-03 ✅ v0.5.34. C-04 (question form React state — **already fixed** en T602), C-05/NAV-01 a NAV-04 (navegación — **already fixed** en T610-T613).
  D-01 shared-types refactor — **completed** en v0.5.31. **Audit consolidado 100% resuelto.**

Full history: `CHANGELOG.md`

---

## Known Issues Right Now

> Full details in `docs/issues/issues-BETA-R1.md`. Fix order: T600 → T601 → T602 → T603 → T604 → T605 → T606 → T607 → T608 → T609

### 🟡 E2E FLAKES (pre-existing, not caused by Phase 2.8)

| ID | Test | Symptom | Passes in isolation |
|---|---|---|---|
| FLAKE-01 | `action-sequence.spec.ts` GAP-02.3 | `[data-testid="action-item"]` not visible after reload in full suite | ✅ Yes — passes alone |
| FLAKE-02 | `question-widget.spec.ts` T601.6 | `slide-item.nth(97)` timeout in full suite — slide panel takes >30s to render at index 97 | ✅ Yes — passes alone |

Root cause FLAKE-01: parallel-suite cross-slide-index contamination — GrapesJS `loadData()` fails silently when another worker's PATCH races with the reload. The Navigate action is saved correctly; the failure is in the re-selection step under load. Not a product bug.

Root cause FLAKE-02: slide accumulation across the full E2E suite — `question-widget.spec.ts` adds one slide per test in `beforeEach`. By the time T601.6 runs (~test 98 in the suite), 97 slides exist and the slide list rendering exceeds the 30s `waitFor` timeout. Not a product bug.

| FLAKE-03 | `authoring-ui-layer.spec.ts` T608.6 | Slide delete count assertion non-deterministic in full suite (3 workers) | ✅ Yes — passes alone |

Root cause FLAKE-03: `global-setup.ts` creates a single shared seed course; all parallel workers navigate to the same course in `fixtures/auth.ts`. The T608.6 `beforeEach` reads slide count then asserts it decreased by 1, but concurrent workers mutate the shared course between read and assertion. Fix tracked in **T642** (per-test course isolation).

### ℹ️ INTENTIONAL ESLINT DISABLES (not bugs — do not flag)

| File | Line | Rule disabled | Reason |
|------|------|---------------|--------|
| `packages/authoring-ui/src/hooks/useComponentProperty.ts` | ~72 | `react-hooks/exhaustive-deps` | `defaultValue` omitted from `useComponentProperty` deps — stable for a panel's lifetime; including it causes redundant re-subscriptions. Justification comment present on the next line. |
| `packages/authoring-ui/src/hooks/useComponentProperty.ts` | ~136 | `react-hooks/exhaustive-deps` | `readValue`/`defaultValue` omitted from `useExtendedProperty` deps — same rationale. Justification comment present on the next line. |

CI reports these as lint annotations on every push. They are correct, intentional, and documented in the source. Do not raise them as issues. (T640 verified 2026-04-11)

### 🟡 SKIPPED TESTS (feature not yet implemented)

| ID | Test | Reason | Task |
|---|---|---|---|
| SKIP-01 | `question-widget.spec.ts` T611.10 | Preview button opens "coming soon" toast — runtime player popup not implemented | **T641** |

### 🔴 CRITICAL

| ID | Description | Task |
|---|---|---|
| ~~BETA-01~~ | ~~MC question: no way to mark correct answer~~ | ✅ Fixed in T602 |
| ~~BETA-02~~ | ~~All questions: question text + option text not editable~~ | ✅ Fixed in T602 |
| ~~BETA-03~~ | ~~All questions: feedback text not editable~~ | ✅ Fixed in T602 |

### 🟠 HIGH

| ID | Description | Task |
|---|---|---|
| ~~BETA-04~~ | ~~Button caption cannot be changed~~ | ✅ Fixed in T603 |
| ~~BETA-05~~ | ~~Button background image cannot be assigned~~ | ✅ Fixed in T603 |
| ~~BETA-06~~ | ~~Positioning bug on initial drag: done-button, question-tf, question-fill, media-player~~ | ✅ Fixed in T600 |
| ~~BETA-07~~ | ~~Asset Manager: generic icon instead of image thumbnail~~ | ✅ Fixed in T601 |
| ~~BETA-08~~ | ~~TF: correct answer selection broken~~ | ✅ Fixed in T602 |
| ~~BETA-09~~ | ~~Fill: accepted answer not editable~~ | ✅ Fixed in T602 |
| ~~BETA-10~~ | ~~Media Player: no properties panel, cannot assign media~~ | ✅ Fixed in T604 |
| ~~BETA-11~~ | ~~Nav buttons: individual captions not changeable~~ | ✅ Fixed in T603 |

### 🟡 MEDIUM

| ID | Description | Task |
|---|---|---|
| ~~BETA-12~~ | ~~Asset Manager: UUID shown instead of original filename~~ | ✅ Fixed in T601 |
| ~~BETA-13~~ | ~~MC props panel doesn't refresh when options added/removed~~ | ✅ Fixed in T602 |
| ~~BETA-14~~ | ~~No loading feedback during SCORM export~~ | ✅ Fixed in T606 |
| ~~BETA-15~~ | ~~Image widget: no placeholder hint~~ | ✅ Fixed in T605 |

### 🔵 MISSING FEATURES

| ID | Description | Task |
|---|---|---|
| ~~MISSING-01~~ | ~~Audio narration component~~ | ✅ Fixed in T607 |
| ~~MISSING-02~~ | ~~Global volume control~~ | ✅ Fixed in T609 |
| ~~MISSING-03~~ | ~~Course progress bar~~ | ✅ Fixed in T608 |

---

## Root Cause Summary for Phase 2.6

### BETA-06 (positioning on 4 widgets)
`done-button`, `question-tf`, `question-fill`, `media-player` block `content`
definitions are missing `style: { position: 'absolute', left, top, width, height }`.
Working widgets (rectangle, question-mc) have it. Fix in `registerBlocks.ts`.

### ~~BETA-01/02/03/08/09 (question props not persisting)~~ — ✅ Fixed in T602
Root cause was missing `useState` in all 3 forms. Forms read `extendedProperties` as a
plain variable — React never re-rendered, stale closure reverted every edit. Fixed with
`useExtendedProperties<T>` hook in `QuestionPropertiesPanel.tsx`.

### BETA-07/12 (Asset Manager preview)
`src` passed to GrapesJS AM after upload is raw Garage path, not presigned URL.
Original filename not stored — only UUID key returned from backend.

### BETA-04/05/11 (button caption + background)
Button components lack a `label` trait wired to content, and background image
assignment is not calling `component.setStyle()` correctly.

---

## What Was Attempted and Failed — DO NOT RETRY

| Approach | Why it failed | Alternative |
|---|---|---|
| `component:update` for immediate save | Infinite save loop in GrapesJS | 2s debounced autosave in `initEditor.ts` |
| `listenTo(model, 'change:style', onRender)` in score widgets (T638) | Fires in dev but not reliably in production minified build (Backbone event registration order / tree-shaking in Vite rollup) | Remove inline styles from `onRender()` and rely on CSS inheritance from `el` — GrapesJS applies `setStyle()` to `el` via CSS rule, children inherit automatically |
| TipTap inside GrapesJS canvas iframe | No React context inside iframe | Native GrapesJS `contenteditable` |
| `minio/minio` Docker image | Discontinued | `dxflrs/garage:v1.0.0` |
| `@opentelemetry/auto-instrumentations-node` full bundle | Unused instrumentations, slow startup | Selective packages only |
| JWT in localStorage | LMS iframe blocks it | Memory-only via Zustand |
| `pressSequentially` for Moodle login | Characters dropped under CPU load | `page.fill()` |
| GrapesJS Studio SDK | Paid product | Open-source `grapesjs` npm only |

FAILED (T630 — 5 fases de bug, no repetir ninguna):
- Fase 1: document.addEventListener('mousemove') en main window → (0,0) en slides 2+
  REASON: El iframe no propaga eventos al main document.
- Fase 2: getMouseRelativePos() con evento iframe → Y offset = +iframeRect.top
  REASON: La función añade frameOffset internamente; con evento iframe lo cuenta dos veces.
- Fase 3: getMouseRelativeCanvas() con evento iframe → X offset = +93.1875px constante
  REASON: Misma causa; frameOffset.left = 93.1875px = ancho del panel izquierdo.
- Fase 4: clientX - iframeRect.left → X offset = -93.1875px (espejo de Fase 3)
  REASON: clientX de iframeDoc ya es canvas-relativo; restar iframeRect.left es doble resta.
CORRECT (Fase 5): clientX/zoom, clientY/zoom. Sin ninguna operación de offset.
  Los eventos de iframeDoc llevan clientX/Y en coordenadas del canvas (iframe-relative).
  Solo dividir por getZoomDecimal() para normalizar zoom.
  Commits: a3417e2 (Phase 5), de16eff (UX ghost fix)


---

## Next Steps (Ordered)

### Phase 2.6 — COMPLETE ✅
1–10. All beta review fixes and missing features done (T600–T609).

### Phase 2.7 — SCORM Navigation Integration
- ~~**T610** — Add `navigationMode`/`requireAllSlides` to CourseSettings~~ ✅ Done
- ~~**T611** — Block Next button until required questions answered~~ ✅ Done
- ~~**T612** — Resume: track visitedSlides, gate `finishCourse()` on `requireAllSlides`~~ ✅ Done
- ~~**T613** — SCORM 2004 conditional sequencing XML based on `navigationMode`~~ ✅ Done

### Audit Consolidado — Bugs Sistémicos (next up)
- ~~**D-01** — `@elearn-studio/shared-types` as monorepo type authority~~ ✅ Done (v0.5.31)
- ~~**C-01** — Wire actions engine to runtime-player~~ ✅ Done (v0.5.32)
- ~~**C-02** — `sharedSequences` Mongoose model support~~ ✅ Done (v0.5.33)
- ~~**C-03** — SCORM export asset bundling~~ ✅ Done (v0.5.34)

### Phase 2.8 — Authoring UI Hardening — COMPLETO ✅
- ~~**T620** — Fix optimistic update in useComponentProperty~~ ✅ Done (64a2ed9 v0.5.35)
- ~~**T621** — Fix stale closure in useExtendedProperties~~ ✅ Done (64a2ed9 v0.5.35)
- ~~**T622** — Save error blocking banner (SaveErrorBanner)~~ ✅ Done (64a2ed9 v0.5.35)
- ~~**T623** — Replace prototype chain hack in image widget~~ ✅ Done (64a2ed9 v0.5.35)

### Audit consolidado — COMPLETO ✅
Todos los items críticos (C-01, C-02, C-03) y deuda técnica (D-01, D-02) cerrados.

### Phase 2.9 — SCORM & Publishing (ACTIVE)
- ~~**T632** — Fix asset picker type for media/audio widgets~~ ✅ Done (ece0142 v0.5.37) — T632.4 E2E added, T632.5 CI green
- ~~**T633** — Fix button background image scale and no-repeat~~ ✅ Done (d4a6055 v0.5.38)
- ~~**T634** — Fix nav-buttons "missing child buttons" error~~ ✅ Done (c7d123f v0.5.39)
- ~~**T635** — Add SCORM format selector to PublishDialog~~ ✅ Done (6b6a9da v0.5.40)
- ~~**T636** — Cross-slide copy/paste~~ ✅ Done (v0.5.42)
- ~~**T637** — Text widget editing: RTE cursor loss investigation + fixes~~ ✅ Done (v0.5.43)
- ~~**T638** — Fix typography changes not affecting Quiz Score and Score Field~~ ✅ Done (v0.5.43)
- ~~**T639** — Stale-closure fix: `getLatest()` in property panels~~ ✅ Done

### Phase 10 — React/GrapesJS Architectural Refactor ✅ CLOSED (v0.5.55)
- ~~**T644** — Fix PhaserSimPropertiesPanel: align with panel pattern~~ ✅ Done (v0.5.48)
- ~~**T645** — Fix storageManager singletons: StorageContextProvider DI~~ ✅ Done (v0.5.49)
- ~~**T646** — Fix initEditor leaks: dragstart listener + autosaveTimer~~ ✅ Done (v0.5.50)
- ~~**T647** — Fix EditorCanvas pre-navigation store(): add UI state update~~ ✅ Done (v0.5.51)
- ~~**T648** — Fix Zustand/Backbone duality in all PropertiesPanel components~~ ✅ Done (v0.5.52)
- ~~**T649** — Fix stale closure in QuestionPropertiesPanel updateOption~~ ✅ Done (v0.5.53)
- ~~**T650** — beforeunload flash save: prevent data loss on tab close~~ ✅ Done (v0.5.54)
- ~~**T651** — Unify persistence via requestSave(): single save entry point~~ ✅ Done (v0.5.55)
- ~~**T1000.TEST / T1000.E2E / T1000.DOCS** — Phase 10 closing tasks~~ ✅ Done

### ⚡ Active (highest priority): TD-014 audit-externo sweep (🟡 IN PROGRESS)

> Sub-block within TD-014 triggered by audit externo 2026-04-24 (11 findings F1–F11; F4 absorbed into TD-014.27.d, F3 owner-decided at audit time as Option B, remaining 9 → subtasks .29–.38). Execution order enforced by numbering — each step unblocks verification of the next.

#### 🗺️ Recommended execution order for TD-014 (DO NOT reorder without reason)

The sequence below is agreed with owner and codified here so it is not lost between sessions. Each entry lists the severity, the reason it must run where it runs, and the gating dependency (if any).

**Phase A — Audit externo sweep (.29–.38)**

| # | Sev. | Step | Why here | Depends on |
|---|---|---|---|---|
| 1 | HIGH | **.29 F7** — expose `__simStore` / `__recorderStore` on window | MUST come first. Every subsequent E2E that reads store state (.30, .31, .35) calls `window.__simStore?.getState()`. Without .29 those tests can't exist. | — |
| 2 | CRIT | **.30 F5** — drag-drop real E2E assertion | The prior assertion was a no-op; .30 is the foundation for the reorder + persistence checks in .31. | .29 |
| 3 | CRIT | **.31 F6** — post-reload content persistence (⚠️ partial) | Lands the 3-surface verification (DOM + StepForm + store). Deliberately ships partial: hotspot-persistence deferred to .35 to keep the spec focused on *content* drift — hotspot drift belongs with the draw-mode gesture. | .29, .30 |
| 4 | CRIT | **.32 F11** — `handleSave` hardening | Grouped with .30/.31 in the same editing pass over `SimulationEditor.tsx` — prevents two separate reviews of the same file. | — |
| 5 | HIGH | **.33 F1** — backend `interactionType` contract | Runs AFTER the frontend E2E work because the mock-lie cleanup (in the authoring-ui integration test) is a companion edit to the backend change; doing them together keeps the commit coherent and avoids a red-interval where the mock claims a field the backend doesn't emit. | — |
| 6 | HIGH | **.34 F2** — recorder stop semantics (3-action split, Option C) | The only finding that required an explicit owner decision (A/B/C); ran once the CRITICAL triage above was settled. ADR lives in `decisions/2026-04-24-recorder-stop-semantics.md`. | — |
| 7 | MED  | **.35 F10** — E2E draw-mode gesture + **inherited .31 hotspot-persistence** | Closes the .31 partial deferral. TD-014.31 is blocked-CLOSED on this — do not mark the F6 finding fully resolved without .35 landing. | .29, .30, .31 |
| 8 | MED  | **.36 F3** — `getLiveScreenshotUrl` docstring rewrite | Docstring-only, trivial. Runs after substantive changes so the wording can reference the shipped behaviour verbatim. | — |
| 9 | LOW  | **.37 F8** — URL length limit test | Cleanup tail. Guards `>` vs `>=` flip in `MAX_RECORDER_URL_LENGTH` check. | — |
| 10 | LOW  | **.38 F9** — click-outside + `isBusy` guards in `RecorderLauncherDialog.test.tsx` | Cleanup tail. Closes the "header promise vs delivered coverage" gap. | — |

**Phase B — Original TD-014 tail (.24–.28) — sequential by design**

| # | Step | Notes |
|---|---|---|
| 11 | **.24** refine | Extract shared dark-theme tokens to `simulationTheme.ts`; move `btnDanger` (shipped in .34) in at this point. Consistency pass + `any` / `as any` sweep. |
| 12 | **.25** docs | User-manual §13 (NEW: documents the 3-verb recorder UX from .34), developer-guide §11 (simulation-recorder architecture), cross-cutting user-guide sweep. Depends on .24 for final copy. |
| 13 | **.26** reviewer | Generate `docs/issues/issues-TD-014.md` with detected problems; resolve all CRITICAL/HIGH before closing. |
| 14 | **.27** verification + block closure | (a) `pnpm -r test` green, (b) `pnpm -r lint` 0 errors, (c) `npx tsc -b` exit 0, (d) **OpenAPI regen including F4** (swagger `AuthoredSimStep` + `SimHotspot` schemas), (e) full E2E incl. `simulation-recorder.spec.ts` (gated), (f) CHANGELOG v0.5.66, (g) WORKING_CONTEXT update, (h) commit + push + CI green. |
| 15 | **.28** unblock TD-013.5 | Confirm §13 captures are now feasible with zero store seeding via real-UI overlay; update TD-013.5 pointers; resume TD-013 parallel track. |

**Important invariants across the order:**

- **.31 cannot be marked fully complete until .35 ships** — the cross-reference is bidirectional: `tasks.md` TD-014.31 carries `⚠️ PARTIAL SCOPE — hotspot persistence deferred to TD-014.35`; TD-014.35 carries `⚠️ INHERITED SCOPE FROM TD-014.31 (MUST CLOSE HERE)`. Do not remove either marker prematurely.
- **.27.d extends scope via F4** — swagger must emit `AuthoredSimStep` + `SimHotspot` component schemas before the OpenAPI regen; otherwise the regen produces drift in `SimConfig.steps` typing. F4 is NOT a standalone subtask; it lives inside .27.d.
- **.34 is done — ADR delivered** — do not re-open the A/B/C decision. If a new requirement emerges, write a superseding ADR and reference the 2026-04-24 one.
- **TD-013 (parallel track) blocks on .28** — TD-013.5 waits for .28 confirmation. TD-013.5b/.6/.7 are independent and can run at any time.

**Done** (uncommitted, this session batch 2026-04-24 → 2026-04-25):
- **.29** F7 HIGH — `__simStore` + `__recorderStore` window exposure ✅
- **.30** F5 CRITICAL — drag-drop real E2E assertion ✅
- **.31** F6 CRITICAL — post-reload content persistence (⚠️ **partial scope** marker retained for historical traceability; hotspot persistence shipped in .35 below)
- **.32** F11 CRITICAL — `handleSave` hardening (async + toast paths + closePanel-on-success-only) ✅
- **.33** F1 HIGH — backend `interactionType` type + emission + contract test; frontend mock-lie cleanup ✅
- **.34** F2 HIGH — recorder stop semantics 3-way split (Stop & import / Stop / Discard) ✅ — ADR `decisions/2026-04-24-recorder-stop-semantics.md` delivered
- **.35** F10 MEDIUM — E2E draw-mode gesture + inherited hotspot-persistence from .31 ✅ (closes the F6 partial-scope deferral; pure E2E change, no production code edited)
- **.36** F3 MEDIUM — `getLiveScreenshotUrl` docstring rewrite ✅ (pure docstring fix; documents the two real reasons the rotating cache-buster is necessary — MITM proxies ignoring `no-store` + React `<img>` src-identity network skip; corrected example from `t=Date.now()` to `t=${tick}` to match actual call site)
- **.37** F8 LOW — URL length limit test ✅ (1 new test pinning the `>` vs `>=` boundary at `RecorderLauncherDialog.tsx:62`; matches the limit number in the error copy rather than the full message — stable across rewords)
- **.38** F9 LOW — click-outside + isBusy guard tests ✅ (3 new tests pinning the two inline JSX guards at `RecorderLauncherDialog.tsx:86` + `:94`; closes the "header promise vs delivered coverage" gap that has been open since TD-014.19)

**🟢 PHASE A COMPLETE (audit-externo sweep)** — all 10 findings closed: F1, F2, F3, F5, F6, F7, F8, F9, F10, F11. F4 closed via TD-014.27.d (OpenAPI regen + swagger schema additions).

**🟢 PHASE B COMPLETE — TD-014 BLOCK CLOSED** (all uncommitted in working tree; single-batch commit + push pending owner confirmation):
- **.24** refine ✅ — `simulationTheme.ts` extracted; 5 production files refactored; 3 project-wide conventions surfaced as ADRs (lint suppression policy → `AGENTS.md §11.8`; widget-vs-app-chrome label-change → own ADR; flat testID naming → row #13 of consistency ADR).
- **.25** docs ✅ — user-guide §13 updated to shipped UX (4-button toolbar, `+ Add step`, `Upload…` + `Asset Library`, drag + ↑/↓ + Alt+↑/↓), §12 one-liner on the three build paths, NEW `developer-guide/11-simulation-recorder-architecture.md` (~340 LOC), cross-cutting sweep documented in `docs/issues/issues-TD-014.md`. Owner-caught vocabulary leak in §18/§20 reverted; `feedback_user_manual_vocabulary.md` saved to memory.
- **.26** reviewer pass ✅ — `docs/issues/issues-TD-014.md` reviewer section appended; R-H1 (3 hardcoded `'#6c7086'` literals in `SimulationEditor.tsx`) and R-M1 (`RawSimStep` naming) fixed; R-M2 deferred to follow-up `TD-014-followup-1`.
- **.27** verification + block closure ✅ — (a) `pnpm -r test` 1039 + 149 green; (b) lint 0; (c) `tsc -b` 0; (d) F4 OpenAPI regen complete (`AuthoredSimStep` + `SimHotspot` schemas); (e) full E2E **167/167** (recorder spec ran end-to-end for the first time, surfaced 5 inherited bugs all fixed in-scope per owner directive — see What Was Last Done entry for details); (f) `CHANGELOG.md` v0.5.66 entry written; (g) this WORKING_CONTEXT update; (h) batch commit + push pending owner confirmation.
- **.28** unblock TD-013.5 — **NEXT** (only pending TD-014 item) — confirm §13 captures now feasible with zero store seeding via real-UI overlay; update TD-013.5 pointers; resume TD-013 parallel track.

### TD-013 — Screenshot campaign 100% automation ✅ BLOCK CLOSED 2026-07-18 (v0.5.67)
- **.1** §01 9-callout overlay ✅ (2026-04-24, WIP uncommitted)
- **.2** §02 dashboard New Course dialog ✅ (2026-04-24, WIP uncommitted)
- **.3** §09 NameField component + capture ✅ (2026-04-24, WIP uncommitted)
- **.4** §09 widget-dropdown-names via Zustand direct-sync + `ensureClickEvent` group-role fix ✅ (2026-04-24, WIP uncommitted)
- **.5** §13 Sim Editor overlay captures (13-overview, 13-hotspot-editor) ✅ (2026-04-29, commit `473067b` — note: the two PNGs themselves are still untracked in the worktree; stage them at block closure)
- **.5b** §08 scoring-section dual strategy (testid + Python fallback) ✅ (2026-07-17 — testid + captureElement scroll fix + `scripts/screenshots-crop.json`; tool itself is .6)
- **.5c** Recompose campaign captures ✅ (2026-07-18 — §17 builds the real "Capitals of Europe" worked example + finals at 1600×900; genuine `02-first-slide`; §11 params filled; §01 callouts below tabs; 5 product/manual findings + the select(null) context-destruction mystery filed in `issues-TD-013.md`)
- **.6** Python + Pillow crop tool ✅ (2026-07-18 — `crop-screenshots.py` + `run-crop.cjs` interpreter launcher; mtime-idempotent; chained as `docs:screenshots` post-step; all error paths exercised. Backlog TD-015…TD-020 filed from .5c findings per owner directive)
- **.7** Playbook update ✅ (2026-07-18 — T-13…T-18 added, T-6 superseded by Escape-deselect, T-12 snippet corrected, Deferred → Historical-deferrals table, run/output sections rewritten for the chained crop)
- **.8** Verification ✅ (2026-07-18 — 55/55 fresh + inventory cross-check; visual pass 11 by hand + 44 via vision agent; 2 pre-existing defects fixed: 13-block-placeholder visible-clip, 14-processflow-builder seeded scene; 20 stale `-fullpage` nets retired, only 08's by-design net remains)
- **.9** Code refinement pass ✅ (2026-07-18 — utils/worked-example.ts + utils/actions.ts extracted, CSS constants, spec 1763→1610 lines)
- **.10** Close block + retire legacy tooling ✅ (2026-07-18 — block CLOSED, v0.5.67)

### Previously closed — TECH DEBT backlog (TD-001…TD-012)
- **TD-001** through **TD-012** — ✅ All closed. See `CHANGELOG.md` entries v0.5.55 through v0.5.65.
- **TD-009** (2026-04-18, v0.5.63): three overlapping races on rapid slide switch — StrictMode concurrent loads, autosave timer mid-load, stale `data-editor-ready` attribute. All three fixed with minimal diffs; E2E `widget-persistence-across-slides` guards against regression.
- **TD-010** (2026-04-18, v0.5.63): centralised Props-tab empty state; 6 panels now `return null` + single `<PropsEmptyState>` router in `AppLayout`.
- **TD-012** (2026-04-23, v0.5.65): e2e/ typed `window.__elearn_editor` boundary — 109 TS errors → 0 via single typed augmentation + `EditorPage.page` made public. Paired with `docs/developer-guide/10-docs-screenshots-playbook.md` cataloguing 12 screenshot-spec techniques.

### Next: User Manual v2 rewrite
> **Scope doc:** `docs/user-manual-v2-scope.md`
> **Blockers:** none (TD-008 closed).
> **Writing order:** §2 (authoring walkthroughs, mechanical) → §3 (Actions Editor, now unblocked) + §8 (end-to-end worked example) → §4 (Phaser with real JSON) → §5 (publish) → §6 (QA) → §7 (troubleshooting) → §9 (glossary).
> **Target length:** 3–4× v1 (≈ 15–25 KB markdown).
> **Pending decision:** T036 Phaser per-simType scene builders (LOW priority; placeholder workaround acceptable until real scene rendering lands).
> **Open follow-ups from TD-012** (optional, non-blocking): (1) CI integration — add `pnpm --filter @elearn-studio/e2e exec tsc --noEmit` to the workflow so the TD-012 invariant is enforced; (2) `09-widget-dropdown-names` structural fix — Zustand store must be populated after session-level widget additions, not only at bootstrap. Tracked in `docs/issues/issues-TD-012.md` §Follow-ups.

---

## Visual Verification Status

| Component | Status | Notes |
|---|---|---|
| Text widget | ✅ Working | No issues |
| Image widget | ✅ Working | AM thumbnail and filename fixed (T601); placeholder hint + dblclick AM (T605) |
| Button | ✅ Working | Caption + background image editable (T603) |
| Done button | ✅ Working | Positioning (T600) + caption + background image (T603) |
| Nav buttons | ✅ Working | Individual prev/next captions editable (T603); child components via defaults.components — no "missing child" error (T634) |
| Multiple Choice | ✅ Working | Text, options, correct answer, feedback all editable (T602) |
| True/False | ✅ Working | Positioning (T600) + correct answer selection (T602) fixed |
| Fill in Blank | ✅ Working | Positioning (T600) + accepted answer editable (T602) |
| Media Player | ✅ Working | Positioning (T600) + Media Source/Type/Playback props panel (T604) |
| Audio Narration | ✅ Working | Block + canvas preview + Props panel (T607); runtime `<audio>` rendering |
| Progress Bar | ✅ Working | Block + Props panel (T608); runtime `visitedSlides` progress tracking |
| Volume Control | ✅ Working | Block + Props panel (T609); runtime global volume/mute with `applyVolumeToSlide()` |
| SaveErrorBanner | ✅ Working | Persistent red banner on autosave failure (T622); Retry button clears on success; nav blocked when `saveError !== null`; **T651.3 fix**: retry now fires `isSaving(true)` — "Saving…" badge visible during retry attempt |
| Unified save path | ✅ Working | All save callers (autosave, pre-nav, retry, actions-save, sim-save) route through `requestSave()` (T651); `grep "editor.store()"` green except `storageManager.ts:68` |
| `beforeunload` warning | ✅ Working | Native browser dialog fires when `hasPendingChanges()` is true (T650); no async work / no `sendBeacon` in the handler |
| useComponentProperty | ✅ Working | Optimistic `setValue` before `comp.set()`; no bounce-back on rapid typing (T620) |
| useExtendedProperties | ✅ Working | Reads `comp.get('extendedProperties')` directly; stale closure eliminated (T621) |
| Image widget initialize | ✅ Working | `extendFnView: ['initialize']` — prototype chain hack removed (T623) |
| Block drag-and-drop positioning | ✅ Working | Widget lands at cursor tip (canvas coords); 24×24px ghost indicator (T630 Phase 5 + UX) |
| Quiz Score widget | ✅ Working | Style Manager font-size/color apply immediately (CSS inheritance, T638); quizTitle trait editable |
| Score Field widget | ✅ Working | Style Manager font-size/color apply immediately (CSS inheritance, T638); scorePrefix trait editable |

---

## Architecture Reminders

- **GrapesJS canvas = iframe** — use `editorPage.canvasComponent()` / `canvasFrame()`, never `page.locator()` on canvas elements directly
- **GrapesJS iframe coordinates** — `clientX/Y` from `iframeDoc` events are ALREADY canvas-relative. Formula: `x = clientX / zoomDecimal`. Never subtract `iframeRect.left/top`. Never pass iframe events to `getMouseRelativePos()` or `getMouseRelativeCanvas()` — both add frameOffset internally (double-counts). See CLAUDE.md Regla 8.
- **Phaser = lazy load only** — never bundle into runtime player; dynamic `import()` only
- **Runtime player = Vanilla JS** — no React/Vue/Angular; runs inside LMS iframes
- **No localStorage in player** — SCORM `suspend_data` via `LMSSetValue` only
- **All assets → Garage** — never MongoDB for binary data
- **Storage Manager is CRITICAL** — never let GrapesJS save raw HTML
- **MinIO does not exist** — Garage only, everywhere
- **`forcePathStyle: true`** — required for all Garage S3 client calls

---

## File Locations Quick Reference

| What | Where |
|---|---|
| Task list | `tasks.md` |
| Beta review issues | `docs/issues/issues-BETA-R1.md` |
| Per-task issues | `docs/issues/issues-TXX.md` |
| Change history | `CHANGELOG.md` |
| E2E tests | `e2e/tests/*.spec.ts` |
| Block definitions | `packages/authoring-ui/src/editor/registerBlocks.ts` |
| Question props forms | `packages/authoring-ui/src/components/panels/` |
| GrapesJS init | `packages/authoring-ui/src/editor/initEditor.ts` |
| Storage converter | `packages/authoring-ui/src/editor/converters.ts` |
| E2E QA skill | `.claude/skills/elearn-e2e-qa/SKILL.md` |
| Audit consolidado (bugs sistémicos) | `docs/issues/audit-consolidado.md` |