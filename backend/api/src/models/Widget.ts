/**
 * Re-exports widget types from @elearn-studio/shared-types.
 * Previously maintained as a local definition; now uses the shared package (D-01).
 */

export { WIDGET_TYPES, type WidgetType, type Bounds, type BaseWidget } from '@elearn-studio/shared-types'
export { type Action, type ActionSequence } from '@elearn-studio/shared-types'
