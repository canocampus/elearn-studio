/**
 * Re-exports shared TypeScript interfaces from @elearn-studio/shared-types.
 * Previously maintained as manual mirrors; now uses the shared package (D-01).
 */

export {
  WIDGET_TYPES,
  type WidgetType,
  type Bounds,
  type BaseWidget,
  type Action,
  type ActionSequence,
  type SharedActionSequence,
  type Slide,
  type SlideTemplate,
  type Resource,
  type NavigationMode,
  type CourseSettings,
  type SCORMMetadata,
  type CourseDoc,
  type CourseListItem,
} from '@elearn-studio/shared-types'
