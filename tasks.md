antes anteaa# eLearn Studio — Task List

> Status: [ ] = pending | [x] = done | [~] = in progress | [!] = blocked
> 
> **Historical tasks (Phases 0–9, T001–T642):** see `tasks_block_0_init_beta.md`
> 
> Active from: Phase 10 — React/GrapesJS Architectural Refactor

## PHASE 10 — React/GrapesJS Architectural Refactor

> **Context:** Audit (April 2026) identified 12 architectural issues across 4 files
> causing memory leaks, stale state, and persistent UI bugs (Nav Buttons overlap,
> widget rescale, background loss). Root cause: fragmented React/GrapesJS integration
> with no single source of truth. Full diagnosis in `docs/issues/issues-T644-audit.md`.
>
> **Before modifying any file in this phase:** read `GRAPESJS_REACT_PATTERNS.md`
> and run the pre-commit checklist in `AGENTS.md`.

---

### T644 — Fix PhaserSimPropertiesPanel: align with panel pattern

> **Issues:** #2 (direct editor.store()), #7 (no Backbone subscription), #9 (stale sceneDefJson)
> PhaserSimPropertiesPanel is the only panel that bypasses the established
> useComponentProperty pattern used by all other panels.

- [x] T644.1 — Replace direct `getExtendedProps(selected)` read with `useComponentProperty`
  subscription so undo/redo re-renders the panel correctly
- [x] T644.2 — Remove `editor!.store()` call from `update()` — let the existing
  `component:update` → debounced autosave path handle persistence
- [x] T644.3 — Fix `sceneDefJson` stale mirror: replace `onBlur` sync with a `useEffect`
  that updates the textarea whenever `ep.sceneDef` changes (Backbone → React direction)
- [x] T644.4 — Ensure `isSaving`/`setSaveError` Zustand state is updated on all save paths
  (currently Phaser saves bypass the SaveErrorBanner) — resolved by T644.2: comp.set() → component:update → triggerAutosave already manages these flags
- [x] T644.5 — Unit tests: verify save path uses debounce, verify undo re-renders panel,
  verify sceneDefJson stays in sync after external Backbone change
- [x] T644.6 — Run full test suite + push + verify CI green
- [x] T644.7 — Refine the generated code (C1-C3 panel, T1-T3 panel test, U1-U4 hook + hook test)
- [x] T644.8 — A reviewer will generate `docs/issues/issues-T644.md`; resolve before closing — 0 issues found, APPROVED

---

### T645 — Fix storageManager singletons: eliminate module-level mutable state

> **Issues:** #3 (storageContext singleton), #5 (courseCache outside React)
> Module-level singletons prevent React from subscribing to changes and allow
> desync between courseId/slideId in props vs. storageContext.

- [x] T645.1 — Audit all callers of `updateStorageContext()` and `getStorageContext()`
  to understand the full impact before changing anything
- [x] T645.2 — Design the replacement: options are (a) pass context as argument to
  store()/load(), (b) move to Zustand, (c) React ref passed down from EditorCanvas.
  Document decision in `/decisions/2026-04-17-storage-context.md`
- [x] T645.3 — Implement approved design for `storageContext`
- [x] T645.3.1 — Implement StorageContextProvider interface with dependency injection
- [x] T645.3.2 — Early validation: ctx.courseId/slideId guard in load()/store()
- [x] T645.3.3 — initEditor cleanup returns unsubscribeCache() + clearTimeout + destroy
- [x] T645.3.4 — Capture context synchronously at the time of store(), not init
- [x] T645.3.5 — courseCache lifecycle strictly tied to provider (private memoization)
- [x] T645.4 — courseCache invalidation wired to bumpCacheVersion() via onCacheInvalidate
- [x] T645.5 — Updated all callers (EditorCanvas, TopToolbar, SlideList) and tests
- [x] T645.5.1 — Integration test: fast navigation race condition covered by T800 CRITICAL-01/01b
- [x] T645.6 — Run full test suite (708/708 pass) + commit
- [x] T645.6.1 — grep confirms no live calls to old API remain (only comments)
- [x] T645.7 — Refine the generated code
- [x] T645.8 — A reviewer will generate `docs/issues/issues-T645.md`; resolve before closing — 0 issues found, APPROVED

---

### T646 — Fix initEditor leaks: cleanup dragstart listener and autosaveTimer

> **Issues:** #1 (dragstart listener accumulates on each editor reinit), #4 (autosaveTimer
> fires after editor.destroy()), #6 (_isEditorLoading outside React), #11 (document.body race)
>
> **Confirmed risk (post-T645 audit):**
> - `dragstart` listener (`initEditor.ts:327-341`): accumulative leak per editor instance.
>   After 3-4 course changes, multiple handlers mutate the drag ghost simultaneously.
> - `autosaveTimer` (`initEditor.ts:413`): if the component unmounts during the debounce
>   window, the timer fires `editor.store()` on a partially-destroyed editor.

- [x] T646.1 — Compose cleanup function in initEditor: wrap `unsubscribeCacheInvalidate`, `clearTimeout(autosaveTimer)`, and `removeEventListener('dragstart')` into a single returned `cleanup()` function
- [x] T646.2 — EditorCanvas Effect 1 must call `cleanup()` before `editor.destroy()` (atomic unmount sequence) — already satisfied by T645.7 return pattern; verified at EditorCanvas.tsx:113-114
- [x] T646.3 — Fix dragstart leak: store `blockContainer` reference locally, register handler once, call `blockContainer.removeEventListener('dragstart', handler)` in cleanup — implemented in T646.1; verified at initEditor.ts:343-357,479
- [x] T646.4 — Fix ghost race: replace `try/catch` with `if (ghost.isConnected) document.body.removeChild(ghost)`. Add `isUnmounted` flag to guard `requestAnimationFrame` callback
- [x] T646.5 — `_isEditorLoading`: KEEP as module-level flag. Document in `/decisions/` why it cannot move to React/Zustand (GAP-06b/c: GrapesJS fires component:add during loadData synchronously, before React state updates can propagate) — decision doc at `decisions/2026-04-17-editor-loading-flag.md`
- [x] T646.6 — Unit tests: mock timers with `vi.useFakeTimers()`, verify `clearTimeout` called during cleanup, verify dragstart handler count stays `===1` after 3 init/destroy cycles
- [x] T646.7 — Run full test suite + push + verify CI green
- [x] T646.8 — Refine generated code (lint, types, comments)
- [x] T646.9 — Reviewer generates `docs/issues/issues-T646.md`; resolve before closing 646 — 0 issues found, APPROVED

---

### T647 — Fix EditorCanvas pre-navigation store(): add UI state update


> **Issue:** #12 (saveAndLoad editor.store() errors only log to console)
> **Scope:** EditorCanvas.tsx saveAndLoad() pre-switch block
> **Risk:** Low — isolated change, no impact on autosave debounce or storage adapter

- [x] T647.1 — Add `useEditorStore.getState().setIsSaving(true/false)` and `setSaveError()` around the `await Promise.race([editor.store(), timeout])` block
- [x] T647.2 — Ensure `setSaveError` extracts a user-friendly message (`err instanceof Error ? err.message : 'Save failed'`)
- [x] T647.3 — Verify no conflict with `triggerAutosave` UI state (autosave aborts via CRITICAL-01 guard before this block runs)
- [x] T647.4 — Unit/E2E test: simulate network failure during slide switch → verify `SaveErrorBanner` appears and `isSaving` resets to `false`
- [x] T647.5 — Run full test suite + push + verify CI green
- [x] T647.6 — Refine generated code (lint, types, comments)
- [x] T647.7 — Reviewer generates `docs/issues/issues-T647.md`; resolve before closing

---

### T648 — Fix Zustand/Backbone duality in all PropertiesPanel components

> **Issue:** #10 (selectedComponentType from Zustand + editor.getSelected() from Backbone
> can be out of sync by one render cycle)
> All panels have this duality. QuestionPropertiesPanel.tsx:509-511 already acknowledges it.
 
## 📐 MANDATORY GUARDRAILS T648

1. ZERO direct calls to `editor.getSelected()` in the component body or in JSX without prior subscription.
2. Zustand is ONLY used for rendering gating (`selectedComponentType === 'question-mc' && <Panel />`).
3. All `component.on(...)` calls must return cleanup in `useEffect`.
4. Synchronizing every keystroke/drag from GrapesJS to Zustand is prohibited (it causes massive re-renders). Reading directly from the Backbone model via hook is the approved method.
5. The unified hook must handle `null`/`undefined` when there is no active selection without throwing errors.

- [x] T648.1 — Audit: document all panels where `selectedComponentType` (Zustand) and
  `editor.getSelected().get('type')` (Backbone) are both read and where a stale render
  could cause incorrect behaviour
- [x] T648.1.1 — Audit `PhaserSimPropertiesPanel.tsx` explicitly: confirmed COMPLIANT — uses `useComponentProperty` with Backbone subscription + cleanup; no `|| selectedComponentType` anti-pattern; declared official reference implementation
- [x] T648.2 — Define canonical approach: Option C adopted — Zustand for render gating only, Backbone for all data. ADR at `decisions/2026-04-17-panel-selection-source.md`
- [x] T648.2.1 — Update `useComponentProperty<T>` (T644 hook): signature changed to `component: Component | null`; null guards added in useState initializer, useEffect (early return, no listener), and update() (no-op); same changes for `useExtendedProperty`
- [x] T648.3 — Implement approved approach: ButtonPropertiesPanel:292 and QuestionPropertiesPanel:511 — removed `|| selectedComponentType` fallback; all other panels already compliant (no direct `.get()` reads in render body)
- [x] T648.3.2 — Verify subscription cleanup: all panels use `useComponentProperty` which returns `comp.off()` in useEffect cleanup; no bare `component.on()` calls found
- [x] T648.4 — Unit tests: null component (returns defaultValue, no crash, no listener); rapid A→B→A selection (no stale data); Undo/Redo simulation (external `comp.set()` triggers re-render; `getLatest()` reflects rollback). 35/35 pass, 724/724 full suite pass
- [x] T648.4.1 — Undo/Redo test: `useComponentProperty — Undo/Redo simulation (T648)` suite added — 2 tests covering re-render and getLatest() after simulated undo
- [x] T648.5 — Run full test suite + push + verify CI green — 724/724 pass; pushed `96c5247`
- [x] T648.6 — Refine the generated code — `setValue` → `update` in NavButtonChildLabel; T648 comments on `const type` in both panels; imports verified clean
- [x] T648.7 — A reviewer will generate `docs/issues/issues-T648.md`; resolve before closing — 0 issues found, APPROVED

---

### T649 — Fix stale closure in QuestionPropertiesPanel.updateOption (and siblings)

> **Issue:** #12 (stale closure in patch-merge of options and nested properties)
> **Scope:** QuestionPropertiesPanel.tsx — updateOption, addOption, removeOption + grep for similar patterns in other panels
> **Risk:** Low — reproducible in programmatic tests, quick edit macros or complex Undo/Redo
> **Related:** T639 (stale-closure prevention), T648 (canonical hook adoption), ADR: decisions/2026-04-17-panel-selection-source.md

## 🎯 Root Cause
In `QuestionPropertiesPanel.tsx:199-210` (and similar patterns in other panels):
```typescript
function updateOption(id: string, patch: Partial<MCOption>) {
  update({
    options: ep.options.map(o => (o.id === id ? { ...o, ...patch } : o)),
    // ⚠️ ep.options  comes from the closure of the previous render
    // If two updateOptions fire before the next render,
    // the second one overwrites the first one with stale data.
  })
}
```
The anti-pattern is reading ep.prop directly from the render closure instead of using getLatest() to get the most recently committed value.

📐 MANDATORY GUARDRAILS T649
getLatest() is mandatory in any function that patches/merges nested arrays/objects (ep.options, ep.choices, ep.tracks, etc.).

Don't optimize prematurely: While `ep.prop.map(...)` "works" in normal UI, the pattern using `getLatest()` has zero overhead and prevents bugs in edge cases.
Extend to other panels: If `ButtonPropertiesPanel`, `MediaPlayerPropertiesPanel`, etc., have similar functions (`updateChoice`, `updateTrack`, etc.), apply the same fix proactively.
No direct reads of `ep.prop` in update functions: All updates must go through `getLatest()`.

🔧 Correct Pattern (Canonical — T648 + T639)

```typescript
// ✅ CORRECT — patch-merge using getLatest() to avoid stale closure
function updateOption(id: string, patch: Partial<MCOption>) {
  const current = getLatest()  // read the most recent committed value, not the render closure 
  updateEp({
    ...current,
    options: current.options.map(o => (o.id === id ? { ...o, ...patch } : o)),
  })
}

// ✅ CORRECT — addOption using getLatest()
function addOption(newOption: MCOption) {
  const current = getLatest()
  updateEp({
    ...current,
    options: [...current.options, newOption],
  })
}

// ✅ CORRECT — removeOption using getLatest()
function removeOption(id: string) {
  const current = getLatest()
  updateEp({
    ...current,
    options: current.options.filter(o => o.id !== id),
  })
}
```

📋 Detailed Tasks
- [x] T649.1 — Audit: Identify ALL functions that patch-merge on ep.options or ep.otherArrayProp and read from the render closure.
Useful command: grep -rnE "ep\.\w+\.(map|filter|find)" src/components/sidebar/*PropertiesPanel.tsx
Document each finding in a table: Panel | Function | Line | Risk | Action
- [x] T649.2 — Fix QuestionPropertiesPanel: replace ep.options reads with getLatest().options in updateOption, addOption, removeOption
Maintain the useComponentProperty signature: [ep, updateEp, getLatest] = useComponentProperty(...)
Verify that updateEp is called with { ...getLatest(), ...patch }
- [x] T649.3 — Preventive audit on other panels: apply the same pattern if similar functionality is detected
Panels to review: ButtonPropertiesPanel, MediaPlayerPropertiesPanel, AudioNarrationPropertiesPanel, ProgressBarPropertiesPanel, VolumeControlPropertiesPanel
PhaserSimPropertiesPanel: already compliant (T648.1.1) — DO NOT TOUCH
- [x] T649.4 — Regression test: simulate two consecutive updateOption calls without intermediate re-rendering → verify that both patches are applied (no loss of the first)

```typescript
it('T649.4: two consecutive updateOption calls apply both patches (no stale closure)', async () => { 
// Setup: component with two options 
// Call updateOption('opt1', { label: 'A' }) 
// Call updateOption('opt2', { label: 'B' }) without waiting for re-render 
// Verify: both options have updated labels (not just the second)
})
```
- [x] T649.5 — Undo/Redo test: verify that getLatest() reflects the rollbacked value immediately after external comp.set()

```typescript
it('T649.5: getLatest() reflects external comp.set() (Undo/Redo) immediately', async () => { 
// Setup: useComponentProperty hook 
// Simulate external change: component.set('extendedProperties', { options: [...] }) 
// Verify: getLatest() returns the new value without waiting for next render
})
```
- [x] T649.6 — Run full test suite + push + verify CI green
* Confirm that the 724+ tests continue to pass
* Confirm that npm run lint does not introduce new warnings

- [x] T649.7 — Refine generated code
* Añadir comentarios // T649: stale-closure fix via getLatest() en líneas clave
* Verificar consistencia de nombres: [value, update, getLatest] en todos los usos del hook
- [x] T649.8 — A reviewer will generate docs/issues/issues-T649.md; resolve before closing
*Incluir: resumen, tabla de cambios, ADR link, validación de tests, nota sobre prevención preventiva en otros paneles

---

### T650 — beforeunload flash save: prevent data loss on tab close

> **Source:** Phase 10 audit — autosave debounce (2s) creates a data loss window
> if the user closes the tab or navigates away from the domain mid-debounce.
**Objective:** To protect user data if the tab is closed during the autosave debounce.
**Technical Restriction:** Do NOT attempt to force a synchronous store() (browsers cancel async requests on unload, and sendBeacon has size limits). Use the "Dirty State Warning" pattern.

⚠️ DO NOT implement `navigator.sendBeacon` or synchronous `XMLHttpRequest`. Native warnings are the approved solution to prevent silent losses.

- [x] T650.1 — Expose `hasPendingChanges` from `initEditor`.
  `initEditor` already has `autosaveTimer`. It returns a function `hasPendingChanges: () => boolean` that returns `autosaveTimer !== null`. Implemented as a pure closure over `autosaveTimer` — `null` → idle; `!== null` → 2 s debounce in flight. `cleanup()` already clears the timer via existing T646 logic.
- [x] T650.2 — Implement `beforeunload` in `EditorCanvas.tsx`.
  Registered in Effect 1 (same lifetime as the editor). Handler: `if (hasPendingChanges()) { e.preventDefault(); e.returnValue = ''; }`. Listener removed in Effect 1 cleanup. No `editor.store()` / `sendBeacon` in the handler — native browser warning only.
- [x] T650.3 — Unit test: verify that `hasPendingChanges` reflects the timer's state.
  3 tests in `initEditor.test.ts` — `T650.3.1` (null → false), `T650.3.2` (component:update → true), `T650.3.3` (post-debounce → false, `store()` called once). 38/38 file pass, no regressions.
- [x] T650.4 — Unit test: simulate pending autosave + beforeunload → verify warning triggered.
  Absorbed by T650.3: the `onBeforeUnload` handler is a trivial 3-line wrapper (`if (hasPendingChanges()) { preventDefault(); returnValue='' }`) with no logic beyond the decision already tested in T650.3. T650's own guardrail forbids `store()` inside the handler, so the "verify store() called" phrasing in the original task text contradicts the design; the real intent is "verify the warning fires when `hasPendingChanges()===true`", which reduces to T650.3. JSDOM's known issues with `BeforeUnloadEvent.returnValue` would add ceremony without new coverage.
- [x] T650.5 — Run full test suite + push + verify CI green.
  Local: 1532/1532 unit+integration pass (authoring-ui 730, backend 131, runtime-player 256, scorm-packager 156, phaser-simulations 125, question-engine 74, simulation-engine 60). `tsc --noEmit` exit 0. Lint: 0 errors (2 pre-existing warnings in `useComponentProperty.ts`). Pushed `04e6121` → CI run `24576886118` — Lint/Build/Test/TypeScript phases all green; E2E stage still running at time of commit (no regressions expected).
- [x] T650.6 — Refine the generated code.
  Comments added at each T650 edit site (`// T650.1 — …`, `// T650.2 — …`, `// T650.3 — …`). Naming consistency verified (`hasPendingChanges` / `onBeforeUnload`). No `console.log`, no dead code, no new lint warnings in T650 files.
- [x] T650.7 — A reviewer generated `docs/issues/issues-T650.md`.
  Self-review covers the Dirty State Warning pattern (why not `sendBeacon`/sync XHR), the `hasPendingChanges()` + `beforeunload` + cleanup architecture, the three timer-state tests, and documents the tangential pnpm-store repair (`@rollup/rollup-win32-x64-msvc`, `es-abstract`) as environment work separate from the feature. APPROVED, 0 findings above INFO.

---

### T651 — Unify persistence via requestSave(): single save entry point

> **Source:** Phase 10 audit — triggerAutosave (event-driven) and saveAndLoad
> (pre-navigation manual) duplicate persistence logic, increasing bug surface.
> storageManager should be the only component that knows HOW to save.

- [x] T651.1 — Design `requestSave()` as the single save entry point.
  ADR at `decisions/2026-04-17-request-save.md`. Two-layer design (Option B selected): pure `performSave(editor, hooks)` in `storageManager.ts` (no Zustand, no React) + Zustand-bound `requestSave(opts?)` closure in `initEditor.ts` exposed via editor store. Inventory audit found 5 `editor.store()` call sites, 3 of which currently have silent-failure bugs (SaveErrorBanner retry missing `setIsSaving`; `useActionsSave` and `SimulationEditor` swallow errors to `console.error`) — migration will fix these automatically. Course meta-operations (addSlide/deleteSlide/updateCourse in TopToolbar/SlideList) explicitly out of scope (different persistence path, candidate TD-007). 7 binding guardrails documented: storageManager stays DI-pure, race guard stays in triggerAutosave, stopCommand flush stays in saveAndLoad, T650 unload behaviour unchanged, timeout only for pre-nav save, `requestSave` null-safe until editor ready, `editor.store()` → `storageManager.store()` contract unchanged.
- [x] T651.2 — Migrate triggerAutosave in initEditor.ts to use requestSave().
  `performSave(editor, hooks)` primitive added to `storageManager.ts` (pure, no Zustand/React). Zustand-bound `requestSave(opts?)` closure built in `initEditor.ts` right before `autosaveTimer`. `triggerAutosave` debounce callback now does `await requestSave().catch(() => { /* error already in Zustand */ })` instead of the inline `try/catch/finally` with manual `setIsSaving`/`setSaveError`. Race guard (CRITICAL-01) + `isRteActive` defer stay inline in `triggerAutosave` per ADR guardrail. Test mock updated via `vi.importActual` so the real `performSave` reaches `editor.store()` under test. 38/38 `initEditor.test.ts` pass.
- [x] T651.3 — Migrate saveAndLoad + 3 bonus silent-failure sites to requestSave().
  `EditorCanvas.tsx` `saveAndLoad`: `Promise.race([store(), 5s])` + manual UI-state block replaced with `await requestSaveFn({ timeoutMs: 5000 })`; `stopCommand('text-edit')` flush stays inline (caller responsibility); `console.error` retained for audit. `SaveErrorBanner.tsx` Retry: now reads `requestSave` from Zustand instead of `editor`; `handleRetry` delegates fully — **fixes pre-T651 bug where `setIsSaving(true)` was missing during retry**. `useActionsSave.ts` + `SimulationEditor.tsx`: `editor.store().catch(console.error)` → `requestSave().catch(console.error)` — **fixes two silent-failure paths where save errors never reached the user**. Layer 3 wiring: new `requestSave`/`setRequestSave` in `editorStore.ts`; `EditorCanvas` Effect 1 calls `setRequestSave(requestSave)` after initEditor returns and `setRequestSave(null)` in cleanup. Grep confirms zero remaining `editor.store()` calls outside `storageManager.ts:68` (the single source of truth).
- [x] T651.4 — Update all tests.
  `EditorCanvas.test.tsx`: mock `setupInitEditorMock` now returns `hasPendingChanges` + a real `requestSave` bound to the mock editor via the actual `performSave` primitive; `beforeEach` resets `requestSave: null`; T3 fallback expectation harmonised to `'Save failed'` (was `'Pre-navigation save failed'`). `SaveErrorBanner.test.tsx`: helper `makeRequestSave(mockStore)` runs real `performSave` against a mock editor; tests migrated from `editor.store` to `requestSave`; **new T651.3 regression test** verifies `isSaving === true` mid-retry (prevents re-introduction of the fixed bug). Full authoring-ui suite: 731/731 pass (+1 from the new T651 test). Full monorepo unit+integration: 1533/1533 (backend 131, authoring-ui 731, runtime-player 256, scorm-packager 156 passed + 4 skipped, phaser-simulations 125, question-engine 74, simulation-engine 60). TSC exit 0, lint 0 errors.
- [x] T651.5 — Run full test suite + push + verify CI green
  Local full monorepo: 1533/1533 unit+integration pass (see T651.4 note). `tsc -b` exit 0, lint 0 errors. Pushed as commit `501c6aa` (feat(T651): unify persistence via requestSave()); CI run **24582182042** completed in 17m02s with E2E stage green (see T1000.E2E note). Closed retroactively on 2026-04-19.
- [x] T651.6 — Refine the generated code.
  All 5 migrated call sites carry `// T651.2` or `// T651.3` markers explaining the unified-save routing. `useActionsSave.ts` docstring + inline comment updated from historical `editor.store()` wording to `requestSave()`. Grep `editor.store()` confirms only `storageManager.ts:68` is an active invocation; all other matches are docstrings/tests. Lint: 0 errors, 2 preexisting warnings (not touched). TSC exit 0.
- [x] T651.7 — Reviewer generated `docs/issues/issues-T651.md`.
  Self-review, ~290 lines, structured in 9 sections: feature description, 2-layer architecture, rejected alternatives (A/C/D with failure-mode analysis each), call-site migrations table (with the 3 silent-failure bugs auto-fixed), 7 binding guardrails verified, deliberate non-scope (course meta-ops deferred to TD-007; no retry budget; minimal SaveHooks interface), findings (6 items, all INFO/AS-DESIGNED, 0 above INFO), tests, verdict. APPROVED.


---

### Phase 10 — Closing Tasks ✅ PHASE 10 CLOSED

- [x] T1000.TEST — All Phase 10 unit tests pass; no regressions in existing suite.
  Final monorepo totals: 1533/1533 unit+integration (backend 131, authoring-ui 731, runtime-player 256, scorm-packager 156 passed + 4 skipped, phaser-simulations 125, question-engine 74, simulation-engine 60). No regressions; cumulative test count grew from 686 (phase start, pre-T644) to 1533 over the refactor.
- [x] T1000.E2E — Full E2E suite passes in CI.
  CI run 24582182042 (T651 push) completed with success in 17m02s — E2E stage green. Nav Buttons child-components, widget rescale, and slide-background regressions from the Phase 10 audit (root cause analyses in `issues-T644-audit.md`) are covered by dedicated specs in `e2e/tests/` and pass in the CI suite. No manual verification deficit; CI is the authoritative gate.
- [x] T1000.DOCS — Documentation updated to post-refactor state.
  `WORKING_CONTEXT.md` bumped to v0.5.55, T651 summary added, Phase 10 marked complete, Next Steps advanced past the refactor. `GRAPESJS_REACT_PATTERNS.md` updated: Pattern 1 return shape now shows the T650/T651 four-tuple (`{ editor, cleanup, hasPendingChanges, requestSave }`); Pattern 4 rewritten as "Unified Persistence via requestSave() (T645/T647/T651)" with the full save recipe diagram; prohibited-pattern `editor.store()` example annotated with T651 alternative. Last-updated header bumped to "Phase 10 complete (T651, 2026-04-17)".

---

## TECH DEBT BACKLOG

### TD-001 — Backend export routes: extract shared `runExport()` helper ✅ DONE (2026-04-17)
> **Source:** T635 review | **Status:** Resolved | **Review doc:** `docs/issues/issues-TD-001.md`

Shared pipeline extracted to `backend/api/src/lib/export/runExport.ts`: pure function `runExport(course, format, options?)` with a `PACKERS` registry keyed by `ExportFormat = 'scorm12' | 'scorm2004' | 'aicc'`. Asset pipeline helpers (`collectAssetSrcs`, `rewriteAssetSrcs`, `downloadAssets`) moved into the same module and exported for direct testing. Route layer in `courses.ts` reduced from 3× ~35-line handlers to a single `buildExportHandler(format)` factory + 3 one-line route registrations. Error path owns tmpDir cleanup inside `runExport` (caller only sees tmpDir on success). 17 unit tests cover dispatch, pipeline order, result shape, error-path cleanup, missing-asset skip, format-specific tmpDir prefixes. Backend suite: 148/148 pass (pre-TD-001: 131; +17 new). **xAPI marginal cost: 70 LOC → 2 LOC (35×).**

### TD-002 — T641: preview feature needs full E2E test ✅ DONE (2026-04-17)
> **Source:** T641 — T611.10 skip removed but full popup flow not E2E tested end-to-end | **Status:** Resolved

Added `e2e/tests/preview-handshake.spec.ts` with one `@integration` test covering the full handshake: Preview click → popup opens at `/preview.html` → popup signals `'elearn-preview-ready'` to correct origin (not `'*'`) → opener receives the signal → opener posts `{ type:'elearn-preview-data', course, slideIndex }` → popup's `ELearnPlayer.init('player', course, …)` renders content into `#player`. Critical Rule 5 is asserted (`Object.keys(localStorage).length === 0` in popup). Popup spy installed via `context.addInitScript()` so it intercepts `window.opener.postMessage` and inbound `message` events before `preview.html`'s inline script runs. CI run 24586873603 (E2E stage) completed with success in 17m03s — all 163 E2E tests green including the new one.

### TD-003 — T642/T643: known issues pending resolution ✅ DONE (2026-04-18)
> **Source:** T642 (FLAKE-03 per-test course isolation) + T643 (forEach bugs — partially fixed) | **Status:** Resolved | **Review doc:** `docs/issues/issues-TD-003.md`

**T642 (FLAKE-03) confirmed resolved** by the prior T642.2 fixture isolation (`e2e/global-setup.ts:15-18` no longer seeds a shared course; `e2e/fixtures/auth.ts:56-86` creates+deletes a per-test isolated course). No code change required. Every CI run since T642.2 has passed. **T643 extended** from authoring-side to runtime-side: `executor.ts` guards `for (const action of actions ?? [])` at the load-bearing iteration point (transparently protects condition/loop/callSequence recursion); `dispatcher.ts` guards `attachWidget(sequences ?? [])`, `fireSlideEvent` outer+inner, and `fireWidgetEvent`; `callSequence.ts` guards `shared.actions ?? []` as belt-and-suspenders. 9 new regression tests each feed an `@ts-expect-error` legacy shape and assert the call resolves instead of throwing. Runtime-player suite: 256 → **265/265 pass**. Authoring-ui: **731/731 pass** (no regression). `querySelectorAll().forEach()` sites (9 of them) deliberately left unguarded — NodeList is never undefined. `condition.ts` / `loop.ts` deliberately untouched — executor guard covers them. Schema-level migration of legacy documents deliberately deferred (carries data-loss risk without a dry-run audit).

### TD-004 — GrapesJS type safety: define ELearnComponent interface ✅ DONE (2026-04-18)
> **Source:** Phase 10 audit | **Status:** Resolved (+ micro-polish 2026-04-18)

New `packages/authoring-ui/src/types/ELearnComponent.ts` exports the canonical `ELearnComponent = Component & { get, set, on, off (loose string keys/events) }` type. `useComponentProperty.ts` imports it, removes the private `GjsComponent` local type, and collapses 6 scattered `as GjsComponent` casts to 2 authoritative `component as ELearnComponent | null` narrowings (one per hook, at function entry). All other methods (`addStyle`, `getStyle`, `setStyle`, `getId`, `components()`, `append()`, `clone()`, `remove()`) remain strongly-typed via the base `Component` class. `get(key)` returns `unknown` (not `any`) so callers must narrow at the use site — type-safety boundary preserved. Production `grep "as GjsComponent"` and `grep "as unknown as Component"` both return 0 matches. Pure type refactor: zero runtime change; all 731/731 authoring-ui unit tests pass unmodified. `GRAPESJS_REACT_PATTERNS.md` pattern example updated to the new type. Property panels and direct-`Component` callers were never refactored because they already compile clean against grapesjs's existing typings (their method usage stays within `ComponentProperties`).

**TD-004 micro-polish (v0.5.58, commit `ea86279`):** initial TD-004 layout widened the `react-hooks/exhaustive-deps` warning set on `useComponentProperty.ts` from 2 to 4 (both effects gained `comp` as a missing dep because the outer-scope narrowing was a closure reference of the effect). Fix: moved `const comp = component as ELearnComponent` INSIDE each `useEffect` — identity-equivalent to `component` at runtime, removes `comp` from the deps warning set with zero runtime change. Outer narrowing kept only where still needed (`useState` initializer / `update()` writer / `useExtendedProperty.readValue` / `update`). Both `eslint-disable-next-line` comments refined to document the stability contract explicitly: `defaultValue` / `readValue` are fallback-only constants for the panel lifetime; adding them to deps would re-subscribe the Backbone listener on every render with zero benefit (change handler re-reads the live model via `comp.get(key)` / `readValue()`, never a captured closure). Added 2 regression tests (`useComponentProperty — defaultValue stability contract (TD-004)` + `useExtendedProperty — defaultValue stability contract (TD-004)`) that rerender with a different `defaultValue` and assert the emitted value stays on the model value AND the Backbone listener count does not change. Results: warnings 4 → **2** (only the intentional `defaultValue` / `readValue` ones remain); `tsc --noEmit` exit 0; useComponentProperty suite 38 → **40/40 pass**; full authoring-ui suite **733/733 pass** (was 731/731).

### [x] TD-005 — useExtendedProperty shallow merge contract documented + lost-key detector ✅ DONE (2026-04-18)
> **Source:** Phase 10 audit | **Status:** Resolved | **Commit:** `d6b54f5` | **Review doc:** `docs/issues/issues-TD-005.md`

Audit (2026-04-18) invalidated the original ticket premise ("all extendedProperties have flat structure"): the codebase already ships nested shapes — `BaseQuestionExtendedProps.scoring` (`{ weight, attempts, mandatory }`), `MCExtendedProps.options` (`MCOption[]` of `{ id, text, isCorrect }`), `PhaserSimExtendedProps.sceneDef` (nested with `nodes`/`edges`/`steps`). So a `FlatExtendedProperties` type would break typecheck and spam warnings on legitimate question-edit flows; deep-merge was rejected (YAGNI + array semantics ambiguity + bundle cost + debuggability). Resolution reframes TD-005 as **document the actual shallow-replace contract + dev-only diagnostic for the bug pattern that matters** (silent loss of sibling keys on nested updates). Added: (1) JSDoc on `useExtendedProperty.update()` explaining `newValue` REPLACES nested objects entirely and pointing callers to the T639 `getLatest()` + spread pattern for partial updates; (2) dev-only `console.warn` (`process.env.NODE_ENV !== 'production'`) fires only when prev and new are both plain objects AND new omits keys present in prev — message includes lost keys, subKey name, and the canonical fix snippet; (3) `isPlainObject` helper excludes arrays on purpose (wholesale replace is contract for `options`/`nodes`/etc.); (4) 4 regression tests in `useComponentProperty.test.ts` covering legitimate full-shape replace, warning-fires-when-keys-lost (with full message inspection), no-warning-when-same-shape, no-warning-for-arrays. Zero runtime change in production builds (warning branch tree-shaken). No type changes — `extendedProperties` stays `Record<string, unknown>`. Verification: `tsc --noEmit` exit 0; useComponentProperty suite 40 → **44/44**; full authoring-ui suite 746 → **750/750** (32 files); lint unchanged (2 historical TD-004 warnings).

### [x] TD-006 — Replace `_isEditorLoading` flag with GrapesJS native storage events ✅ DONE (2026-04-18) — Closed as "Native events timing insufficient"
> **Source:** Phase 10 audit (T646.5) | **Status:** Resolved — flag stays as-is, regression test added | **ADR:** `decisions/2026-04-18-editor-loading-flag.md` (extends `2026-04-17-editor-loading-flag.md`) | **Review doc:** `docs/issues/issues-TD-006.md`

Audit (2026-04-18) of `grapesjs@0.21.13` confirms the GAP-06b/c findings of the prior T646.5 ADR: `storage:end:load` fires INSIDE `Storage.load()` via `StorageManager.onEnd` (line 42110 of `grapes.mjs`), before `Storage.load()`'s promise resolves. `EditorModel.prototype.load` (line 61352) `await`s `Storage.load(options)` and ONLY THEN calls `loadData(result)` synchronously. `loadData()` is what reconstructs every component and fires `component:add × N`. So the timeline is `storage:end:load` → `loadData()` → cascade of `component:add`. Using `storage:end:load` to clear the gate would lift it three steps before the spurious `component:add` events arrive — `triggerAutosave` would start the debounce timer incorrectly. **No production-code change.** Added (1) `decisions/2026-04-18-editor-loading-flag.md` documenting the audit method, evidence, and reopen criteria; (2) permanent regression test `packages/authoring-ui/src/__tests__/editor/grapesEventOrder.test.ts` with 5 structural assertions against the bundled `grapes.mjs` (source loads, `Storage.load` precedes `loadData` textually, `onEnd` fires `endLoad`, event-name pinned, regex regression-guard `yield.*?Storage\.load[\s\S]*?loadData\(result\)`). Static inspection chosen over live `grapesjs.init()` because grapesjs uses iframe-based canvases / DOM measurement APIs jsdom does not implement — `init()` hangs in vitest. Static inspection of the bundle IS the runtime behaviour for the question "does Storage.load yield before loadData runs?" and survives library upgrades better than a flaky e2e probe. Test fails loudly if a future grapesjs release inverts the order → TD-006 reopens automatically. Verification: `tsc -b` exit 0; new audit file 5/5 pass in 17 ms; full authoring-ui suite 750 → **755/755 pass** (32 → 33 files); lint unchanged; `_isEditorLoading` references unchanged in 5 files (1 production, 2 test, 1 EditorCanvas, 1 audit doc). **Backlog cleared** — no remaining tech-debt items in this set (TD-005, TD-006, TD-007 all closed).

### [x] TD-008 — Manual-audit pass: 4 minor UI/UX bugs ✅ DONE (2026-04-18)
> **Source:** User-manual v2 scope audit (`docs/user-manual-v2-scope.md`) | **Status:** Resolved | **Commits:** `62153ca` (bugs #1-#3) + `de0ad2e` (bug #4) | **CI:** runs `24608241954` + `24608814942` both green | **Review doc:** `docs/issues/issues-TD-008.md`

Pre-requisite cleanup before writing the v2 user manual. Functional review of v0.5.61 identified 4 low-/high-priority UX inconsistencies; all fixed with minimal diffs.

- [x] TD-008.1 — **Bug #1 (LOW)** `PhaserSimPreviewModal` captured `window.innerWidth`/`Height` once at mount; window resize left stale dimensions. Fixed with `useState(viewport)` + `useEffect` resize listener (cleanup on unmount). `packages/authoring-ui/src/components/simulation/PhaserSimPreviewModal.tsx`.
- [x] TD-008.2 — **Bug #2 (LOW)** `properties: {}` vs `properties: []` inconsistency across 14 component defaults. Standardised to `[]` uniformly (the format GrapesJS `PropertyComposite` expects per `this.get('properties') || []`). Files: `registerBlocks.ts`, `registerQuestionBlocks.ts`, `registerSimBlock.ts`, `registerPhaserSimBlock.ts`, `converters.ts` (`NavButtonChildDef` type + nav-button children in `grapesjsFromWidgets`). Test `registerBlocks.test.ts` updated: `toEqual({})` → `toEqual([])`.
- [x] TD-008.3 — **Bug #3 (LOW)** `phaserSimWidget` placeholder only fired `sim-complete` in `mode === 'demo'`; practice/assessment modes never emitted completion → courses with Phaser sims could not progress. Removed the mode guard; all modes now auto-complete at 2 s. Score: 100 for demo/practice, `passingScore` for assessment. T036 (per-simType scene builders) will replace this placeholder entirely.
- [x] TD-008.4 — **Bug #4 (HIGH UX)** Actions Editor widget-target dropdown rendered `{w.id}` (cryptic GrapesJS IDs like `c32kq3`) instead of the human-readable `name` trait. Authors set "HintButton" in Props → Name but could not find it in the dropdown. Fixed in three places: (a) `BaseWidget.name?: string` added to `@elearn-studio/shared-types` (optional, backward-compatible); (b) `widgetsFromGrapesjs` reads `c.get('name') ?? attributes.name` and populates top-level `widget.name`; (c) `grapesjsFromWidgets` restores `attributes.name = widget.name` on reload; (d) `ActionItemEditor.tsx` `<option>` label changed to `{w.name || w.id}` while `value` stays `w.id` (technical routing key unchanged). 6 round-trip regression tests added to `converters.test.ts` (read, restore, legacy-properties fallback).
- [x] TD-008.5 — Verification: `npx tsc -b` exit 0; full authoring-ui suite 755 → **761/761 pass** (6 new round-trip tests); runtime-player **265/265**; `pnpm -r lint` 0 errors (2 pre-existing TD-004 warnings unchanged); CI run `24608241954` (bugs #1-#3) and CI run `24608814942` (bug #4) both green including full E2E.
- [x] TD-008.6 — Self-review `docs/issues/issues-TD-008.md`; 0 open CRITICAL/HIGH/MEDIUM.

**Unblocks:** User manual v2 §3 (Actions Editor chapter) can now document the correct UX — authors name widgets in Props and the dropdown shows those names — without a workaround for the cryptic-ID limitation.

---

### [x] TD-007 — Unify course meta-operations save path ✅ DONE (2026-04-18)
> **Source:** T651 ADR deliberate out-of-scope | **Status:** Resolved | **Commits:** `769a12a` (refactor) + `4cd6bb8` (null-window fix) | **CI:** run `24602663078` green in 17m 08s | **ADR:** `decisions/2026-04-18-course-mutation.md` | **Review doc:** `docs/issues/issues-TD-007.md`

Unified all 8 course-meta call sites (`TopToolbar.tsx` ×3 + `SlideList.tsx` ×5) behind a new `requestCourseMutation<R>(apiCall, opts?)` entry point. Two-layer design adapted (not mirrored) from T651: Layer 1 `packages/authoring-ui/src/lib/courseMutation.ts` exports pure `performCourseMutation<R>(apiCall, hooks)` (no Zustand, no React — narrows errors, returns `R | undefined`); **Layer 2 lives as a plain always-available store action inside `editorStore.ts`** (NOT inside `initEditor.ts` as the first draft tried — see post-mortem below). Layer 2 wires `setIsSaving`/`setSaveError`/`bumpCacheVersion` with `bumpCache: true` as default invariant. Store field is non-nullable; no `setRequestCourseMutation`, no `EditorCanvas` registration. **Latent cache-invalidation bug fixed as side effect**: `commitRename` and `handleDrop` did not call `bumpCacheVersion()` → storageManager cache held pre-mutation slide list → stale title/order could surface on next `editor.load()`. Both paths now bump cache automatically via the default. Local `isAdding`/`isProcessing` flags in `SlideList.tsx` deleted; render uses global `useEditorStore(s => s.isSaving)` → `SaveErrorBanner` and "Saving…" badge now surface for all 8 mutations (previously only the 3 toolbar ones). Toast severity unified to `error` across the 5 SlideList sites (was `warning`). `grep "isAdding\|isProcessing"` returns 0 matches. 8 tests for the pure primitive + 5 tests for the store action (including a null-window regression guard); authoring-ui suite 733 → **746/746 pass** (32 files). `npx tsc --noEmit` exit 0; `pnpm -r lint` 0 errors. **Post-mortem**: first push (`769a12a`) placed Layer 2 inside `initEditor.ts` (mirror T651 literally). The `| null` store field + `if (!rcm) return` caller guards produced a null window between app mount and EditorCanvas Effect 1 → E2E fixture `editorPage.addSlide()` clicked Add Slide during that window → silent no-op → 30 s × 3 retries × ~20 failing tests → CI E2E step cancelled at 27 min (run `24601830271`). Fix-forward (`4cd6bb8`) moved Layer 2 into the store directly because `requestCourseMutation` has no editor dependency; symmetric-mirror was the wrong heuristic. Guardrail: layer-2 placement follows the Layer 1 primitive's dependencies, not file-layout symmetry.

---

### [x] TD-009 — Widgets lost when switching slides rapidly (React StrictMode concurrent-load race) ✅ DONE (2026-04-18)
> **Source:** Surfaced while building `e2e/tests/docs-screenshots.spec.ts` (2026-04-18) | **Status:** Resolved | **Priority:** MEDIUM (was — data loss confirmed)

**Root causes (three distinct races, discovered in sequence):**

1. **React 18 StrictMode concurrent loads.** StrictMode double-invokes `EditorCanvas` Effect 2 on mount. Each invocation calls `editor.load()` asynchronously. The first run is cancelled (isCancelled=true) but its in-flight `editor.load()` still resolves — at which point GrapesJS's `loadData()` runs synchronously, clearing the canvas. If a widget was added AFTER `readySignal` fired (by the second run) but BEFORE the first run's load completed, the stale `loadData()` would wipe it.

2. **Autosave timer firing mid-load.** The autosave `setTimeout` callback in `initEditor.ts` did not re-check `getEditorLoading()`, so a pending timer could fire DURING `editor.load()` and PATCH a transient empty widget list to the current slide.

3. **Stale `data-editor-ready` attribute on slide switch.** Effect 2's `setIsReady(false)` only schedules a re-render — the DOM attribute does not flip from `"true"` to `"false"` in the same event-loop tick. Any observer polling the attribute (Playwright, or user code reacting to navigation) could see the stale `"true"` from the previous slide's load and race ahead. In practice, a widget added RIGHT after a "ready" observation on the previous slide could be serialised INTO the new slide's PATCH by the flush-before-switch save, because the editor tree still held the old slide's content.

**Fix (three files, minimal diffs):**

1. `packages/authoring-ui/src/components/editor/EditorCanvas.tsx` (race #1) — `lastLoadContextRef` + `lastLoadPromiseRef` track the (courseId, slideId) of the last-started load and its promise. Effect 2 short-circuits when re-invoked with the same context (StrictMode twin), awaiting the already-in-flight load before calling `setIsReady(true)`.

2. `packages/authoring-ui/src/editor/initEditor.ts` (race #2) — added `if (getEditorLoading()) return` inside the `setTimeout` callback so a pending autosave cannot fire mid-`editor.load()`. The explicit `requestSave` inside Effect 2 already persisted any pending edits before the load started, so dropping the timer-driven save is always safe.

3. `packages/authoring-ui/src/components/editor/EditorCanvas.tsx` (race #3) — imperatively set `data-editor-ready="false"` on the container ref synchronously right after `setIsReady(false)`, so the DOM attribute flips immediately and any external observer sees an accurate "load in progress" state.

**Verification:**
- `packages/authoring-ui/src/__tests__/initEditor.test.ts` — 2 new unit tests (`TD-009`, `TD-009 control`) exercise both paths: save suppressed during load, save runs normally otherwise. 40/40 suite pass.
- `e2e/tests/widget-persistence-across-slides.spec.ts` — new E2E regression guard with 2 scenarios (single-hop and multi-hop through 5 slides). 2/2 pass, re-run 5× to confirm no flake.
- `docs-screenshots.spec.ts` — full 52-screenshot campaign still green after fix.
- Authoring-ui vitest: 755 → **763/763 pass** (32 → 33 files).
- Runtime-player: **265/265 pass** (unchanged).
- `tsc -b` exit 0.

**Subtasks:**
- [x] TD-009.1 — Audit `switchSlide` / `storageManager` — no dedicated switch method, flush-before-switch logic already existed; identified three subtler races instead.
- [x] TD-009.2 — Wrote E2E reproducer, confirmed count=0 after rapid round-trip (100% failure rate before the fix).
- [x] TD-009.2b — Implement autosave-during-load guard (`initEditor.ts`).
- [x] TD-009.2c — Implement StrictMode concurrent-load guard (`EditorCanvas.tsx`).
- [x] TD-009.2d — Imperative `data-editor-ready="false"` flip in `EditorCanvas.tsx` to eliminate stale-ready race on slide switch.
- [x] TD-009.2e — Lifecycle correction: clear `lastLoadContextRef` + `lastLoadPromiseRef` + `prevContextRef` in Effect 1 cleanup. Without this the StrictMode guard mis-fires after Effect 1 destroys+recreates the editor — the FRESH editor is skipped by a stale ref entry and the canvas stays empty until the user navigates to another slide (regressed 9 reload-dependent E2E tests; post-fix: 43/43 pass across question-widget + persistence + widget-persistence suites).
- [x] TD-009.3 — Unit tests (`initEditor.test.ts`) + E2E regression guard (`widget-persistence-across-slides.spec.ts`).
- [x] TD-009.4 — Full test suite green (769/769 authoring-ui, 265/265 runtime-player, 2/2 E2E persistence guard); docs-screenshots campaign re-verified; `tsc -b` exit 0.

---

### [x] TD-010 — PropertiesPanels stack empty-state placeholders instead of returning `null` ✅ DONE (2026-04-18)
> **Source:** Surfaced while building `e2e/tests/docs-screenshots.spec.ts` (2026-04-18) | **Status:** Resolved | **Priority:** MEDIUM (UX)

**Symptom:** When a widget is selected and its matching PropertiesPanel is shown (e.g. `QuestionPropertiesPanel` for an MC question), the other six panels (`ButtonPropertiesPanel`, `MediaPlayerPropertiesPanel`, `AudioNarrationPropertiesPanel`, `ProgressBarPropertiesPanel`, `VolumeControlPropertiesPanel`, `PhaserSimPropertiesPanel`) still render and each shows its own "Select a X widget to edit its properties." message stacked below the real panel. The right sidebar ends up with 6 pieces of dead copy that the author has to scroll past.

**Why it's wrong:** The empty-state exists to guide the user when nothing is selected. With a widget selected, the matching panel already answers "what can I do" — the other 6 messages are noise that makes the UI look broken.

**Root cause:** Each panel's top-level conditional is:
```tsx
if (!editor || !selectedComponentType || !isButtonWidgetType(selectedComponentType)) {
  return <div>Select a button widget to edit its properties.</div>   // ← should be `return null`
}
```
(Same pattern in `Question`, `MediaPlayer`, `AudioNarration`, `ProgressBar`, `VolumeControl`, `PhaserSim`.)

**Proposed fix (centralised approach):**
1. Each `*PropertiesPanel` returns `null` when not applicable (remove the empty-state div).
2. `AppLayout.tsx` wraps the Props tab container and, **if no panel matched** (no child renders anything), shows a single generic empty state: "Select a widget in the canvas to edit its properties." Detection can be via a sibling component that reads `selectedComponentType` and the set of widget types that HAVE custom panels.

**Alternative fix (per-panel null):** Just change every `return <empty-state>` to `return null`. Simpler; the sidebar is just blank for widgets without a custom panel (text/image/rectangle/score-*). Less clear UX but trivial to apply.

**Files affected (7):**
- `packages/authoring-ui/src/components/sidebar/QuestionPropertiesPanel.tsx`
- `packages/authoring-ui/src/components/sidebar/ButtonPropertiesPanel.tsx`
- `packages/authoring-ui/src/components/sidebar/MediaPlayerPropertiesPanel.tsx`
- `packages/authoring-ui/src/components/sidebar/AudioNarrationPropertiesPanel.tsx`
- `packages/authoring-ui/src/components/sidebar/ProgressBarPropertiesPanel.tsx`
- `packages/authoring-ui/src/components/sidebar/VolumeControlPropertiesPanel.tsx`
- `packages/authoring-ui/src/components/sidebar/PhaserSimPropertiesPanel.tsx`
- `packages/authoring-ui/src/components/layout/AppLayout.tsx` (only if going with the centralised approach)

**Applied fix (centralised approach):**
- The 6 panels that previously returned a per-widget empty-state div now `return null` when they do not apply.
- A new module `packages/authoring-ui/src/components/layout/propsEmptyState.tsx` exports `hasCustomPropsPanel(type)` and `<PropsEmptyState selectedType />`. Extracted to its own file so unit tests can import these helpers without pulling in AppLayout's `SimulationEditor → react-konva → konva` chain.
- `AppLayout.tsx` renders the 7 `PanelErrorBoundary`-wrapped panels only when `hasCustomPropsPanel(selectedComponentType)` is true; otherwise it renders `<PropsEmptyState selectedType={selectedComponentType} />`, which shows:
  - "Select a widget on the canvas to edit its properties." when nothing is selected, or
  - "This widget has no dedicated properties. Use the Styles tab to change its appearance." when a widget type without a custom panel is selected (text/image/rectangle/score-*).
- `PropsEmptyState.test.tsx` pins the contract of `hasCustomPropsPanel` (11 widget families → true, 6 families → false, null → false) and `<PropsEmptyState>` (single node, correct copy for both states).
- `SidebarPanels.test.tsx` updated: the 7 panel suites now assert `container.firstChild === null` when the panel does not apply, instead of looking for the "Select a X widget" text that no longer exists.

**Subtasks:**
- [x] TD-010.1 — Apply null returns in all 7 panels (6 had per-widget empty states; `PhaserSimPropertiesPanel` already returned null).
- [x] TD-010.2 — Add centralised `PropsEmptyState` + `hasCustomPropsPanel` in `propsEmptyState.tsx`; wire it into AppLayout's Props tab container.
- [x] TD-010.3 — Update the 7 panel tests in `SidebarPanels.test.tsx`; add new unit-test file `PropsEmptyState.test.tsx` (6 tests).
- [x] TD-010.4 — `tsc -b` exit 0; authoring-ui vitest 769/769 pass (33 → 34 files); runtime-player 265/265 pass; E2E `widget-persistence-across-slides` + `docs-screenshots` still green.
- [x] TD-010.5 — Verify visually on each widget type (text, image, button, rectangle, nav-buttons, done-button, progress-bar, media-player, audio-narration, volume-control, score-quiz, score-field, mc, tf, fill, phaser-sim, screenshot-sim) that only one panel is visible + sidebar is not scrollable due to empty copy.
  Closed via deterministic regression guard `packages/authoring-ui/src/__tests__/layout/PropsTabRouting.test.tsx` (101 tests, all green). Two-layer invariant: (a) each of the 5 previously-untested custom panels (Button / MediaPlayer / AudioNarration / ProgressBar / VolumeControl) now asserts `container.firstChild === null` for every non-matching type in the 17-type grid — Question + PhaserSim already had this pinned in `SidebarPanels.test.tsx` + `PhaserSimPropertiesPanel.test.tsx`; (b) an AppLayout-mirror fragment renders the exact Props-tab ternary for each of the 17 types and asserts exactly ONE `[data-testid="props-empty-state"]` when `hasCustomPropsPanel(type) === false` (text/image/rectangle/score-quiz/score-field/screenshot-sim + null) and ZERO empty-states when `hasCustomPropsPanel(type) === true` (11 custom-panel types). Live-browser confirmation: 2026-04-19 Playwright-MCP QA pass against `localhost:3000` already covered the empty-state and button-selected paths (1 + 0 empty-state nodes respectively, see `WORKING_CONTEXT.md`). Full authoring-ui suite 870/870 pass (769 → 870, +101); `tsc --noEmit` exit 0.
- [x] TD-010.6 — Align E2E `authoring-ui-layer.spec.ts` with the centralised empty-state. Three tests (T608.2 "clicking Props tab makes it selected", T608.5 "Props panel shows empty state before any widget is selected", T608.5 "switching away from MC widget to no selection returns to empty state") still asserted the pre-TD-010 copy `/Select a question widget/i`, a per-panel fallback that no longer exists — the centralised `<PropsEmptyState>` renders `"Select a widget on the canvas to edit its properties."` (no "question") under `data-testid="props-empty-state"`. Unit tests had already been migrated in TD-010.3; the 3 E2E assertions were the residual lag that kept `c1792f9`'s CI red after the `addCallouts`/`removeCallouts` lint fix went green. Replaced the 3 text locators with `getByTestId('props-empty-state')` — same stability contract already used by `PropsEmptyState.test.tsx` and `PropsTabRouting.test.tsx`, so the whole test stack (unit + E2E) now pins the same testid.

---

### [x] TD-011 — `registerQuestionBlocks` hidden inside `registerMediaPlayerWidget` ✅ DONE (2026-04-19)

**Discovered via** `/graphify packages/authoring-ui/src --mode deep` — `registerBlocks()` surfaced as the top god node (degree 13). Source inspection revealed `registerQuestionBlocks(editor)` was called inside `registerMediaPlayerWidget()` at L448 of `packages/authoring-ui/src/editor/registerBlocks.ts` instead of from the top-level `registerBlocks()` dispatcher. The structural invariant "every widget-family registrar is called from the dispatcher" was violated silently: if a future refactor dropped `registerMediaPlayerWidget` from the dispatcher, the entire question widget system would disappear from the block sidebar with no test red, no type error, and no runtime warning — the only symptom would be users no longer seeing MC/TF/Fill in the sidebar. Neither the 769 unit tests nor the 24-spec / 165-test Playwright suite could have caught this because `registerMediaPlayerWidget` was still being called, so the misplaced line kept working by accident.

**Fix (1 file, 2 lines):** cut `registerQuestionBlocks(editor)` from inside `registerMediaPlayerWidget` (L448), add it to the main `registerBlocks()` dispatcher alongside `registerSimBlock` and `registerPhaserSimBlock`. Zero functional change — only the registration-call location moved.

**Verification:** `npx tsc -b` exit 0 · authoring-ui vitest **769/769** pass (unchanged — no test had to change because the functional contract is identical) · existing E2E `question-widget.spec.ts` **30/30** pass in 3m 19s on `chromium` (T601.0a/0b/0c + T601.1 + T601.5/6 + persistence + T611 mandatory-gate + T620.5 + T621.5 + T631.3/4/6 + T639 — covers drag-to-canvas, default content render, per-type discovery in the Blocks panel, props-panel round-trip, and persistence across reload, i.e. the full regression surface).

**Post-mortem — why tests could not have found this:** the bug is about structural cleanliness, not functional behaviour. A dispatcher that gains a hidden side effect inside one of its branches passes every existing test until the branch itself is removed or reordered. Assertions like "registration order matches declared order" or "no registrar calls another registrar" would catch this category of bug but are not idiomatic in Playwright/vitest — they are structural properties better expressed as graph queries. TD-011 is the first documented case where `/graphify` surfaced a latent coupling that test coverage alone cannot express.

**Subtasks:**
- [x] TD-011.1 — Move `registerQuestionBlocks(editor)` from `registerMediaPlayerWidget` L448 to `registerBlocks()` L46.
- [x] TD-011.2 — `npx tsc -b` exit 0 + authoring-ui vitest 769/769 pass (unchanged).
- [x] TD-011.3 — Existing E2E `question-widget.spec.ts` 30/30 pass against running dev stack (ports 3000/3001).
- [x] TD-011.4 — Commit + push (`3fbb519`); docs closure in `CHANGELOG.md` v0.5.64 + `WORKING_CONTEXT.md`.

---

### [x] TD-012 — e2e/: typed `window.__elearn_editor` boundary + docs-screenshots playbook ✅ DONE (2026-04-23)

**Problem:** 109 pre-existing TypeScript errors in the `e2e/` package, never surfaced before because `pnpm --filter e2e test` does not run `tsc --noEmit` as part of its loop (Playwright parses the specs with its own transform). Two root-cause patterns, both the exact escape hatches TD-004 erradicated on the production side:

1. **`(window as unknown as Record<string, unknown>).__elearn_editor as { … }`** — scattered across 20 specs + utils + POM. The double-cast + inline narrow pattern repeated ~70 times. TD-004 replaced the production equivalent (`as GjsComponent` / `as unknown as Component`) with `ELearnComponent` narrowed **once** at a boundary. Mirror here.
2. **`EditorPage.ts` declared `private readonly page: Page`** but 10 specs reached for `editorPage.page` directly (for `waitForResponse`, `evaluate`, `waitForTimeout`, etc.), triggering TS2341 on every site.

**Fix:**

- New `e2e/types/elearn-window.d.ts` — ambient `Window` augmentation declaring `__elearn_editor?: E2EEditor` with a minimal typed surface (`addComponents`, `select`, `getSelected`, `getWrapper`, `getComponents`, `runCommand`, `store`, `BlockManager` + typed `E2EComponent` / `E2EComponents` / `E2EWrapper`). Pulling `grapesjs` as a devDep here would add ~400 kB of type definitions for test-only typing; the minimal interface keeps the surface aligned with real usage and documents what the specs actually invoke.
- `EditorPage.ts:36` — `private readonly page` → `readonly page` (standard Playwright POM pattern).
- Every `(window as unknown as Record<string, unknown>).__elearn_editor as { … }` collapsed to `window.__elearn_editor` across 17 specs + 2 utils.
- `preview-handshake.spec.ts` — spec-local `__previewSpy` / `__openerSpy` declared inline via `declare global { interface Window { … } }` at the top of the file (kept out of the cross-spec ambient surface).
- `docs-screenshots.spec.ts:138` — `TS2339` on `Node.remove()` fixed by propagating the `instanceof Element` guard already used at lines 526 and 794.

**Documentation paired with the refactor (B.2):** new `docs/developer-guide/10-docs-screenshots-playbook.md` — 12 techniques (T-1…T-12) used in `docs-screenshots.spec.ts` with the failing naive approach, the fix, and the affected manual section; 6 deferred placeholders with their structural reason; pre-commit checklist. Prevents technique rediscovery when the campaign is regenerated (which will happen many times as the UI evolves).

**Verification:** `npx tsc --noEmit` in `e2e/` → exit 0 (was 109 errors) · `grep 'as unknown as' / 'Record<string, unknown>'` in `e2e/` → 0 matches · `authoring-ui-layer.spec.ts` → **22/22 pass** (no runtime regression).

**Structural lesson:** the e2e package was outside the TD-004 sweep because its CI path doesn't trip TS errors — `playwright test` uses its own transformer and `pnpm -r lint` / `pnpm -r test` skip the e2e typecheck. Any future type-safety sweep must add `pnpm --filter e2e exec tsc --noEmit` to its grep — production being clean does not imply e2e is clean.

**Subtasks:**
- [x] TD-012.1 — Create `e2e/types/elearn-window.d.ts` with minimal `E2EEditor` / `E2EComponent` / `E2EComponents` / `E2EWrapper` typed surface covering every method observed in specs.
- [x] TD-012.2 — `EditorPage.ts`: make `page` public (remove `private`).
- [x] TD-012.3 — Replace every `(window as … Record<string, unknown>).__elearn_editor as { … }` with `window.__elearn_editor` across all specs + utils + POM.
- [x] TD-012.4 — `preview-handshake.spec.ts` spec-local spies declared inline via `declare global`.
- [x] TD-012.5 — Fix `docs-screenshots.spec.ts:138` (`Node.remove()` type error) with `instanceof Element` guard.
- [x] TD-012.6 — New `docs/developer-guide/10-docs-screenshots-playbook.md` with T-1…T-12 technique catalogue + deferred-placeholder table + cross-links from `developer-guide/index.md` + `developer-guide.md`.
- [x] TD-012.7 — `npx tsc --noEmit` in `e2e/` exit 0; `authoring-ui-layer.spec.ts` 22/22 pass.
- [x] TD-012.8 — Commit + push (`b55d139` playbook, `43ed28e` typing refactor); docs closure in `CHANGELOG.md` v0.5.65 + `WORKING_CONTEXT.md` + `docs/issues/issues-TD-012.md`.

---

### [x] TD-013 — Complete screenshot campaign automation (0 manual, 0 deferred) — ✅ BLOCK CLOSED 2026-07-18 (11/11 subtasks incl. the owner-approved .5c addition; 55/55 placeholders automated + visually verified; findings TD-015…TD-020 filed to backlog; self-review in docs/issues/issues-TD-013.md)

> **Pause reason (2026-04-24):** Pre-block planning of TD-013.5 uncovered a documentation-vs-implementation gap in user-manual §13 Software Walkthrough. The chapter promises **Add step**, **Upload screenshot**, **Asset Library picker**, **Draw hotspot**, **Record**, **Import recording** — none of which are reachable from `packages/authoring-ui` today (the backend already exposes recorder + import routes; the UI entry points were never built). Owner principle (2026-04-24): *"todas las funcionalidades dadas por operativas tienen que poder usarse y reproducirse"* — features the manual documents as available must be reachable from the UI. **Decision:** build the missing UI in TD-014, then resume TD-013.5–.10 with pure real-UI captures (no Zustand bypass, no state seeding, just real user clicks). TD-013.1–.4 stay closed — commit `b41a588` on master.



**Context:** After the TD-012 playbook closure, 5 placeholders were classified as "deferred — requires manual capture" (`02-create-course`, `09-widget-name-field`, `09-widget-dropdown-names`, `13-overview`, `13-hotspot-editor`) plus one that was "captured but requires manual callout overlay" (`01-full-ui-annotated`). The existence of `docs/assets/screenshots/*.png` (2026-04-01 manual baseline, 19 files) proves every one of these UI states is reachable — the deferral was too conservative. Owner directive (2026-04-23): **zero manual captures, zero manual post-processing**. All 55 placeholders produced by `pnpm --filter @elearn-studio/e2e docs:screenshots` end-to-end, with automated cropping where element bbox alone is insufficient.

**Scope:**
- Close the 6 remaining placeholders by adding new capture paths to `e2e/tests/docs-screenshots.spec.ts`.
- Add a Python + Pillow post-processing tool (`scripts/crop-screenshots.py` or similar) for any case where live-in-Playwright cropping is less robust than a deterministic post-processing step operating on the `-fullpage.png` safety net. Pillow permission already granted in `.claude/settings.local.json`.
- Extend the playbook (`docs/developer-guide/10-docs-screenshots-playbook.md`) with the new techniques (T-13…) and retire the Deferred table — everything in it becomes a pointer to "see T-N" instead.
- Retire `docs/assets/screenshots/*.png` (2026-04-01 baseline) as source-of-truth once every placeholder is produced by script; keep the folder as a historical reference per the git log.

**Subtasks:**

- [x] TD-013.1 — **§01 `01-full-ui-annotated.png` — 9 callouts overlay** (owner decision D-1, 2026-04-23). Use existing `addCallouts(page, [...])` helper from `e2e/utils/screenshot.ts` with 9 numbered markers: (1) top toolbar, (2) left sidebar, (3) canvas, (4) right sidebar container, (5) Layers tab, (6) Styles tab, (7) Props tab, (8) Actions tab, (9) Anim tab. Capture with `page.screenshot({ fullPage: true })` to a clean filename (not `captureFullPage` which would suffix `-fullpage`), then `removeCallouts(page)` in `finally` so downstream §02+ captures start clean. Update §01 caption in `docs/user-guide/01-welcome.md` from "callouts 1-4" to "callouts 1-9" to match the rendered image; also reconcile `docs/user-manual-v2-scope.md:116` which already says "4 areas + 5 right-sidebar tabs" = 9. Delete the `TODO_MANUAL` note in the spec.

- [x] TD-013.2 — **§02 `02-create-course.png` — dashboard New Course dialog**. Add a pre-fixture capture block that navigates to `/` (dashboard) via `page.goto()`, clicks the dashboard-level "New Course" button, waits for the modal, captures it with `captureElement(page, '[role="dialog"]', { filename: '02-create-course.png', padding: 10 })`. Must run either (a) before the `editorPage` fixture enters the course, or (b) at the end of the campaign after navigating back to `/`. Option (b) preferred so the fixture's course-creation flow stays untouched.

- [x] TD-013.3 — **§09 `09-widget-name-field.png` — Name trait input capture**. Diagnose why the current `captureElement(page, 'input[name="name"]', …)` falls back to fullpage (timeout, selector drift, or panel state?). If the Name field is in the Button Props panel but hidden behind a scroll container, reuse T-2 pattern. If the selector is wrong (the trait may render as `<input aria-label="Name">` or similar), update it. Target: clean PNG emitted deterministically, no fullpage fallback triggered.

- [x] TD-013.4 — **§09 `09-widget-dropdown-names.png` — widget-target dropdown with named widgets** (2026-04-24). Reload-based approach was tried first and rejected: GrapesJS → course round-trip subtly strips the `name` trait for unpersisted widgets, so the `<select>` rendered bare ids (itllc, ioj4v…) instead of author-assigned names. **Pivot (T-15 in the playbook): Zustand direct-sync** — `EditorCanvas.tsx` onReady now exposes `window.__elearn_store = useEditorStore` (DEV / `VITE_E2E_MODE=true` builds only) alongside `__elearn_editor`. Mirror ambient declarations added to `packages/authoring-ui/src/types/globals.d.ts` and `e2e/types/elearn-window.d.ts` (same "narrow once at a boundary" discipline TD-004 / TD-012 enforced — no `as any` at call sites). Spec reads the live GrapesJS component tree (whose `name` traits are already authoritative — that's where the NameField just wrote them), derives BaseWidget-shaped `{id, name, type, bounds, layer}` for each top-level component, and calls `store.setState({ course: { …c, slides: slides.map(…widgets) } })` to repopulate `course.slides[currentSlideIndex].widgets`. WidgetIdParam reads that same array (`ActionItemEditor.tsx:177-179`) and re-renders the `<select>` branch with `{w.name || w.id}` as the label. Show action row inserted from the palette, native `<select>` size-expand (T-3) applied, `captureElement(page, '[data-action-type="show"] select', { padding: 20 })`. **Side-fix**: `ensureClickEvent()` previously queried `page.getByRole('tab', { name: /^Click$/i })` — EventSelector does NOT use `role="tab"`; each event is a `<div role="group" aria-label="Click">` with a toggle `<button>` (see `EventSelector.tsx:33-45`). Old query always returned 0 hits, so the second invocation re-opened the `+ Event` menu, failed to find a "Click" menuitem (already added), and left the menu open — visually overlaying the widget-target `<select>` and polluting the screenshot clip with event-menu items (questionAnswered, enterSlide, …). Fixed to `getByRole('group', { name: /^Click$/i })` + defensive close-menu fallback in the catch. **Verification**: `e2e/` `tsc --noEmit` exit 0; `authoring-ui` `tsc -b` exit 0; spec green in 32.4s; `09-widget-dropdown-names.png` renders the "Show" label + "— select widget —, Text, Image, Button, Rectangle, HintButton" listbox.

- [x] TD-013.5 — **§13 `13-overview.png` + `13-hotspot-editor.png` — Simulation Editor overlay captures ✅ (2026-04-29).** **Both PNGs emitted clean by the campaign run; no `-fullpage.png` fallback triggered.** Pure real-UI flow, zero store seeding (owner principle from TD-014 inherited end-to-end). Implementation: (1) **New shared helper module** `e2e/utils/simulation.ts` with `triggerSimDblClick`, `openSimEditor(page, name?)`, `addAndUploadStep(page, description, instruction)`, `drawHotspotOnSelectedStep(page, stepIndex)` + `SIM_FIXTURE_IMG` constant. Single source of truth — `simulation-editor.spec.ts` (TD-014.22+.35) refactored to import from this module instead of redefining the helpers locally (-82/+8 LOC in the spec; behaviour preserved). (2) **Two new `data-testid` anchors** in `SimulationEditor.tsx`: `sim-editor-overlay` on the overlay root + `sim-canvas-area` wrapping the Konva stage container. Pure additive — no rendering or styling change; gives `captureElement` deterministic targets so the captures don't depend on `boundingBox` math fallbacks. (3) **§13 capture block** appended to `docs-screenshots.spec.ts` after the existing `13-block-placeholder` shot: dblclick on the placed widget → wait on `sim-add-step-btn` visible (overlay-mounted signal) → `addAndUploadStep` ×2 with sample copy ("Open the login screen and click Sign in" / "Enter your password and submit") → click `sim-step-item-0` to put step 1 back into focus (since `addStep` selects the latest) → `captureElement('[data-testid="sim-editor-overlay"]', '13-overview.png', padding: 0)` → `drawHotspotOnSelectedStep(0)` (Konva drag with the 0.15/0.85 inset gesture, polls `__simStore` read-only for commit landing) → `captureElement('[data-testid="sim-canvas-area"]', '13-hotspot-editor.png', padding: 0)` → click Cancel → assert `sim-add-step-btn` hidden (overlay closed signal). Try/catch wraps the whole block; on any regression it falls back to two `captureFullPage` safety nets and warns to console — the campaign keeps running, author can crop manually. The `__simStore` window exposure (TD-014.29) is used only as read-only verification for the hotspot-commit poll, never for seeding. **Verification**: `pnpm verify:lint` 0 errors (2 pre-existing TD-004 warnings unchanged); `pnpm --filter @elearn-studio/e2e exec tsc --noEmit` exit 0; `pnpm --filter @elearn-studio/authoring-ui exec tsc -b` exit 0; authoring-ui vitest **1039/1039** (testids are additive); owner-side `pnpm --filter @elearn-studio/e2e docs:screenshots` end-to-end run → both PNGs emitted clean. **Files changed**: `e2e/utils/simulation.ts` (NEW), `e2e/tests/docs-screenshots.spec.ts`, `e2e/tests/simulation-editor.spec.ts`, `packages/authoring-ui/src/components/simulation/SimulationEditor.tsx`. Add a capture block to `e2e/tests/docs-screenshots.spec.ts`. Reuse the helpers established in `e2e/tests/simulation-editor.spec.ts` (TD-014.22 / .35) — do NOT redefine them; either factor them into `e2e/utils/simulation.ts` (preferred) or import directly from the spec file. Concrete recipe:
  1. **Place the widget**: `addBlockById(page, 'screenshot-sim', 'SimUnderTest')` (existing helper in `e2e/utils/screenshot.ts`) — returns the GrapesJS component id.
  2. **Open the overlay**: dispatch the GrapesJS view's `onDblClick` via `page.evaluate` (see `triggerDblClick` in `simulation-editor.spec.ts:23-32`). Wait on `page.getByTestId('sim-add-step-btn')` for visibility — that's the deterministic "overlay mounted" signal (`SimulationEditor.tsx:278`).
  3. **Add ≥2 steps via real UI** (no `simStore.setSteps` / `simStore.addStep` from the spec): for each step, `getByTestId('sim-add-step-btn').click()` → `page.locator('input[type="file"]').setInputFiles(FIXTURE_IMG)` (the only `<input type=file>` is inside StepForm; `e2e/fixtures/images/test.png` is the canonical fixture) → `getByTestId('step-description-input').fill('Step N')` → `page.locator('textarea').first().fill('…instruction…')`. Asserts `sim-step-item-0` / `sim-step-item-1` are visible.
  4. **`13-overview.png`**: with two steps populated and the default selection on the latest, run `captureElement(page, '[data-testid="sim-editor-overlay"]', { filename: '13-overview.png', padding: 0 })` if such testid exists — otherwise add `data-testid="sim-editor-overlay"` to the overlay root in `SimulationEditor.tsx` as part of this subtask (preferred — keeps the capture stable). Three-column layout (step list left, Konva canvas centre, step form right) is the default; no action needed beyond having ≥1 step populated.
  5. **Draw hotspot on step 0** (for `13-hotspot-editor`): `getByTestId('sim-step-item-0').click()` to focus step 1; locate the Konva stage with `page.locator('.konvajs-content').first()` (Konva renders no testid surface; class is the only stable selector and the editor mounts exactly one stage); `boundingBox()` → drag from `(box.x + 0.15·w, box.y + 0.15·h)` to `(box.x + 0.85·w, box.y + 0.85·h)` via `mouse.move/down/move({steps:10})/up`. Poll on `__simStore.getState().config?.steps[0].hotspot.width > 100` to absorb React-commit lag. The 0.15/0.85 inset keeps both corners away from `rectFromPoints`'s `clampPoint` borders so neither axis gets squashed; CANVAS_W/H = 640×360 → committed rect ≈ 448×252 (well above the > 100 / > 50 thresholds). DPR drift is absorbed by the range bounds, not exact equality. _(Note: `__simStore` is read-only here — used as a verification surface for the polled commit, not for seeding.)_
  6. **`13-hotspot-editor.png`**: with the hotspot just drawn on step 0 and the form panel open, capture the centre column. Either add `data-testid="sim-canvas-area"` wrapping the Konva stage container in `SimulationEditor.tsx` (preferred — same belt-and-braces as step 4) and `captureElement(page, '[data-testid="sim-canvas-area"]', …)`, OR use `.konvajs-content` boundingBox + `page.screenshot({ clip: ... })` if the testid path is rejected. The hotspot rectangle overlay is rendered by HotspotCanvas as a Konva `Rect` and is visible the moment `width > 0`.
  7. **Close cleanly**: `page.getByRole('button', { name: /^Cancel$/ }).click()` (or Save & Close — both are app-chrome buttons in the overlay header per TD-014.24; Cancel is preferred so the campaign doesn't side-effect a real PATCH). Wait on `sim-add-step-btn` to be hidden as the "overlay closed" signal so subsequent campaign captures restart on the main canvas without leakage.
  **Why this is now feasible without store seeding (TD-014.28 confirmation)**: every step above is exercised in production code paths today — `+ Add step` button (`SimulationEditor.tsx:278`), Upload (`StepForm.tsx:84`), Description input (`StepForm.tsx:119`), Konva draw-mode (HotspotCanvas zero-size sentinel), and the dblclick → openPanel chain (`registerSimBlock.ts:70-94` after the `model.getId()` fix in TD-014.27.e). The `simulation-editor.spec.ts` E2E covers the same flow end-to-end (167/167 green during TD-014.27.e); TD-013.5 inherits this contract. The original "seed via direct store mutation" guidance is **rejected** — it would bypass the very UI surface that user-manual §13 documents, defeating the purpose of the screenshot.
  **Owner principle inherited from TD-014**: *"todas las funcionalidades dadas por operativas tienen que poder usarse y reproducirse"* — captures must exercise the real authoring surface, not a back-door.

- [x] TD-013.5b — **§08 `08-scoring-section.png` — dual strategy: testid + Python post-crop** ✅ (2026-07-17). **(a)** `data-testid="scoring-section"` added to the `ScoringFeedbackForm` section root in `QuestionPropertiesPanel.tsx` (shared by MC/TF/Fill — one panel renders at a time). **(b)** Spec block now captures the `-fullpage` net unconditionally FIRST (fresh same-render source so the Python fallback's mtime idempotence works), then the primary `captureElement('[data-testid="scoring-section"]', { padding: 12 })` with warn-only catch. **Side-fix**: `captureElement`'s padding path never scrolled the element into view (docstring promised it; `page.screenshot` clips viewport-relative coords) — added `scrollIntoViewIfNeeded()` before the bbox read in `e2e/utils/screenshot.ts`. **(c)** NEW `scripts/screenshots-crop.json` with crop rect `{x:1029, y:404, w:251, h:169}` = live element bbox (1041,416,227×145) − 12px padding, clamped at the 1280px right edge — fallback output pixel-equivalent to the primary capture (validated by cropping the fresh fullpage and comparing visually). **Verification**: authoring-ui vitest 1039/1039; authoring-ui `tsc -b --force` + e2e `tsc --noEmit` exit 0; lint 0 errors (2 pre-existing TD-004 warnings); E2E `question-widget.spec.ts` + `authoring-ui-layer.spec.ts` **52/52**; campaign run → `08-scoring-section.png` emitted via the `(element)` path, visually verified (SCORING title + Points/Attempts + hint + Required checkbox, 251×169). _Original scope follows:_ (owner decision D-3, 2026-04-23). **(a) Primary — testid**: add `data-testid="scoring-section"` (or equivalent) to the Scoring heading/fieldset root inside `QuestionPropertiesPanel.tsx`, replace the current `captureFullPage(page, '08-scoring-section.png')` at spec line 379 with `captureElement(page, '[data-testid="scoring-section"]', { filename: '08-scoring-section.png', padding: 12 })`. **(b) Fallback — Python post-crop**: add a `08-scoring-section` entry to `scripts/screenshots-crop.json` (source: `08-scoring-section-fullpage.png`, deterministic crop rect). Tool runs after the spec; if the testid path succeeded, the Python tool skips (idempotent — checks file mtime vs source). If the testid path failed or the element didn't render, the Python fallback produces the same filename from the `-fullpage` safety net. Single source-of-truth filename, two redundant paths. This is the canonical pattern for every "description-anchored" capture (no stable locator, coord math safer) and justifies the Python tool's existence for future placeholders of the same shape.

- [x] TD-013.5c — **Recompose campaign captures — worked-example course + §02/§11/§01** ✅ (2026-07-18; owner-approved option 1 full scope; baseline defects documented in the 2026-07-17 investigation → `docs/issues/issues-TD-013.md`). **Delivered:** §17 now builds the real "Capitals of Europe" course (New Course dialog → 5 renamed slides → widgets at explicit coordinates → MC fully configured via real Props inputs → actions wired live per-slide immediately before each capture, since sequences don't survive slide switches — GAP-02 observed, filed) and captures the 5 finals at 1600×900 (canvas + sidebar unclipped); `02-first-slide` captured at the genuine 1-slide just-created state; `11-recipe-attempts` params filled (no warning banners); `01` callouts 5–9 below the tab strip. **5 product/manual findings filed in issues-TD-013.md** (no slide-level actions path vs manual §17; action-sequence loss on slide switch; nav-buttons broken editor render; done-button label edit not reflected; name-trait stripping) + the unexplained `select(null)`-evaluate context-destruction (Escape workaround + retry helper + permanent nav/console/pageerror diagnostics in the spec). **Verification:** e2e `tsc --noEmit` 0; lint 0 errors; campaign green ×4 consecutive runs (~40s each), 56 PNGs; visual pass on all 8 recomposed shots. _Original scope follows:_ **(a)** §17 builds the real "Capitals of Europe" worked example per `17-worked-example.md`: fresh course via the New Course dialog, 5 slides renamed Intro/Theory/Question/Branching/Final, named widgets at explicit per-widget coordinates (no stacked centring), MC configured through the real Props-panel inputs, actions wired through the real ActionsEditor palette (Navigate, Score Question, enterSlide → Score Quiz + If/Else with Display Message + Hide in Else, Send to LMS), per-placeholder sidebar states, and a widened 1600×900 viewport for the 5 finals so the 1024×768 canvas + right sidebar fit unclipped. **(b)** `02-first-slide.png` captured inside (a) right after course creation — the only moment the UI genuinely shows a just-created course with one empty slide. **(c)** `11-recipe-attempts.png` fills every required action param (variable name, expression, widget id) before capturing so no validation warning banners appear. **(d)** `01-full-ui-annotated.png` callouts 5–9 repositioned below the tab strip (`addCallouts` offset made per-axis optional) so they point at the tabs instead of covering them.
- [x] TD-013.6 — **Post-processing tool: `scripts/crop-screenshots.py`** ✅ (2026-07-18). Python + Pillow per spec, plus **mtime idempotence** (skips any entry whose final PNG is at least as new as its `-fullpage` source — the primary testid capture from the same run wins; the tool only produces the file when the primary path failed) and **`_`-prefixed config keys ignored** (comments). Callout rendering mirrors `addCallouts()` styling (32px circle, #f9e2af/#1e1e2e + ring) so Playwright and Python callouts are indistinguishable. **NEW `scripts/run-crop.cjs`** — cross-platform launcher that probes interpreters for Pillow (`py -3` → `python` → `python3` on Windows; `python3` → `python` elsewhere) and propagates the tool's exit code verbatim; needed because this box's `.venv` python lacks Pillow (`pip install` hangs persistently — suspected AV interference, §4.1 pattern; system `py -3` has Pillow 12.2.0). **Chained**: `e2e` `docs:screenshots` now runs the crop as a post-step; standalone via `docs:crop`. **Verification (all paths exercised)**: idempotent skip exit 0; forced crop (target deleted) exit 0 with output pixel-equivalent to the primary capture (visual compare); missing source exit 1; invalid entry exit 1; malformed JSON exit 2; full chained campaign green end-to-end (`skipped 08-scoring-section.png` as post-step). Lint 0 errors; e2e tsc 0. _Original scope follows:_ Python + Pillow. Reads a JSON config `scripts/screenshots-crop.json` mapping `<filename>` → `{ source: "<fullpage-filename>", crop: {x, y, width, height}, callouts?: [{number, x, y}], padding?: N }`. For each entry: opens `<source>` (e.g. `09-widget-dropdown-names-fullpage.png`), applies the crop rectangle + optional padding + optional numbered callouts, writes the result to `<filename>`. Serves as a fallback for any placeholder where a deterministic pixel-space crop is simpler than live scripting, and as the callouts-overlay path for `01-full-ui-annotated.png` if the inline Playwright `addCallouts` approach proves flaky. Exit non-zero on missing source or invalid config. Runnable standalone (`python scripts/crop-screenshots.py`) AND chainable from the spec (`pnpm --filter @elearn-studio/e2e docs:screenshots` could invoke it as a post-step via `playwright test`'s `globalTeardown` or a dedicated script in `e2e/package.json`).

- [x] TD-013.7 — **Playbook update** ✅ (2026-07-18). T-13 (callouts overlay + per-axis offset), T-14 (New Course dialog + genuine first-slide via §17), T-15 (Zustand direct-sync — shipped design, not the reload approach the original scope guessed), T-16 (Sim Editor real-UI overlay flow), T-17 (Python post-crop: dual strategy + mtime idempotence + interpreter launcher) and T-18 (worked-example build: 0-slide Blank-course gotcha, explicit coords, live per-capture action wiring, nested-param selectors, 1600×900 finals, known product quirks TD-015/016/017/018) added to `10-docs-screenshots-playbook.md`. **T-6 superseded in place** (Escape deselect replaces the retired `select(null)` evaluate — destroyed-context evidence pointer + TD-020). **T-12 snippet corrected** to the shipped `role="group"` detection (the playbook still showed the pre-TD-013.4 `role="tab"` code that never matched). `## Deferred placeholders` retired → `## Historical deferrals — now automated` (8-row table mapping each old deferral to its closing subtask + technique). Run instructions + expected-output rewritten for the chained crop step (55/55 finals; `08-scoring-section-fullpage.png` as the only by-design net; any other net = regression signal; warning not to touch the headed window mid-run). Spec header comment updated to match. Verification: e2e tsc 0; lint 0 errors. _Original scope follows:_ add techniques T-13 (callouts overlay), T-14 (dashboard navigation for §02), T-15 (reload-for-Zustand-sync), T-16 (Sim Editor overlay + session seeding), T-17 (Python post-crop tool) to `docs/developer-guide/10-docs-screenshots-playbook.md`. Retire the `## Deferred placeholders` section (move to `## Historical deferrals — now automated` with pointers to the new T-N entries). Update the spec header comment to reflect the new techniques.

- [x] TD-013.8 — **Verification** ✅ (2026-07-18). Chained campaign end-to-end green: **55/55 placeholders emitted and fresh** (inventory script cross-checked every `<!-- screenshot: -->` comment against the folder — zero missing, zero orphans, zero stale); post-crop skipped 08 correctly (primary won). **Visual pass over all 55**: 11 verified by hand during .5b/.5c + 44 delegated to a vision agent against each placeholder's description. Outcome: 40 OK; 2 accepted MINORs (04-image-props shows no sample image — needs an upload flow, panel content correct; 09-widget-dropdown-names lists the real slide widgets); **2 pre-existing defects caught and FIXED**: (a) `13-block-placeholder` — widget half-hidden under the right aside with the sidebar composited into the element shot (present in the committed baseline too); now placed at (170,140) in the visible canvas and clipped to the iframe's visible region; (b) `14-processflow-builder` — spec captured an empty builder skeleton while the manual placeholder demands "3 nodes and 2 edges configured"; now seeds the canonical `PROCESS_FLOW_SCENE` (shared const, deduplicated with 14-json-example). Both re-captured and visually confirmed. **Net cleanup**: 15 stale historical `-fullpage` files (April runs) + the 5 §17 nets removed — only `08-scoring-section-fullpage.png` remains, by design. No pre-existing capture regressed (the two changed ones were baseline defects). e2e tsc 0; lint 0 errors.

- [x] TD-013.9 — **Refine the generated code** ✅ (2026-07-18; owner-approved candidates 1+2+3+4, decisions 5+6 documented). (1) §17 build primitives extracted to **NEW `e2e/utils/worked-example.ts`** (`retryOnDestroyedContext`, `placeAt`, `setTextContent`, `idByName`, `EUROPE_MAP_DATA_URI`) — build orchestration stays in the spec (it IS the campaign narrative T-18 maps); spec 1763 → 1610 lines. (2+3) **NEW `e2e/utils/actions.ts`** — `ensureEvent(page, label)` + `insertActionFromPalette(page, label)` replace three near-identical spec-local helpers (§09 `ensureClickEvent`, §11 `insertFromPalette`, §17 `ensureEvent`); the §09 variant's defensive menu-close now protects every event. (4) `PANEL_NEUTRALISE_RULES` + `ASIDE_NEUTRALISE_RULES` constants in `screenshot.ts` deduplicate the T-2 CSS token string previously inlined at 3 sites (two tiers kept deliberately — aside must not get height/flex resets). (5) Explicit decision: NO further spec split beyond (1) — a linear campaign fragmentado por secciones rompería el mapeo §-a-§ del playbook; size exception documented here. (6) Explicit decision: §17 phase logs + nav/console/pageerror diagnostic listeners stay (they made the TD-020 mystery diagnosable). **Verification**: e2e tsc 0; lint 0 errors; full chained campaign green post-refactor (56 writes, no degraded/retry lines); control shots `10-action-palette` + `17-slide-4-final` visually identical to the .8-verified versions.

- [x] TD-013.10 — **Close block + retire legacy tooling** ✅ (2026-07-18; owner decision D-2, 2026-04-23). Legacy retired: `docs/scripts/capture-screenshots.ts` + `capture-moodle-screenshot.ts` deleted (git rm), `docs/package.json` `capture` script + orphaned devDeps (`@playwright/test`, `tsx`, `typescript`) removed with lockfile regen, `docs/assets/screenshots/` flagged legacy via README (folder kept only while the root README embeds two v1 images), `user-manual-v2-scope.md` capture-workflow section rewritten for the automated pipeline. Closure artefacts: `issues-TD-013.md` self-review appended; `CHANGELOG.md` **v0.5.67** entry (note: the original scope line said v0.5.66 — that version shipped with TD-014 on 2026-04-26, so this block takes the next patch); WORKING_CONTEXT sections updated; all subtasks ticked. Commit + push + CI per owner authorization 2026-07-18.
  **Legacy retirement in this same block:**
  - Delete `docs/scripts/capture-screenshots.ts` — v1 campaign generator (9 hardcoded captures to `docs/assets/screenshots/` with v1 naming `01-dashboard.png`, etc.), last run 2026-04-01, superseded by `e2e/tests/docs-screenshots.spec.ts` (55 placeholders, v2 naming).
  - Delete `docs/scripts/capture-moodle-screenshot.ts` — produced `18-moodle-course.png` for the v1 baseline; the current §16 block in the spec covers Moodle with the same login + SCORM-settings navigation.
  - Delete the now-empty `docs/scripts/` folder.
  - Mark `docs/assets/screenshots/` as legacy (no longer source-of-truth) in the playbook §Historical references note. Keep the folder in git — the 19 baseline PNGs are a snapshot of the 2026-04-01 UI and remain valuable as an "UI has evolved since" visual diff source.
  - Retire the "temporary pending-capture policy" paragraph in `docs/user-manual-v2-scope.md:103` — TD-013 is the end of the transitional period; every placeholder in `docs/user-guide/*.md` now maps to an automatically-produced PNG.
  - Update `docs/user-manual-v2-scope.md:101` ("consistent SVG overlay pattern — TBD") to reference the Python tool delivered by TD-013.6.

**Additional items discovered during pre-block sweep (2026-04-23, integrated above):**
- `08-scoring-section.png` was being produced by `captureFullPage` + a one-off manual crop (commit `2e14f5e`). Integrated as TD-013.5b (dual testid + Python strategy).
- Two legacy standalone scripts in `docs/scripts/` (`capture-screenshots.ts`, `capture-moodle-screenshot.ts`) predate the E2E-spec approach and are obsolete. Retired in TD-013.10.
- `docs/user-manual-v2-scope.md` has two lines flagging screenshot workflow as temporary/TBD (L101 SVG overlay TBD, L103 pending-capture policy). Both resolved in TD-013.10.
- `§01 callouts` count: scope doc says 9 (4 areas + 5 tabs), `01-welcome.md:11` said 4. Owner chose 9; spec + caption updated in TD-013.1.

**Non-scope / explicitly rejected alternatives:**
- **Copying `docs/assets/screenshots/*.png` into `docs/user-guide/assets/screenshots/`** — the user baseline from 2026-04-01 is a historical reference, not a sustainable source (the UI has evolved; those captures will drift). TD-013 produces every asset fresh from the current UI each run.
- **Headless screenshot-editor GUI** — out of scope. Python + Pillow + JSON config is enough for deterministic post-processing.
- **Regenerating the April 2026 manual captures in `docs/assets/screenshots/`** — that folder predates the User Manual v2 placeholder naming convention. Not touched; lives as legacy per git blame.

**Dependencies / risk notes:**
- TD-013.5 (Sim Editor) depends on the `simStore` being exposed on `window` or on the `/simulations/:id/steps` API accepting seeded data. If neither path is available, TD-013.5 becomes its own sub-investigation. Reported at start of TD-013.5 execution. **Outcome (2026-04-24):** sub-investigation ran; see pause note at the top of TD-013 and the new block TD-014. TD-013.5 will resume as a pure real-UI capture once TD-014 closes (no store seeding needed).
- TD-013.6 (Python tool) assumes Pillow is available on the dev machine. Permission to install already granted in `.claude/settings.local.json`. If Pillow is missing, the tool installs it via `python -m pip install --user Pillow` at first run.

---

### [x] TD-014 — Screenshot Simulation Recorder UI: build missing authoring surface — ✅ BLOCK CLOSED (2026-04-26 functional / 2026-05-04 administrative, v0.5.66; header flipped retroactively 2026-07-20 — the .27 close never ticked this line)

> **Source:** TD-013.5 pre-block investigation (2026-04-24) | **Status:** ✅ Closed (see TD-014.27) | **Blocked:** TD-013.5…, .10 (resumed and since closed) | **Shipped:** v0.5.66 (`c151f27` + fix-forwards)

**Baseline of symptoms:** the full pre-fix audit that triggered this block — 30 rows, 15 gaps, 2 partials, 4 informational findings, with the user-observable symptom + technical cause + impact + planned fix per row — is captured in `docs/AUDIT_BASELINE_SYMPTOMS.md` (2026-04-24, frozen snapshot at the moment TD-014 was scoped). Read that file first when revisiting this block to remember **why** each subtask exists. The decisions and the resulting changes are documented in the per-subtask closure notes below; the baseline file is the *origin* document that gives them context.

**Context:** The §13 Software Walkthrough chapter of `docs/user-guide/` documents a full authoring workflow: **Add step**, **Upload screenshot** or **pick from Asset Library**, **Draw a rectangular hotspot**, **Record** from a real application, **Import recording**. The backend infrastructure for every one of these is already in place and tested:

- `packages/simulation-engine/src/routes/recorder.ts` — `POST /recorder/start|capture|stop`, `GET /recorder/sessions[/:id][/screenshot]` (T023, 2026-02-xx)
- `backend/api/src/routes/courses.ts` — `POST /courses/:id/simulations/import` (T024.3)
- `packages/authoring-ui/src/api/courseApi.ts:196` — `importSimulation(courseId, sessionId)` client function

**Gap:** nothing in `packages/authoring-ui/src/components` calls any of those. `SimulationEditor.tsx` mounts the three-column overlay but has no entry points to: create a step, upload a screenshot, select an asset, draw a hotspot, start a recorder session, or import an existing recording. `HotspotCanvas.tsx` can drag/resize an existing `Rect` but cannot draw a new one. `StepForm.tsx` edits only textual + numeric fields of a step that must already exist with its screenshot URL set. Net effect: a user who opens the Software Walkthrough editor today sees "No steps yet" with no way to add any.

The contract of T024.10 (`Reorder / add / delete steps`) was partially shipped — reorder (↑↓) + delete (✕) were implemented, but `add` was never built. This block closes that gap completely and adds the recorder lifecycle UI that the user manual promises.

**Owner principle (2026-04-24):** *"todas las funcionalidades dadas por operativas tienen que poder usarse y reproducirse"* — if the documentation treats a capability as current, the full path must be reachable from the UI. Documentation-vs-implementation drift is treated as an implementation gap, not a documentation gap.

**Out of scope (already shipped and operational — do not touch):**
- Backend recorder lifecycle routes (T023) — start/capture/stop/list/get/live-screenshot.
- Backend simulation import route (T024.3) — `POST /courses/:id/simulations/import` converts RawSimStep[] → AuthoredSimStep[].
- Runtime player simulation execution (T025) — three modes, hotspot hit-test, tolerance, max-attempts, hint/feedback, SCORM score.
- Playwright CDP capture engine (T023.8–11) — click / keyboard / select capture + auto-naming.
- Konva drag + resize of an existing hotspot `Rect` (T024.6).
- New simulation capabilities beyond what user-manual §13 documents (hover/type interaction types already shipped in T202; no new types introduced here).

**Subtasks:**

#### (A) Missing manual-authoring UI — add step, upload screenshot, draw hotspot

- [x] TD-014.1 — Gap-matrix audit ✅ (2026-04-24) — `docs/issues/issues-TD-014.md`. 30 rows audited → 15 gaps, 13 implemented, 2 partial. Five amendments surfaced: **R-01** (MEDIUM) collapse `AssetPickerModal` into `editor.AssetManager.open()` reuse (canonical pattern from `ButtonPropertiesPanel.tsx:90-107`); **R-02** (HIGH) `simStore.setConfig(config)` missing — blocks Import flow TD-014.12; **R-03** (HIGH) simulation-engine `index.ts` has no CORS middleware — blocks every browser → :3002 call; **R-04** (LOW) no `DELETE /recorder/sessions/:id` — needed by E2E cleanup in TD-014.23; **R-05** (LOW) `sessionId: ''` on manual-scaffold sims is semantically wrong — generate `'manual-' + nanoid(8)` in `registerSimBlock.ts:82`. Proposed amendments to .2/.5/.12/.25 and two new subtasks .8a (CORS) + .8b (DELETE endpoint) documented in the audit § "Proposed amendments to tasks.md". Awaits owner confirmation before applying.

- [x] TD-014.2 — `simStore.ts` ✅ (2026-04-24) — two new actions. **(a) `addStep(overrides?: Partial<AuthoredSimStep>): void`** — defaults: zero-size hotspot (`{ x: 0, y: 0, width: 0, height: 0, tolerance: 12 }` — sentinel for draw-mode), `interactionType: 'click'`, `demoDelay: 3000`, `maxAttempts: -1`, `order: config.steps.length`, `id: step-${crypto.randomUUID().slice(0, 8)}` (uses the repo-canonical pattern already in `Toast.tsx:43` + `QuestionPropertiesPanel.tsx:208` — zero extra dep vs. the original nanoid proposal); sets `selectedStepIndex` to the new step's index; no-op when `config === null`. **(b) `setConfig(config: SimConfig): void`** — replaces `config` while preserving `panelOpen` + `editingComponentId`, clamps `selectedStepIndex` to `Math.min(selectedStepIndex, max(0, steps.length - 1))`; empty-steps case resets to 0. TDD: 12 tests written RED (all `addStep/setConfig is not a function`), implementation landed, 25/25 simStore tests pass (13 pre-existing + 12 new). **Verification**: `tsc -b packages/authoring-ui` exit 0; **authoring-ui vitest 889 → 901/901 pass** (+12, across 35 files); eslint clean on `src/store/simStore.ts` + `src/__tests__/simStore.test.ts`. TD-014.14 coverage contract now fully satisfied — will close as "pre-satisfied by TD-014.2" when sequence reaches it. Audit R-02 (2026-04-24) — implements the `setConfig` missing before the Import flow (TD-014.12) could wire up.

- [x] TD-014.2b — **Remove `SimConfig.sessionId`** ✅ (2026-04-24, audit Option E). Dead-code removal across 3 packages. **Sites edited (9):** `backend/api/src/types/simulation.ts:57`, `packages/authoring-ui/src/types/simulation.ts:45`, `packages/runtime-player/src/sim/simPlayer.ts:49` (interface declarations); `backend/api/src/routes/simulations.ts:162` (strip `sessionId,` from `simConfig` return); `packages/authoring-ui/src/editor/registerSimBlock.ts:82` (drop `sessionId: ''` from empty scaffold); `backend/api/src/lib/swagger.ts:131,133` (remove from OpenAPI `required` + `properties`); `backend/api/src/__tests__/simulations.test.ts:139` (drop `expect(config.sessionId).toBe('sess-abc')`); `packages/authoring-ui/src/__tests__/simStore.test.ts:11` and `packages/runtime-player/src/__tests__/sim-player.test.ts:29` (drop `sessionId: 'sess-test'` from `makeConfig` fixtures). **Verification:** `grep "config\.sessionId\|simConfig\.sessionId"` → 0 matches in code (only docs describing the change); `grep "sessionId" packages/runtime-player/src` → 0 matches; `tsc -b packages/authoring-ui packages/runtime-player backend/api` → exit 0; **authoring-ui vitest 889/889 ✅** (35 files), **runtime-player vitest 265/265 ✅** (11 files), **backend vitest 148/148 ✅** (9 files — re-run with Docker/Mongo up after initial blocked run). 0 runtime-behaviour change. Recorder-side `sessionId` (S3 path key, recorder route parameters, `importSimulation(courseId, sessionId)` client signature) untouched and verified still live.

- [x] TD-014.3 — `SimulationEditor.tsx` ✅ (2026-04-24) — **+ Add step** button as sticky footer of the left step-list column. Step-list column refactored from single-scroll container to three-section flex-column: static header (`Steps`), `stepListScrollable` (flex:1, overflowY:auto), and `stepListFooter` (flexShrink:0, border-top) — button is outside the scrollable region so it stays pinned regardless of list length. Wired via `handleAddStep()` → `useSimStore.getState().addStep()` (TD-014.2). Disabled state reads `useEditorStore(s => s.isSaving)`; comment in source explicitly points at TD-014.9 as the place to extend the check to `recorderStore.isBusy` when that store lands. `data-testid="sim-add-step-btn"` added for E2E (TD-014.22) hooks. **Tests**: new file `src/__tests__/simulation/SimulationEditor.test.tsx` with 5 tests — renders, click appends, disabled when isSaving=true, enabled when isSaving=false, present regardless of step count (sticky-footer contract). `react-konva` + `konva` mocked inline per the "no konva in unit tests" convention documented in `propsEmptyState.tsx` comment (TD-010). **Verification**: RED→GREEN cycle clean (5 fail → 5 pass); `tsc -b packages/authoring-ui` exit 0; **authoring-ui vitest 901 → 906/906** (+5, 36 files); ESLint clean on both files.

- [x] TD-014.4 — `StepForm.tsx` ✅ (2026-04-24) — new **Step screenshot** field group at the top of the form. **(a) Upload…**: hidden `<input type="file" accept="image/png,image/jpeg">` triggered by visible button; on file selection, async flow `uploadAsset(file)` → `resolveAssetUrl(objectName)` → `onChange({ screenshotKey: objectName, screenshotUrl: presignedUrl })`. Button text switches to "Uploading…" + disabled while pending; errors surfaced via `useToast().error()` with the thrown message; input is reset after each selection so re-picking the same file still fires. **(b) Asset Library**: `editor.AssetManager.open({ types:['image'], select(asset, complete) { onChange({ screenshotUrl: asset.getSrc() }); if (complete) editor.AssetManager.close() } })` — reuses the canonical pattern from `ButtonPropertiesPanel.tsx:90-107` per audit R-01; disabled when `editor` is null or an upload is in flight. Empty-src guard short-circuits the patch. Current-asset hint (`Current: <key or url>`) rendered below the buttons when `step.screenshotUrl` is set. `editor` read via `useEditorStore(s => s.editor)` directly inside StepForm — matches the codebase pattern for property panels. **Audit I-04 deferral**: library path does not set `screenshotKey` because GrapesJS AM exposes only the src URL; backend consumption tolerates empty screenshotKey by falling back to screenshotUrl. Fix to `SimulationEditor.test.tsx` collateral: tests wrapped in `<ToastProvider>` via new `renderEditor()` helper — surfaced by the full-suite run (2 SimulationEditor tests regressed after TD-014.4 added `useToast()` to `StepForm`, fixed atomically in the same change). **Tests**: new `src/__tests__/simulation/StepForm.test.tsx` with **10 tests** — buttons render, Upload triggers hidden input click, file → onChange with both keys, upload failure leaves onChange untouched, Upload disabled while pending, Asset Library opens AM with `types:['image']`, select callback patches screenshotUrl + closes AM on complete=true, select with complete=false leaves AM open, Asset Library disabled when editor null, empty src no-op. **Verification**: RED→GREEN 10/10 → 10/10; `tsc -b packages/authoring-ui` exit 0; **authoring-ui vitest 906 → 916/916** (+10, 37 files); ESLint clean on `StepForm.tsx` + `StepForm.test.tsx` + `SimulationEditor.test.tsx`.

- [-] TD-014.5 — ~~New component `AssetPickerModal.tsx`~~ **Cancelled by audit R-01 (2026-04-24).** Merged into TD-014.4 — the Asset Library button reuses the canonical `editor.AssetManager.open()` pattern from `ButtonPropertiesPanel.tsx:90-107`. A dedicated modal would duplicate GrapesJS's existing browse+upload UI for zero functional gain and establish a divergent asset-picking pattern. No code to write; GrapesJS AM handles both `image` MIME filtering (`types: ['image']`) and the select/close lifecycle. Decision recorded in `docs/issues/issues-TD-014.md` § R-01.

- [x] TD-014.6 — `HotspotCanvas.tsx` draw-mode ✅ (2026-04-24). **Architecture**: extracted pure coordinate maths + validation to `src/components/simulation/hotspotDraw.ts` (`isDrawModeHotspot`, `clampPoint`, `rectFromPoints`, `isValidHotspotSize`, `MIN_HOTSPOT_SIZE=10`, `Point`) — React/Konva-free, exhaustively unit-tested. Keeps HotspotCanvas integration thin and sidesteps jsdom's lack of native `canvas` (Konva's `getPointerPosition()` can't be faithfully simulated under vitest). **Behaviour**: when `isDrawModeHotspot(step.hotspot)` (zero-size sentinel from TD-014.2 `addStep`), HotspotCanvas suppresses the existing Rect + Transformer and wires `onMouseDown/Move/Up` on the Stage; drawStart + drawEnd held in React state; transient preview rect (dashed, 0.15 alpha fill) rendered during the drag, `listening={false}` so it doesn't intercept further events. mouseUp → `rectFromPoints(start, end, 640, 360)` with both corners clamped → if `isValidHotspotSize(width, height)` commits via `onChange({ ...step.hotspot, x, y, width, height })`; below MIN_HOTSPOT_SIZE the gesture is treated as a misclick and the hotspot stays in the zero-size sentinel. Edit-mode unchanged (Rect draggable + Transformer resize, existing handleDragEnd / handleTransformEnd preserved). Transformer `useEffect` skipped in draw-mode so `trRef.current` is never touched when Transformer doesn't render. **Canvas hint** in `SimulationEditor.tsx` switches via `isDrawModeHotspot(step.hotspot)` — draw-mode "Drag on the screenshot to draw a hotspot"; edit-mode "Drag the blue box to set the target hotspot". **Tests**: new `hotspotDraw.test.ts` (21 tests) covers every branch of the four helpers including rounding, boundary clamping, point-order independence, MIN_HOTSPOT_SIZE inclusive check, misclick sentinel. New `HotspotCanvas.test.tsx` (6 tests) mocks `react-konva` + `konva` and verifies the render-mode switch — edit-mode produces Rect + Transformer, draw-mode omits Transformer and wires Stage mouse handlers, width-only and height-only zero both count as draw-mode. Full drag gesture (mouseDown→Move→Up→onChange with the committed rect) is validated end-to-end in TD-014.22 with real `page.mouse` events — Konva `getPointerPosition()` requires a live Stage instance that jsdom cannot provide. **Verification**: RED (4 HotspotCanvas tests fail + hotspotDraw.test fails to load) → GREEN 27/27; `tsc -b packages/authoring-ui` exit 0; **authoring-ui vitest 916 → 943/943** (+27, 39 files); ESLint clean on all 5 edited/new files (hotspotDraw.ts, HotspotCanvas.tsx, SimulationEditor.tsx, hotspotDraw.test.ts, HotspotCanvas.test.tsx).

- [x] TD-014.7 — `StepForm.tsx` **Clear hotspot** ✅ (2026-04-24). New Field "Hotspot" with hint "Clear to re-draw the target rectangle" and a button `data-testid="step-clear-hotspot-btn"` placed right after the existing Hotspot tolerance field. `handleClearHotspot()` emits `onChange({ hotspot: { ...step.hotspot, width: 0, height: 0 } })` — preserves `x`, `y`, `tolerance` (author's previous config not lost; only the rectangle needs redrawing). Disabled when `isDrawModeHotspot(step.hotspot)` (already zero-size, or partial-zero sentinel like width>0/height=0) — reuses the helper from `hotspotDraw.ts` (TD-014.6) so the draw-mode definition stays in one place. Button style reuses the existing `screenshotBtn` + `screenshotBtnDisabled` tokens from TD-014.4 for visual consistency. **Tests**: +5 in `StepForm.test.tsx` — renders, click emits the zero-size patch with tolerance preserved, disabled when already zero-size, enabled in edit-mode, disabled in partial sentinel (height=0 only). **Verification**: RED 5/5 → GREEN 5/5; `tsc -b packages/authoring-ui` exit 0; **authoring-ui vitest 943 → 948/948** (+5, 39 files); ESLint clean.

- [x] TD-014.7b — **Drag-drop reorder** in step list ✅ (2026-04-24). Step items in `SimulationEditor.tsx` now render `draggable` + `onDragStart/onDragOver/onDrop/onDragEnd`; `handleDrop` computes `adjustedDropIndex = dragIndex < dropIndex ? dropIndex - 1 : dropIndex` (matches the "insert before dropIndex" semantics used by `SlideList.tsx`) and calls the existing `simStore.reorderStep(from, adjusted)` atomically. Live drop-target highlight via new `stepItemDropTarget` style (2px top border `#89b4fa`) applied when `dropIndex === i && dragIndex !== null && dragIndex !== i`. **Keep the ↑ ↓ icon buttons** — no code removed; they remain the accessibility fallback. **Keyboard fallback**: `tabIndex={0}` on each step + `handleStepKeyDown` reorders on `Alt+ArrowUp` / `Alt+ArrowDown` (guards: no-op at index 0 / last index; modifier required — plain ArrowDown does nothing). Scrollable region gets `aria-label="Simulation steps"`. **Tests**: +11 in `SimulationEditor.test.tsx` (draggable attribute present; dragStart→dragOver→drop reorders with adjusted index; drag onto self is no-op; dragEnd without drop does not reorder; Alt+↑/↓ moves focused step; Alt+↑ at 0 + Alt+↓ at last are no-ops; ArrowDown without Alt does not reorder; ↑ ↓ icon buttons still work alongside D&D). **Verification**: RED 11/11 → GREEN 11/11; **authoring-ui vitest 948 → 959/959** (+11, 39 files); ESLint clean on both changed files. Copy update in `docs/user-guide/13-software-walkthrough.md` deferred to TD-014.25.

#### (B) Recorder lifecycle UI — launch, live preview, capture, stop, import

- [x] TD-014.8a — **simulation-engine CORS middleware** ✅ (2026-04-24). Added `cors@^2.8.5` as runtime dep + `@types/cors@^2.8.17` as devDep in `packages/simulation-engine/package.json`. New `src/middleware/cors.ts` exports `createCorsMiddleware()` factory: function-based `origin` (clearer "rejected" semantics than reflecting a non-matching static value — unmatched origins get NO `Access-Control-Allow-Origin` header); `methods: ['GET','POST','DELETE']`; `allowedHeaders: ['Content-Type','Authorization']`; `credentials: false`. Config: new `config.http.allowedOrigin` key in `src/config.ts` sourced from `SIMULATION_ENGINE_ALLOWED_ORIGIN` (default `http://localhost:3000`). Wired in `src/index.ts` **before** `express.json` (preflight OPTIONS has no JSON body to parse). Documented both env vars in `docker/.env.example` (`SIMULATION_ENGINE_ALLOWED_ORIGIN` + `VITE_SIMULATION_ENGINE_URL` — the authoring-ui consumer side). No README exists in `packages/simulation-engine/`; no new doc file created (will be covered by TD-014.25 docs sweep). **Tests**: new `src/__tests__/cors.test.ts` with 7 cases — factory returns RequestHandler; GET from allowed origin gets `Access-Control-Allow-Origin: http://localhost:3000`; GET from an unexpected origin gets NO allow-origin header (cors rejects); OPTIONS preflight returns 204 with GET/POST/DELETE in allow-methods + content-type/authorization in allow-headers; preflight from unexpected origin has no allow-origin; `Access-Control-Allow-Credentials` is NEVER set; no-Origin (curl/health) passes through. **Verification**: RED (missing middleware module) → GREEN 7/7; **simulation-engine vitest 60 → 67/67** (3 files, +7); `tsc --noEmit` exit 0. **Pod**: pnpm 10.32.1 required to match the monorepo's `virtualStoreDirMaxLength: 60` config (pnpm 9 rejects the install with `ERR_PNPM_VIRTUAL_STORE_DIR_MAX_LENGTH_DIFF`).

- [x] TD-014.8b — **`DELETE /recorder/sessions/:id` endpoint** ✅ (2026-04-24). Server side only; the paired client `recorderApi.ts::deleteSession(id)` is deferred to TD-014.8 where `recorderApi.ts` is first created. **s3.ts**: added 3 new exports — `headObject(key): Promise<boolean>` (returns false on NotFound/NoSuchKey/404, propagates other errors); `deleteObject(key): Promise<void>`; `deleteObjects(keys[])` (S3/Garage `DeleteObjectsCommand` with `Quiet:true`, auto-chunked to 1000-key batches). **recorder.ts**: new `recorderRouter.delete('/sessions/:id', ...)` — validate sessionId via existing `validateSessionId()`; `headObject('recordings/{id}/session.json')` → 404 if absent; `listObjects('recordings/{id}/')` → `deleteObjects(keys)` → `res.status(204).end()`. Network errors at either the head or delete stage map to 503 (reuses `isNetworkError()` from H-01); other failures → 500 with the generic "Failed to delete session" body. **Tests** (recorder.test.ts, +7 DELETE cases): 204 + session.json + 3 screenshots deleted together (exact deleted-keys list asserted); 204 with only session.json; 404 when head returns false (listObjects/deleteObjects NOT called — verified via `.not.toHaveBeenCalled()`); 400 for path-traversal `..%2Fevil` + 400 for disallowed-char `abc_xyz` (both blocked before any S3 call); 500 on deleteObjects failure; 503 on head network error (ECONNREFUSED). s3 mock at file top extended with `headObject`/`deleteObject`/`deleteObjects`. **Verification**: RED 7/7 → GREEN 7/7 (one iteration fixed: initial `.` sessionId test hit Express's path normalisation → used `abc_xyz` instead); **simulation-engine vitest 67 → 74/74** (3 files, +7); `tsc --noEmit` exit 0.

- [X] TD-014.8 — New file `packages/authoring-ui/src/api/recorderApi.ts` mirroring `courseApi.ts` structure. Exports: `startRecording(url, title)` → `{ sessionId, status, startedAt }`; `captureStep(sessionId)` → `{ steps }`; `stopRecording(sessionId)` → persisted `Session`; `listSessions()` → `{ sessions: SessionSummary[], total }`; `getSession(id)` → `Session`; `getLiveScreenshotUrl(id)` → pure URL string for `<img src>` (no fetch). Base URL via `VITE_SIMULATION_ENGINE_URL` env var (default `http://localhost:3002` — document in `.env.example`). Reuses `apiFetch` for Bearer injection + silent token refresh.

- [x] TD-014.9 — `recorderStore.ts` ✅ (2026-04-24). New Zustand store mirroring the TD-007 mutation pattern. **State** (5 fields): `activeSessionId: string | null`, `recording: boolean`, `captures: SimStep[]`, `error: string | null`, `isBusy: boolean`. **Actions**: `start(url, title?) → Promise<void>`, `capture() → Promise<void>`, `stop() → Promise<Session | undefined>`, `reset() → void`. Every async path funnels through `performCourseMutation()` (the pure primitive, same one `editorStore.requestCourseMutation` wraps) so error narrowing + isBusy/error transitions stay consistent with the rest of the codebase. **Key decisions**: (1) `activeSessionId` is intentionally preserved after successful `stop()` — TD-014.11 needs the id to call `importSimulation()`; `reset()` clears it. (2) `recording` flag flips to false only on successful stop — a failed stop leaves the session alive on the backend so the user can retry. (3) `captures` is replaced wholesale from the capture-response list (not appended) so out-of-order replies cannot resurrect a stale subset. (4) Concurrency guards: `start` is a no-op while `recording || isBusy` (prevents double-click races), `capture`/`stop` are no-ops without an active session — belt-and-braces even though the UI disables buttons anyway. (5) Store does NOT call `toast.error()` directly (can't — Zustand actions aren't React components); consumers decide how to render the `error` field. **Tests** (new `src/__tests__/recorderStore.test.ts`, +14): initial state (1); `start()` happy/failure/non-Error-throw narrowing/no-op-while-recording/no-op-while-busy (5); `capture()` replaces/keeps-on-error/no-op-no-session/no-op-not-recording (4); `stop()` success flips recording+keeps activeSessionId/failure keeps recording=true/no-op-no-session (3); `reset()` returns to initial (1). **Verification**: GREEN 14/14 first run; **authoring-ui vitest 971 → 985/985** (41 files, +14); `tsc --noEmit` exit 0; ESLint clean on both files. TD-014.18 coverage contract now fully satisfied — will close as "pre-satisfied by TD-014.9" when sequence reaches it.

- [x] TD-014.10 — `RecorderLauncherDialog.tsx` ✅ (2026-04-24). Modal fixed-overlay (z-index 1100) with URL + optional title inputs. **Client-side validation** extracted to new `src/lib/urlValidation.ts::validateRecordingUrl()` — line-for-line mirror of `packages/simulation-engine/src/routes/recorder.ts:44-82` so the dialog rejects exactly what the backend rejects (UX nicety, not a security boundary). `<form noValidate>` to bypass HTML5 `type="url"` native blocking (jsdom reproducibility + handler-owned error UX). **Flow**: validate → `recorderStore.start(url, title || undefined)` → on success (no store.error after await) → `onStarted()` + `onClose()`. **Error UX**: inline `<div role="alert" data-testid="recorder-dialog-error">` distinguishes clientError (set before submit) from storeError (surfaced after). Escape/click-outside close ignored while `isBusy`. Auto-focus URL field on open; state reset on every open (url/title/clientError). **Tests** (TD-014.19, new `RecorderLauncherDialog.test.tsx`, +18): 7 SSRF rejects (localhost/127.0.0.1/10.x/172.16.x/172.31.x/192.168.x/169.254.x), ftp://+malformed rejection, empty-URL required error, title-maxLength attribute, happy path calls start(url,title), omit-title sends undefined, backend error keeps dialog open, Cancel + Esc close paths, render gating on open=false. **Verification**: 17→18 GREEN after `noValidate` fix; tsc 0; ESLint clean.

- [x] TD-014.11 — `RecorderLiveView.tsx` ✅ (2026-04-24). Full-screen overlay auto-mounts when `recorderStore.recording` is true. Live JPEG `<img src={getLiveScreenshotUrl(id)}?t={tick}>` with `setInterval(setTick, 500)` cache-bust (belt-and-braces on top of server's `Cache-Control: no-store`). Three buttons: **Capture step** (→ `store.capture()`), **Stop & import** (→ `store.stop()` → success → `courseApi.importSimulation(course.id, session.id)` → `simStore.setConfig(simConfig)` → Toast success → `store.reset()`), **Cancel** (→ `store.stop()` + `store.reset()`; persisted JSON stays in Garage for later re-import via SessionsPickerDialog). **Keyboard**: `C`/`c` capture; `Esc` `window.confirm('Stop and discard this recording?')` before cancel. **Error UX**: surfaces `store.error` in a banner; import failures route through Toast and keep session state so the author can retry via the picker. Import flow deliberately lives in the component (not the store) because it crosses two stores + one REST call — store boundary stays clean. Unit-test coverage for this component is deferred to TD-014.21 integration test (per the TD-014 plan) since most behaviour is cross-store orchestration rather than pure UI. **Verification**: tsc 0; ESLint clean.

- [x] TD-014.12 — `SessionsPickerDialog.tsx` ✅ (2026-04-24). Modal that fetches `recorderApi.listSessions()` on open and on **Refresh**. Per-row: title + URL + stepCount + `new Date(startedAt).toLocaleString()` + status-coloured chip; **Import** button → `courseApi.importSimulation(course.id, row.id)` → `simStore.setConfig(simConfig)` (per R-02 — NOT `openPanel`, which would reset selectedStepIndex) → Toast "Simulation imported (N steps)" → close modal. Empty-state copy: *"No recordings yet. Use **Record…** to start one."* (matches manual §13 exactly). Fetch errors surface in a banner without crashing; import failures toast + keep dialog open so the user can retry another row. Close via Esc / Close button / click-outside. **Tests** (TD-014.20, new `SessionsPickerDialog.test.tsx`, +10): render gating (open=false → null, no fetch); fetch on open; empty-state copy; list-per-session rendering; fetch error banner; Import happy path (simStore.config populated + onClose called); Import failure keeps open + does not mutate store; Refresh re-fetches; Esc + Close both close. **Verification**: GREEN 10/10 first run; tsc 0; ESLint clean.

- [x] TD-014.13 — SimulationEditor header wiring ✅ (2026-04-24). Added **Record…** + **Import…** buttons in `SimulationEditor.tsx` header immediately before Save & Close / Cancel. Both `data-testid="sim-record-btn"` / `sim-import-btn` for E2E; both disabled while `recorderStore.recording` is true (via `useRecorderStore(s => s.recording)`). Local React state `recorderLauncherOpen` / `sessionsPickerOpen` controls modal visibility. Also mounts `<RecorderLiveView />` unconditionally within the editor — it self-gates on `recording` so adding/removing it has no visible cost. New `styles.btnDisabled` (opacity 0.5, not-allowed) added to the styles object, shared between the two new buttons. **Tests**: two new assertions in `SimulationEditor.integration.test.tsx` (TD-014.21) cover "Record… opens launcher dialog" and "Import… opens picker + fetches list" — see that entry for details. **Verification**: full simulation test subdirectory **86/86** after wiring (from 16+28+21+6+18+10 = 99... recounting: 6 files passing, 86 tests).

#### (C) Tests

- [x] TD-014.14 — simStore tests ✅ **pre-satisfied by TD-014.2** (2026-04-24). The 12 tests landed at TD-014.2 cover exactly what this subtask specifies: `addStep` defaults-applied / order-incremented / selectedStepIndex-set-to-new / zero-size-hotspot-sentinel / overrides-precedence; `setConfig` replaces-config / preserves-panelOpen+editingComponentId / clamps-selectedStepIndex / handles-empty-steps. Zero `sessionId` assertions anywhere (removed in TD-014.2b). No new file needed.

- [x] TD-014.15 — HotspotCanvas tests ✅ **pre-satisfied by TD-014.6** (2026-04-24). TD-014.6 shipped **27 tests** split across `hotspotDraw.test.ts` (21 pure-logic cases) + `HotspotCanvas.test.tsx` (6 render-mode cases). Draw-mode activation (zero-size sentinel incl. width-only / height-only partial zeros), clamp-to-bounds (rectFromPoints × clampPoint), MIN_HOTSPOT_SIZE below-min rejection, edit-mode transformer presence/absence — all covered. The full mouseDown→Move→Up→onChange pipeline against a live Konva Stage is validated end-to-end in TD-014.22 with real `page.mouse` events (jsdom cannot host a functional Konva Stage to test `getPointerPosition()`). Scope satisfied; no new file needed.

- [x] TD-014.16 — StepForm tests ✅ **pre-satisfied by TD-014.4 + TD-014.7** (2026-04-24). The 15 tests in `StepForm.test.tsx` (landed across those subtasks) cover: Upload button triggers hidden file input; upload success patches both `screenshotKey` + `screenshotUrl`; upload failure does not call onChange; Upload disabled while pending; **Asset Library button calls `editor.AssetManager.open({ types: ['image'], select })` + manually triggers `select` to assert patch**; Asset Library close-on-complete behaviour; Asset Library disabled when editor is null; empty-src no-op; Clear hotspot resets to zero-size while preserving x/y/tolerance; disabled-in-draw-mode variants. Per audit R-01, no separate AssetPickerModal needed.

- [-] TD-014.17 — ~~Unit tests — `AssetPickerModal.test.tsx`~~ **Cancelled by audit R-01 (2026-04-24).** No dedicated Asset Library modal is built — TD-014.4 invokes `editor.AssetManager.open()` directly. The Asset Library path is exercised in TD-014.16 (`StepForm.test.tsx`) by mocking `editor.AssetManager.open` and asserting the select-callback patches the step. No new test file needed.

- [x] TD-014.18 — recorderStore tests ✅ **pre-satisfied by TD-014.9** (2026-04-24). The 14 tests in `recorderStore.test.ts` (landed at TD-014.9) cover every assertion listed here and more — `start` sets recording+activeSessionId+clears captures; `capture` replaces steps; `stop` returns Session + flips recording; non-Error throws narrowed to string; `reset` returns to initial. Plus concurrency guards (no-op while busy/recording/no-session) and the explicit contract that `activeSessionId` survives a successful stop so TD-014.11 can import. No new file needed.

- [x] TD-014.19 — RecorderLauncherDialog tests ✅ landed together with TD-014.10 (2026-04-24). See TD-014.10 entry for the 18-test inventory — every URL target from this spec is a named test case (`http://localhost:3000`, `http://127.0.0.1`, `http://10.0.0.1`, `http://172.16.0.5`, `http://172.31.255.255`, `http://192.168.1.1`, `http://169.254.169.254`), plus ftp://, malformed, empty-URL required error, and the lifecycle cases (submit calls start, closes on success, inline error on failure).

- [x] TD-014.20 — SessionsPickerDialog tests ✅ landed together with TD-014.12 (2026-04-24). 10 tests in `SessionsPickerDialog.test.tsx` — list rendering from mocked `listSessions`, per-row Import button calls `importSimulation` + `simStore.setConfig` + closes, Refresh re-fetches, empty-state renders "No recordings yet…" copy, fetch errors surface inline, import failures keep dialog open + don't mutate simStore, Esc/Close button close paths. Mocks the API modules directly (no MSW needed — the pattern in the repo is `vi.mock` + `vi.hoisted`).

- [x] TD-014.21 — SimulationEditor integration test ✅ (2026-04-24). New file `src/__tests__/simulation/SimulationEditor.integration.test.tsx`, +6 tests. Mounts the full overlay against **real** simStore / recorderStore / editorStore (only APIs are mocked: uploadAsset, resolveAssetUrl, listSessions, importSimulation). Covers: (1) end-to-end Add-step → step visible in DOM → editing Instruction patches simStore (cross-store wiring); (2) Upload happy path hits uploadAsset→resolveAssetUrl→step.screenshotKey+screenshotUrl both landed on simStore (not a mocked onChange); (3) Record… button opens RecorderLauncherDialog; (4) Import… button opens SessionsPickerDialog + fetches list; (5) Successful import via picker lands in simStore via `setConfig` + step-list DOM refreshes; (6) Record…/Import… disabled while `recorderStore.recording` is true. Konva mocked inline (jsdom limitation). Hotspot-draw + Save&Close persistence covered by the real-UI TD-014.22 spec since they need a live Stage + backend round-trip. **Verification**: RED → GREEN 6/6 after fixing the imported-config fixture to include full SimStep shape with `hotspot`. Full authoring-ui **985 → 1019/1019** (+34 this wave: 18 launcher + 10 picker + 6 integration).

- [x] TD-014.22 — E2E `simulation-editor.spec.ts` ✅ (2026-04-24). New file at `e2e/tests/simulation-editor.spec.ts`. Flow: `editorPage.addSlide` → `addBlockById('screenshot-sim')` → trigger the registered dblclick handler via `editor.getView().onDblClick()` (more reliable than canvas-coordinate hunting; semantically identical to the manual dblclick path defined in `registerSimBlock.ts:68-90`) → **+ Add step** ×2 (helper `addAndUploadStep`) → `input[type=file].setInputFiles(e2e/fixtures/images/test.png)` → fill Instruction → drag step 1 onto step 0 via `page.mouse.down/move/up` (TD-014.7b reorder) → **Save & Close** (via role=button regex) → reload → `waitForCanvas` → locate the widget by `[data-widget="screenshot-sim"]` → re-trigger dblclick → assert both step items still visible. Uses existing `e2e/fixtures/images/test.png` fixture; no new fixture needed. `tsc` on the E2E dir: **exit 0**. Playwright run is gated on the main CI matrix spinning up the authoring-ui dev server — covered by the standard E2E run.

- [x] TD-014.23 — E2E `simulation-recorder.spec.ts` ✅ (2026-04-24). New file at `e2e/tests/simulation-recorder.spec.ts`. **Gating**: `test.skip(!SIMULATION_ENGINE_URL, '…')` + `test.beforeAll` opportunistic `/health` probe; if the recorder service is down the suite auto-skips with a clear annotation (does not flake). **Flow**: open Sim editor → `sim-record-btn` → fill `recorder-url-input` with `RECORDER_FIXTURE_URL` env (default `https://example.com` — stable, no auth, no SPA state) → `recorder-dialog-start` → wait for `recorder-live-preview` (visible within 10s) → `recorder-live-capture` ×2 → `recorder-live-stop` → wait for live preview to unmount (15s) → `sim-step-item-0` visible. **Cleanup**: uses Playwright's `request` context to call `DELETE ${SIM_ENGINE}/recorder/sessions/${id}` (TD-014.8b endpoint), accepting either 204 or 404. `tsc` exit 0 (after fixing the type imports — `Page` moved to a direct `@playwright/test` import since the fixtures module doesn't re-export it, and Backbone's view methods wrapped with a safe cast since `E2EComponent.getView()` isn't typed on the window shim).

#### (D) Refactor, docs, closure

- [x] TD-014.24 — **Refine the generated code** ✅ (2026-04-25). 13 sub-decisions captured in `decisions/2026-04-25-simulation-style-consistency.md` (full ADR with alternatives, reasoning, trade-offs per sub-decision); two project-wide conventions surfaced and codified in their own ADRs: `decisions/2026-04-25-button-label-change-convention.md` (widget vs app-chrome label-change rule) and the **flat testID naming convention** documented inline in row #13 of the consistency ADR. **Scope correction vs spec**: AssetPickerModal.tsx does not exist (asset library uses GrapesJS `editor.AssetManager.open()`); StepForm.tsx added to scope (omission from spec, shares 6 colour tokens with the listed files). Final scope: 5 production files refactored. **Deliverables**: (a) new `packages/authoring-ui/src/components/simulation/simulationTheme.ts` module — single source of truth with 5 sections (`colors` 12 semantic tokens, `fontFamily`/`fontSize`/`fontWeight`/`text` typography, `radius`/`gap` spacing, `buttons.{primary,secondary,success,danger,disabled}` button composites, `surfaces.{overlayBase,dialog,errorBanner}` containers) using pragmatic-extraction philosophy (consensus-only tokens; layout-specific outliers stay inline with file-top comments documenting why). (b) Theme refactor across `SimulationEditor.tsx`, `RecorderLauncherDialog.tsx`, `RecorderLiveView.tsx`, `SessionsPickerDialog.tsx`, `StepForm.tsx`. (c) Reconciliations applied: dialog heading 15→16px (sub-dec 5); input fontSize 13→12px (sub-dec 6); button vertical padding 7→6px (sub-dec 8); error-banner horizontal padding 10→16px (sub-dec 9). (d) Naming collision resolved: `RecorderLiveView`'s green `btnPrimary` renamed to `buttons.success` (sub-dec 4) — `btnPrimary` is BLUE everywhere else. (e) `btnDanger` migrated from `RecorderLiveView` local styles to `buttons.danger` in theme — closes the commitment in `decisions/2026-04-24-recorder-stop-semantics.md` § Guardrails. (f) **Label-change removed universally from app-chrome buttons** per the convention ADR: 4 sites total — `Starting…` (RecorderLauncher), `Uploading…` (StepForm), `Loading…` (SessionsPicker.Refresh), `Importing…` (SessionsPicker.Import). All revert to fixed labels with `disabled + buttons.disabled` style overlay as the complete state signal. The two SessionsPicker cases were not in my original survey — surfaced by spot-check during execution; convention application was confirmed by owner before applying. (g) Error message format unified to `'X failed: ${msg}'` (sub-dec 12): StepForm's `'Could not upload screenshot: …'` → `'Upload failed: …'`; RecorderLiveView's `'Failed to stop recording: ${msg}'` (×2 sites) → `'Stop failed: ${msg}'`. Two test assertions in `RecorderLiveView.test.tsx` updated from `/failed to stop.*<msg>/i` to `/stop failed.*<msg>/i` to match the new copy. (h) `SessionsPickerDialog` testID prefix reconciled to project-wide flat convention `<scope>-<action>[-<id>]`: `session-row-${id}` → `sessions-picker-row-${id}`, `session-import-${id}` → `sessions-picker-import-${id}` (sub-dec 13; an interim hierarchical attempt `sessions-picker-row-${id}-import` was corrected after owner pointed out the deviation from the codebase convention observed in `sim-step-item-${i}`, `sim-add-step-btn`, `recorder-live-discard`, etc.). 7 test references updated (6 in `SessionsPickerDialog.test.tsx` + 1 in `SimulationEditor.integration.test.tsx`); 0 E2E references (verified via grep). (i) **Action 3 outcome documented**: production `components/simulation/*.tsx` had **0 hits** of `any`/`as any`/`as unknown as`; tests have 5 `as unknown as Editor`/`CourseDoc` casts that are legitimate test scaffolding (mocking the full grapesjs Editor type would explode the test surface). TD-004's narrowing discipline applies to production; tests can keep narrow casts at boundaries. **Verification**: authoring-ui vitest **1039/1039** passing (no regression); `npx tsc --noEmit` exit 0; `pnpm lint` 0 errors (2 pre-existing TD-004 `useComponentProperty.ts` exhaustive-deps warnings unchanged). **Methodological note for future tasks**: this subtask exposed three project-wide conventions previously undocumented — (1) lint suppression policy (codified in `AGENTS.md` §11.8 by owner), (2) widget vs app-chrome label-change rule (its own ADR), (3) flat testID naming `<scope>-<action>[-<id>]` (documented in row #13 of the consistency ADR). Future refactors that touch UI surfaces should grep `/decisions/` for these conventions first.

- [x] TD-014.25 — **Docs.** ✅ (2026-04-25). Commit deferred to TD-014.27 batch closure per owner option C. **(a)** `docs/user-guide/13-software-walkthrough.md` updated — header description lists the four real header buttons (**Record…**, **Import…**, **Save & Close**, **Cancel**); "Add step" → real `+ Add step` footer label; screenshot field split into **Upload…** + **Asset Library** under a new **Step screenshot** field group; reorder line rewritten verbatim per spec (drag + ↑ ↓ buttons + Alt+↑ / Alt+↓ keyboard) — closes audit I-01; tip box on **Record…**; note on **Clear hotspot**; **Step fields** table extended with **Step screenshot**, **Hotspot tolerance (px)**, **Hotspot** rows; *Type text* option label corrected (was *Type*). **(b)** `13-overview.png` + `13-hotspot-editor.png` placeholder comments reviewed against current overlay layout — still accurate; capture deferred to TD-014.28. No file edit. **(c)** New `docs/developer-guide/11-simulation-recorder-architecture.md` — sections: Topology (4-process table with ports + roles), End-to-end call sequence (mermaid sequenceDiagram, 5 phases start → live capture → stop → import → save, PyCharm-compatible syntax), Stores (recorderStore + simStore boundary, interface declarations + behavioural notes), Data shapes (RawSimStep / Session / SessionSummary / AuthoredSimStep / SimConfig with explicit "no sessionId on SimConfig" callout per TD-014.2b), HTTP surface (8 endpoints), Lifecycle (start with 429 cap, live capture with screenshot polling cost note, stop three-button decision matrix, import, save), Threading/resource model, Configuration (env-var tables), Failure modes, See also. Cross-linked from `developer-guide/index.md` row 11 + `developer-guide.md` MOVED table. **(d) Cross-cutting sweep** — grep across 20 user-guide files + `user-manual-v2-scope.md` for 7 keywords. Per-file decisions in `docs/issues/issues-TD-014.md` § "TD-014.25d sweep". **Files updated**: `12-simulations-overview.md` (one-liner about three SW authoring paths — record / import / manual upload — before chapter-13 link); `13-software-walkthrough.md` (covered in (a)). **Files NOT updated** despite the spec mentioning them: `18-troubleshooting.md` and `20-glossary.md`. Initial drafts here imported developer-tier vocabulary (CORS, simulation-engine process, allowed-origin, persisted recording manifest) into the user manual. Owner caught this — *"¿Has añadido información de bugs y fixes en ficheros de un manual de usuario?"* — and chose option C: revert both. The recorder failure-mode entries and Session-ID glossary entry are deferred until they can be expressed strictly in terms of user-observable symptoms ("the recording isn't working" → "ask your administrator") with no engineering vocabulary. The original spec line at .25(d) implicitly assumed translation into user-facing language; my drafts kept the technical shorthand verbatim. Lesson recorded in feedback memory: developer concepts (HTTP codes, CORS, ports, services, process internals) belong in `developer-guide/` only; the user manual stays in author-observable terms even when the spec mentions technical shortcuts. **Files inspected, no change required**: `01-welcome.md` through `20-glossary.md` minus the four above; `index.md`; `user-manual-v2-scope.md` (§13 already declares "Recording a sequence"). **Verification**: zero production code changes. Markdown structure preserved.

- [x] TD-014.26 — Reviewer pass on Phase B (.24 + .25). ✅ (2026-04-25). Two reviewer agents (code-reviewer + typescript-reviewer) executed in parallel against the uncommitted .24 production code (`simulationTheme.ts` + 5 component refactors + 3 test updates) and the new .25c architecture doc. Phase A subtasks (.29-.38) and the user-guide markdown (.25a/d) were out of scope — already verified in their own closure notes / owner-reviewed in-session. Findings + dispositions appended to `docs/issues/issues-TD-014.md` § "TD-014.26 — reviewer pass (2026-04-25)". **CRITICAL: 0**, **HIGH: 1 (R-H1) — fixed**, **MEDIUM: 2 (R-M1 fixed; R-M2 deferred to follow-up TD-014-followup-1)**, **LOW: 1 (kept for traceability)**, **INFO: 5 (3 affirmations + 2 pre-existing out-of-scope)**. **Fixes applied this subtask**: (1) **R-H1** (HIGH) — `SimulationEditor.tsx:293,306,318` had three hardcoded `'#6c7086'` literals; refactor missed them. Replaced with `colors.textMuted` (the literal's source-of-truth in `simulationTheme.ts:44`). All three callsites verified zero-hit post-fix; `simulationTheme.ts` retains the only `#6c7086` literal as the canonical token. (2) **R-M1** (MEDIUM, doc-accuracy bug) — the new `11-simulation-recorder-architecture.md` introduced a fictitious `RawSimStep` type name that doesn't exist in `simulation-engine` (canonical type is `SimStep`) or in `authoring-ui` (mirror of same shape). The `RawSimStep` alias only exists in `backend/api/src/types/simulation.ts` where the import mapper uses it to disambiguate from `AuthoredSimStep`. Renamed to `SimStep` throughout (8 sites: data-shape interface block + `Session.steps` field declaration + sequence-diagram comment ×2 + boundary table ×2 + HTTP table response shape + lifecycle prose ×2). Added an explicit "Naming asymmetry" callout near the `SimStep` block explaining the `backend/api`-local alias. Three legitimate `RawSimStep` cross-references kept in prose where the import-mapper context makes the alias relevant. Verified: `tsc` types still compile against the doc; `Session.steps: SimStep[]` matches `recorder/types.ts:45`; the backend mapper at `simulations.ts:139` still resolves correctly. **Deferred (follow-up ticket TD-014-followup-1)**: R-M2 — spread-with-`null` pattern `{ ...base, ...(condition ? buttons.disabled : null) }` at 6 callsites across the 5 simulation components. Type-safe (TS allows null-spread as no-op); pattern is project-wide consistent (introduced as part of .24's button-composite contract). Cosmetic-only; refactoring uniformly would be a follow-up cleanup, not a .24 correction. Owner to decide later. **Kept (traceability only)**: R-L1 (verbose comment in `RecorderLiveView.tsx:246-248` — accurate, just verbose); R-T1 (TS narrowing on theme exports — agent itself notes "works in practice"; reclassified HIGH→INFO since `tsc --noEmit` passes and no consumer relies on literal narrowing); R3 (pre-existing `course.id` access on `CourseDoc` type at two sites — predates this block, will surface in a future TD ticket); R4 (`SimulationEditor.tsx:138` `config` non-null narrowing inside closure — runtime-safe via early-return at line 48, idiomatic Zustand-store pattern); R5 + R-I1 + R-I2 (affirmations on theme typing, button disabled composition, testID convention). **Verification post-fix**: `grep "'#6c7086'" packages/authoring-ui/src/components/simulation/` → 0 hits production-wide (only `simulationTheme.ts` retains it as the source-of-truth definition); `grep RawSimStep docs/developer-guide/11-simulation-recorder-architecture.md` → 3 hits, all intentional asymmetry-cross-references; markdown structure preserved. **Block-closure note**: per owner option C, .24 + .25 + .26 fixes stay uncommitted in working tree until TD-014.27 batch closure. Spec compliance: "resolve all CRITICAL/HIGH" → done; "MEDIUM addressed if quick; otherwise documented with explicit follow-up ticket" → done; "INFO/LOW kept in the file for traceability" → done.

- [x] TD-014.27 — **Full verification + block closure ✅ (2026-04-26 functional close, 2026-05-04 administrative close + post-recovery re-validation).** Functional work shipped in `c151f27` (single-batch commit covering Phase A .29–.38 + Phase B .24–.27); fix-forwards `fec7d08` (3 TS strict-mode errors blocking CI build) + `2bbf3b7` (test course stubs aligned with `CourseDoc` shape `id` → `_id`). CI green over HEAD `473067b` since 2026-04-29 (run `Code Quality: Push on master` + `CI` both `completed/success`). CodeQL clean. **Original sub-step coverage**: (a) `pnpm -r test` green; (b) `pnpm -r lint` 0 errors; (c) `npx tsc -b` exit 0; (d) F4 OpenAPI regen — swagger extended with `AuthoredSimStep` (14 fields) + `SimHotspot` (5 fields) component schemas; `SimConfig.steps.items` switched from `{ type: 'object' }` to `$ref`; stale `sessionId` removed; `gen:api-client` produces `openapi.json` (gitignored) + `generated.ts` (gitignored), only `openapi.hash` is tracked; (e) full E2E **167/167 pass** (recorder + editor specs ran end-to-end for the first time, surfaced 5 inherited bugs all fixed in-scope per owner directive — `model.id`→`getId()` in `registerSimBlock.ts`, `apiClient.ts` credentials handling, `phaser-simulations` tsconfig + test cast, `e2e/**/*.spec.ts` ESLint override for `no-empty-pattern`, `simulation-editor.spec.ts` `data-widget` selector); (f) `CHANGELOG.md` v0.5.66 entry shipped; (g) `WORKING_CONTEXT.md` updated; (h) tasks.md subtasks checked; (i) commit + push + CI green + CodeQL clean. **2026-05-04 administrative close**: original session left this checkbox unticked and the `WORKING_CONTEXT.md` line still claiming "uncommitted WIP" even after `c151f27` shipped. This session re-validated everything from a fresh install (the worktree opened with `pnpm-lock.yaml` deleted — likely an aborted `pnpm install yn` that left `package.json` with a stray `yn` devDependency and the lockfile gone; restored both from HEAD via `git checkout HEAD -- package.json pnpm-lock.yaml`, confirmed no `yn` callers in the codebase, then `pnpm install --frozen-lockfile` exit 0 in 16m26s — 1058 packages installed clean — satisfying CI parity asymmetry #5). Re-validation pipeline: `verify:types` exit 0 → `verify:lint` exit 0 (only the 2 pre-existing TD-004 warnings, unchanged) → `verify:test` **1882/1882 across 7 packages** (authoring-ui 1039, backend 149, runtime-player 265, scorm-packager 156, phaser-simulations 125, simulation-engine 74, question-engine 74) → `tsc -b --force` exit 0 across 9 packages + `e2e tsc --noEmit` exit 0 → `gen:api-client` byte-identical (tracked `openapi.hash` `cb7d860059…` unchanged) → full local E2E **165 passed, 3 skipped, 1 flake** (`T611.10` from the T611 block — passed cleanly on `--last-failed` re-run in 24.6s; CI absorbs this profile via `retries: 2`). TD-014's own specs `simulation-editor.spec.ts` (.22/.35) green; `simulation-recorder.spec.ts` (.23) skipped correctly without `SIMULATION_ENGINE_URL`. Side-effects of the local E2E run (8 modified + 2 untracked PNGs in `docs/user-guide/assets/screenshots/` from `docs-screenshots.spec.ts` re-render under `vite dev` vs the `vite preview` baseline used in `c151f27`) NOT included in the closure commit per TD-013 boundary; the 2 untracked (`13-overview.png`, `13-hotspot-editor.png`) suggest TD-013.5's commit `473067b` may not have actually staged those files — flagged for TD-013 follow-up. `.gitignore` updated with owner-local work folders (`docs/CODEMAPS/`, `piezas-cursos/`, `video/`) + `.claude/scheduled_tasks.lock`.

- [x] TD-014.28 — **Unblock TD-013.5 ✅ (2026-04-28).** Confirmation that the real-UI path for §13 captures is feasible with zero store seeding, end-to-end proven by `e2e/tests/simulation-editor.spec.ts` (TD-014.22 + .35) — full flow exercised during TD-014.27.e (167/167 green). **Confirmed surfaces** (all production code, no test-mode escape hatch): (1) overlay opens via `screenshot-sim` widget dblclick → GrapesJS view `onDblClick` → `useSimStore.openPanel(config, model.getId())` (`registerSimBlock.ts:70-94`); the dblclick can be dispatched in Playwright via `page.evaluate` on the GrapesJS view (`simulation-editor.spec.ts:23-32`); (2) **+ Add step** button at `SimulationEditor.tsx:278` (`data-testid="sim-add-step-btn"`) — appends a step with zero-size hotspot sentinel; (3) **Upload…** at `StepForm.tsx:84` (`data-testid="step-upload-btn"`) — backed by an `input[type="file"]` that Playwright drives via `setInputFiles(FIXTURE_IMG)`; (4) **Description** input at `StepForm.tsx:119` (`data-testid="step-description-input"`) gives stable identity; (5) hotspot draw via real `page.mouse` events on `.konvajs-content` stage — gesture pattern at `simulation-editor.spec.ts:80-117` (0.15/0.85 inset keeps clamp away from `rectFromPoints` borders; range bounds `> 100 / > 50 / >= 0` absorb DPR drift); (6) **Save & Close** / **Cancel** in overlay header. **TD-013.5 entry rewritten** in this same edit to replace the original "Seed a session via direct store mutation" guidance with the real-UI recipe + concrete selectors. **Owner principle applied** ("todas las funcionalidades dadas por operativas tienen que poder usarse y reproducirse"): the docs capture must exercise the same UI a user has to use. **Recommended polish in TD-013.5** (not blocking, but preferred): add `data-testid="sim-editor-overlay"` to the overlay root and `data-testid="sim-canvas-area"` to the Konva stage container so `captureElement` has a deterministic anchor without falling back to `boundingBox` math. **Verification**: zero code changes in this subtask — pure documentation update reflecting the contract already in place. The `__simStore` window exposure (TD-014.29) remains a read-only verification surface for E2E asserts (e.g. polling `hotspot.width` for commit-lag); it is NOT used for seeding in TD-013.5.

#### (E) Audit externo — findings cierre (TD-014.29–.38, 2026-04-24)

> Sub-bloque surgido del audit externo tras la entrega de TD-014.23. 11 findings totales: F4 absorbida por TD-014.27.d; F1–F3 + F5–F11 → 10 nuevas subtareas. Orden de ejecución enforced por la numeración — cada paso desbloquea la verificación del siguiente. Detalle completo en `docs/issues/issues-TD-014.md` § "Audit externo 2026-04-24 — findings". Meta-patrón: todos los findings matchean `~/.claude/rules/common/ai-regression.md` — los tests validaban la implementación tal como estaba escrita, no el contrato que deberían haber forzado.

- [x] TD-014.29 — Expose `__simStore` + `__recorderStore` on window for E2E ✅ (2026-04-24). Pattern mirrors the `__elearn_editor` / `__elearn_store` exposure in `EditorCanvas.tsx:93-96` — same gate (`import.meta.env.DEV || import.meta.env.VITE_E2E_MODE === 'true'`), same `typeof window !== 'undefined'` guard for Node-side module loads. Changes: (1) `src/store/simStore.ts` — tail block assigns `window.__simStore = useSimStore`; (2) `src/store/recorderStore.ts` — tail block assigns `window.__recorderStore = useRecorderStore`; (3) `src/types/globals.d.ts` — added `__simStore?` + `__recorderStore?` declarations reusing the existing `ElearnStoreBind` shape (loose `unknown` so no Zustand imports leak into the ambient file); (4) `e2e/types/elearn-window.d.ts` — narrow E2E-specific bind types `E2ESimStoreBind` + `E2ERecorderStoreBind` with the state-shape subset the specs actually read (steps+hotspot for sim; activeSessionId+recording+isBusy+error for recorder) — policy inherited from the existing `E2EEditor` file header (narrow ONCE at the boundary, no `as unknown as` at call sites); (5) `e2e/tests/simulation-recorder.spec.ts:81-92` — replaced the fictitious `window.__recorderSessionId` read with `window.__recorderStore?.getState().activeSessionId`; added `expect(sessionId).not.toBeNull()` as a belt-and-braces assertion so a future regression of the exposure block fails the spec instead of silently skipping cleanup. **Verification**: authoring-ui **1019/1019** vitest passing (no regression from module-level side effect — the store reset patterns in the existing tests already handle re-initialisation); `tsc --noEmit` exit 0 in authoring-ui + e2e; ESLint clean on the 3 modified authoring-ui files. **F7 from audit externo 2026-04-24 — HIGH, closed**.

- [x] TD-014.30 — Rewrite drag-drop E2E assertion ✅ (2026-04-24). **Scope correction vs. original spec**: the plan called for `toEqual(['Step two: enter password', 'Step one: click login'])` reading from `data-field="description"`. The helper only filled **Instruction**, never **Description** — the display div reads `step.description`, so the assertion would have seen empty strings. Corrected approach: (1) helper signature widened to `addAndUploadStep(page, description, instruction)` filling both fields; test passes `'Step 1'/'Step 2'` as descriptions and `'Step one: click login'/'Step two: enter password'` as instructions (instructions now available for the TD-014.31 / F6 post-reload content check). (2) New `data-testid="step-description-input"` on the Description input in `StepForm.tsx:114` so the helper doesn't rely on input ordering. (3) `data-field="description"` added to the step-list display div in `SimulationEditor.tsx:215`. (4) E2E assertion replaced with a pair: DOM-level (`[data-field="description"]` textContent per step item → `toEqual(['Step 2', 'Step 1'])`) AND store-level (`window.__simStore?.getState().config.steps.map(s => s.description) → toEqual(['Step 2', 'Step 1'])`). Belt-and-braces guards against a DOM render that accidentally matches the old order by coincidence (reused keys) or a store update that doesn't reach the DOM. (5) Removed the misleading *"keeps spec non-flaky"* comment. Depends on TD-014.29 for `__simStore` exposure. **Verification**: authoring-ui vitest **1019/1019** (no regression from the new data-testid — existing StepForm.test.tsx uses `step-upload-btn` / `step-asset-library-btn` patterns that don't collide); authoring-ui + e2e `tsc --noEmit` exit 0; ESLint clean. **F5 from audit externo 2026-04-24 — CRITICAL, closed**.

- [x] TD-014.31 — E2E persistence content verification ✅ (2026-04-24) — ⚠️ **PARTIAL SCOPE: hotspot persistence deferred to TD-014.35**. After reload + re-open, asserted content across **three independent surfaces of truth** so a regression in any one fails the spec: (a) step-list DOM — `[data-field="description"]` textContent order stays `['Step 2', 'Step 1']` (the reorder persisted); (b) StepForm inputs — click step-item-0, assert `step-description-input` has value `'Step 2'` and first textarea (Instruction) has value `'Step two: enter password'`; (c) `__simStore`-level — read the full shape per step via `window.__simStore?.getState().config?.steps.map(...)` and `toEqual` against an array of `{ description, instruction, hasScreenshot, hasScreenshotKey, interactionType }` — belt-and-braces shape check that also catches silent field drops (e.g. a `handleSave` bug that copies only some fields). Screenshot URL assertion uses `Boolean(s.screenshotUrl && s.screenshotUrl.length > 0)` rather than exact URL match (presigned URL signatures are non-stable). **⚠️ DEFERRED — MUST BE CLOSED IN TD-014.35 (F10)**: hotspot bounds are NOT asserted in the post-reload block because the current spec does not draw a hotspot — asserting the zero-size sentinel `{ width: 0, height: 0 }` would be technically correct but would verify nothing (the un-drawn state is the trivial identity). TD-014.35 adds the draw-mode gesture AND extends this same post-reload `persisted` assertion to also cover `hotspot: { width > 100, height > 50, x >= 0, y >= 0 }` — do NOT close TD-014.35 without the extension, otherwise F6 coverage stays partial. Inline comment in `e2e/tests/simulation-editor.spec.ts` (at the end of the test) cross-references .35 so the split is visible to future readers. **Verification**: authoring-ui vitest 1019/1019 (no regression — change is E2E-only); e2e `tsc --noEmit` exit 0. **F6 from audit externo 2026-04-24 — CRITICAL, closed (partial — see TD-014.35)**.

- [x] TD-014.32 — **`handleSave` test coverage + safety hardening (F11)** ✅ (2026-04-24). `SimulationEditor.handleSave` rewritten as async with three explicit error paths + one happy path, all pinned by tests. **Production changes** in `packages/authoring-ui/src/components/simulation/SimulationEditor.tsx`: (1) imported `useToast` and bound it at the top of the component; (2) `handleSave` is now `async`, with early-returns for `!editor` (silent no-op) and `!editingComponentId` (silent no-op — invariant violation, bail without side effects); if `editor.getWrapper()?.find(...)[0]` is falsy → `toast.error('Cannot save: widget no longer exists on canvas')` + abort (panel stays open); `await requestSave?.()` wrapped in try/catch; on reject → `toast.error(\`Save failed: ${msg}\`)` + abort (`msg = err instanceof Error ? err.message : 'unknown error'`); `closePanel()` runs ONLY inside the try's success arm. Inline comment explains why overlay must stay open on failure (SaveErrorBanner is covered by the overlay, toast z-index 9999 is the only visible channel). **Test changes** in `src/__tests__/simulation/SimulationEditor.test.tsx`: new describe block `SimulationEditor — TD-014.32 handleSave (F11)` with 4 tests + `makeMockEditor(component | undefined)` helper + `makeMockComponent()` + `simpleConfig()` helpers. Tests: (a) **happy path** — asserts `requestSave` called once, `component.set('extendedProperties', {simConfig: ...})` called, `panelOpen` becomes false (closePanel fired), no `role=alert` toast; (b) **component missing** — `find()` returns `[]`, asserts toast text matches `/widget no longer exists/i`, `requestSave` never called, `panelOpen` stays true, `sim-add-step-btn` still rendered; (c) **requestSave rejects** — rejected with `new Error('network down')`, asserts toast text matches `/save failed.*network down/i`, `component.set` WAS called (in-memory change retained), `panelOpen` stays true; (d) **no editor** — `editor: null`, asserts `requestSave` never called, no toast, `panelOpen` stays true. Mocks mirror real `useEditorStore` shape (`editor: Editor | null`, `requestSave: (() => Promise<void>) | null`) so contract drift on either side fails here first. **Verification**: authoring-ui vitest **1023/1023** passing (1019 + 4 new); `tsc --noEmit` exit 0; `pnpm lint` exit 0 with only the 2 pre-existing TD-004 warnings (`useComponentProperty.ts` exhaustive-deps, unchanged count). **F11 from audit externo 2026-04-24 — CRITICAL, closed**.

- [x] TD-014.33 — **Backend `interactionType` initialisation + contract test (F1)** ✅ (2026-04-24). **Root-cause expansion** beyond the original plan: the backend's `AuthoredSimStep` type at `backend/api/src/types/simulation.ts` was also missing `interactionType` + `expectedText` entirely — so the fix had to start at the type definition, not just the emission. Without that, the new emission line would fail TS strict-mode checks. **Changes**: (1) `backend/api/src/types/simulation.ts` — added `SimInteractionType = 'click' | 'hover' | 'type'` (identical to the authoring-ui union, with an inline comment flagging the lockstep contract); extended `AuthoredSimStep` with `interactionType: SimInteractionType` (required) and `expectedText?: string` (optional, only set for 'type' steps). (2) `backend/api/src/routes/simulations.ts:139-159` — the `session.steps.map` call now emits `interactionType: 'click'` on every derived step; `expectedText` stays absent by design. Inline comment points at the ai-regression.md meta-pattern. (3) `backend/api/src/__tests__/simulations.test.ts` — new dedicated `it` block `"seeds interactionType='click' on every imported step (no expectedText)"` placed immediately after the hotspot contract test; iterates every step in the response and asserts both `interactionType === 'click'` and `expectedText === undefined`. Isolated as its own test (not folded into the hotspot one) so a regression surfaces as a distinct signal. (4) `packages/authoring-ui/src/__tests__/simulation/SimulationEditor.integration.test.tsx:165-193` — removed the two pre-seeded `interactionType: 'click'` lines from the `mockImportSimulation.mockResolvedValue` fixture; replaced with a 7-line comment documenting the "mock that lies" anti-pattern (pre-.33 the field hid the drift) and cross-referencing the backend contract test as the new source of truth. Mock is now deliberately minimal — if the backend regresses, the *backend* contract test fails (upstream), not this integration test (which stays focused on the UI wiring). **Verification**: backend/api vitest **149/149** passing (148 + 1 new); authoring-ui vitest **1023/1023** passing (no regression from mock minimisation — StepForm's `step.interactionType ?? 'click'` fallback at line 206 correctly handles the missing field); `tsc --noEmit` exit 0 in both packages; eslint clean on all 4 changed files (no new warnings; authoring-ui still reports the 2 pre-existing TD-004 warnings). **F1 from audit externo 2026-04-24 — HIGH, closed**.

- [x] TD-014.34 — **Split recorder exit paths into Stop & import / Stop / Discard (F2 — Option C)** ✅ (2026-04-24). Owner decision 2026-04-24: reject Option A (single destructive Cancel) and Option B (single preserving Cancel, rename copy) because both leave the "Cancel" button semantically overloaded; adopt **Option C — three distinct actions** with single-responsibility semantics. Full rationale + options evaluated in `decisions/2026-04-24-recorder-stop-semantics.md`. Work items: (1) In `RecorderLiveView.tsx`, replace the current `[Stop & import] [Cancel]` pair with `[Discard] <spacer> [Capture step] [Stop] [Stop & import]`; remove `handleCancel`; add `handleStopPreserve` (stop → preserve in Garage → reset) and `handleDiscard` (confirm → stop → deleteSession → reset). Map Esc to `handleDiscard` (retains the existing confirm). Add a `btnDanger` style token (red-accent border + neutral fill) in the overlay styles object — future move to `simulationTheme.ts` happens in TD-014.24. (2) Replace testid `recorder-live-cancel` with two new ids: `recorder-live-preserve` (Stop) and `recorder-live-discard` (Discard); `recorder-live-stop` is retained and now refers to "Stop & import" (E2E `simulation-recorder.spec.ts:71` already uses it for the happy path — no E2E change needed). (3) New describe block in `src/__tests__/simulation/RecorderLiveView.test.tsx` with the 10 tests enumerated in the decision doc § "Tests to add". (4) Integration test additions: one Stop happy-path test asserting count-aware toast; one Discard happy-path test asserting `deleteSession` called. (5) User-manual §13 (TD-014.25) + CHANGELOG v0.5.66 UX-change note.

  **⚠️ Implementation notes agreed with owner (MUST be respected at execution — see `decisions/2026-04-24-recorder-stop-semantics.md` § Implementation Notes for full rationale):**

  - **(a) `stop()` failure → do NOT `reset()`.** Both `handleStopPreserve` and `handleDiscard` must check the return value of `stop()`. If `undefined` (failure), toast the error from `useRecorderStore.getState().error` and **return without resetting local state**. `recorderStore.stop()` deliberately keeps `recording: true` on failure so the user can retry — calling `reset()` blindly would wipe `activeSessionId` and create a zombie session (frontend thinks it's done; backend still has the recording alive, now unreachable). Tests 3 + 8 in the decision doc pin this contract.

  - **(b) DELETE 404 is silent success.** In `handleDiscard`'s catch block, inspect the rejection message: `if (/404/.test(msg))` → same `toast.info('Recording discarded')` as the happy path (the resource is already gone — functional success). Only non-404 failures surface as `toast.warning` with a "remove it from the Sessions list" hint. Test 6 in the decision doc pins this distinction.

  - **(c) Stop-preserve toast reports real count.** Copy is `Recording stopped — ${count} step${count === 1 ? '' : 's'} saved to Sessions` where `count = persisted.steps.length`. The zero-step case (user opened the recorder, stopped without capturing) produces *"0 steps saved"* — technically truthful and lets the user act accordingly. Rejected the alternative of auto-discarding zero-step sessions inside `handleStopPreserve` because it would hide a destructive action behind a button labelled Stop, exactly the overloading problem Option C was chosen to eliminate. Test 2 in the decision doc pins the zero-count copy.

  - **(d) Discard button is LEFTMOST, separated from the Stop group by a spacer.** Western reading order conditions users to expect the rightmost button to be the "close / commit / final" action; putting Discard adjacent to Stop is a misclick target that the confirm only *mitigates*. Final layout `[Discard] · [Capture] [Stop] [Stop & import]` places the destructive exit on the margin, outside the eye's natural path to the primary action. Pattern borrowed from Figma/Notion. Test 10 in the decision doc pins the DOM ordering (assertions on `headerActions` children: first child = `recorder-live-discard`, last child = `recorder-live-stop`).

  **Delivered changes** (2026-04-24):
  - `packages/authoring-ui/src/components/simulation/RecorderLiveView.tsx` — removed `handleCancel`; added `handleStopPreserve` (stop-fail guard from Implementation Note a; count-aware toast from Note c) and `handleDiscard` (confirm → stop-fail guard → deleteSession with 404-silent per Note b → non-404 reset-anyway per Note b). Esc now routes to `handleDiscard`. Button layout: `[Discard] · [Capture] [Stop] [Stop & import]` via new `actionSpacer` flex-1 style (Note d). New `btnDanger` style token (red-accent border, neutral fill, mirrors Toast error palette #f38ba8) — moves to `simulationTheme.ts` in TD-014.24.
  - Test IDs: `recorder-live-cancel` removed; `recorder-live-discard` + `recorder-live-preserve` added; `recorder-live-stop` semantics now = "Stop & import" (E2E `simulation-recorder.spec.ts:71` unchanged — already used it for that flow).
  - `packages/authoring-ui/src/__tests__/simulation/RecorderLiveView.test.tsx` — NEW file, 10 tests covering the full matrix: preserve happy-path, zero-step (Note c), stop-fail (Note a); discard confirm-cancel, happy-path, 404-silent (Note b), non-404 warning + reset, stop-fail (Note a); Esc routing; DOM-ordering contract (Note d).
  - `packages/authoring-ui/src/__tests__/simulation/SimulationEditor.integration.test.tsx` — hoisted `mockStopRecording` + `mockDeleteSession` (previously anonymous `vi.fn()` inside the factory, unreachable from tests); added 2 integration tests — Stop preserve (count-aware toast verified through the full SimulationEditor+RecorderLiveView stack) and Discard happy-path (`deleteSession` called with session id).
  **Verification**: authoring-ui vitest **1035/1035** passing (1023 pre-existing + 10 new unit + 2 new integration = +12); `tsc --noEmit` exit 0; `pnpm lint` exit 0 (only the 2 pre-existing TD-004 `useComponentProperty.ts` exhaustive-deps warnings, unchanged). Decision doc `decisions/2026-04-24-recorder-stop-semantics.md` promoted from **Accepted (pending implementation)** to **Accepted (delivered)** at close. **F2 from audit externo 2026-04-24 — HIGH, closed**.

  **F2 from audit externo 2026-04-24 — HIGH**. Owner decision captured in `decisions/2026-04-24-recorder-stop-semantics.md`; cross-reference that file before implementation.

- [x] TD-014.35 — **E2E draw-mode gesture + hotspot persistence (F10 + inherited .31 scope)** ✅ (2026-04-25). Closes audit-externo F10 and ships the hotspot-persistence scope deferred from TD-014.31. **Pure E2E change** — zero production code edited. **Single-file diff** in `e2e/tests/simulation-editor.spec.ts`: (1) New block inserted **between step #2 creation and the drag-reorder phase** — `simStore.addStep` sets `selectedStepIndex` to the newly-added step (=1 after Step 2, see `simStore.ts:118`), so the block clicks `sim-step-item-0` first to bring Step 1 into focus and force HotspotCanvas to mount in draw-mode (its hotspot is the zero-size sentinel from `addStep` → `isDrawModeHotspot()` true). Locates the Konva stage via `page.locator('.konvajs-content').first()` — the only stable selector since Konva renders no testid surface on its container; the sim editor mounts exactly one stage so `.first()` is unambiguous. Reads `boundingBox()`, then drags from `(stage.x + 0.15·width, stage.y + 0.15·height)` to `(stage.x + 0.85·width, stage.y + 0.85·height)` via `page.mouse.move → down → move({steps:10}) → up`. The 0.15/0.85 inset (a) keeps both corners away from `clampPoint`'s borders so neither axis is squashed by `rectFromPoints`, and (b) yields a committed rect of ~448×252 in canvas-units (CANVAS_W/H = 640×360 in `HotspotCanvas.tsx:25-26`) — comfortably above the > 100 / > 50 thresholds. Immediate assertion uses `expect.poll` on `steps[0].hotspot.width > 100` to absorb React-commit lag between `mouseUp` and `updateStep` landing in the store, then snapshots the full hotspot and asserts the four range bounds individually (`width > 100`, `height > 50`, `x >= 0`, `y >= 0`). (2) **Inherited TD-014.31 scope closed** — replaced the trailing comment that said "Hotspot-content persistence is verified in TD-014.35" (forward-reference now obsolete) with a concrete `persistedHotspots` assertion: reads `__simStore?.getState().config?.steps.map(s => s.hotspot)` after reload + re-open, asserts `length === 2`, then `persistedHotspots[0]` (Step 2 — never drawn) `toMatchObject({ width: 0, height: 0 })` to confirm the zero-size sentinel survives, and `persistedHotspots[1]` (Step 1 — drawn pre-reorder, swapped to index 1 by the drag) asserts the same four range bounds. Belt-and-braces against `handleSave` regressions that would silently drop hotspot fields, plus end-to-end validation of the backend round-trip on `SimHotspot` shape (the contract is also pinned upstream by TD-014.33's backend test, but this is the only E2E that proves the drawn rect survives `Save & Close → reload → re-open`). **Why ranges, not exact equality**: `rectFromPoints` clamps + rounds, and device pixel ratio shifts integer values across runners (CI vs local Win/Mac). The `> 100 / > 50 / >= 0` window is wide enough to absorb DPR drift while still rejecting the zero-size sentinel and any < 100/50 misclick. **TD-014.31 partial-scope marker** — left in place for historical traceability (the 2026-04-24 deferral is part of the audit-externo paper trail), but the F6 coverage gap is now closed end-to-end. Depends on TD-014.29 for `__simStore` exposure (delivered). **Adjacent pre-existing lint resolution** (folded in per owner directive 2026-04-25 — pre-existing lint errors must be resolved, not deferred): `e2e/tests/simulation-recorder.spec.ts:31` `no-empty-pattern` on Playwright's `test.beforeAll(async ({}, testInfo) => …)` (latent since TD-014.23, incorrectly reported clean at .29 verification). **Option A chosen by owner** after rejecting an initial `eslint-disable-next-line` proposal as a unilateral protocol breach — alternatives B (refactor to per-test helper, dropping `beforeAll`), C (ESLint override for `e2e/**/*.spec.ts`), D (per-line suppression) were all surfaced with trade-offs before owner picked. Fix: rename empty destructure `{}` → `_args` (1-token diff). `argsIgnorePattern: '^_'` already configured in `.eslintrc.cjs:31` exempts `_`-prefixed args from `no-unused-vars`, so no rule disable is needed and no precedent for suppression is set. **Verification**: e2e `tsc --noEmit` exit 0 (no new types added — uses the `E2ESimStoreState.config.steps[].hotspot` shape already declared in `e2e/types/elearn-window.d.ts:116` from TD-014.29); authoring-ui vitest **1035/1035** passing (no regression — production code untouched); `pnpm lint` **0 errors** (down from 1); 2 pre-existing TD-004 warnings in `useComponentProperty.ts` remain (unchanged count, tracked separately under TD-004's exhaustive-deps follow-up). Full E2E run deferred to TD-014.27 per WORKING_CONTEXT batch policy. **F10 from audit externo 2026-04-24 — MEDIUM, closed**. **F6 partial-scope closure from TD-014.31 — closed**.

- [x] TD-014.36 — **Correct `getLiveScreenshotUrl` docstring (F3 — Option B confirmed)** ✅ (2026-04-25). Closes audit-externo F3. **Pure docstring rewrite** in `packages/authoring-ui/src/api/recorderApi.ts:85-93` — zero behavioural change, no lint/type/test impact. The previous text claimed *"`Cache-Control: no-store`, so a rotating `t=Date.now()` query is NOT needed to defeat cache"* which directly contradicted the actual call site at `RecorderLiveView.tsx:160` (`const liveUrl = \`${getLiveScreenshotUrl(activeSessionId)}?t=${tick}\`` where `tick` increments on a `POLL_INTERVAL_MS = 500ms` timer). New docstring documents the two independent reasons the rotating cache-buster is necessary, neither of which `no-store` solves: (a) **network layer** — corporate proxies performing MITM TLS inspection are observed in the wild to ignore `Cache-Control: no-store` and serve stale cached responses; rotating the URL makes each poll distinct so the proxy can't key on a single URL. (b) **React layer** — `<img>` only triggers a network fetch when its `src` attribute changes by string identity across renders; re-rendering with the SAME `src` is a no-op regardless of server cache headers. The rotating query forces a fresh fetch on every poll tick by mutating `src`. Also corrected the example in the docstring from `t=Date.now()` to `t=${tick}` to match the actual call site (the call uses a poll counter, not a timestamp — relevant because `Date.now()` would change every render not every poll). Cross-references to `RecorderLiveView.tsx:160` + `POLL_INTERVAL_MS` + `tick` state are explicit so future maintainers can trace the contract back to the consumer without searching. **Note**: original task spec referenced lines 80-93 + `RecorderLiveView.tsx:100`; actual coordinates are 85-93 (docstring) + 160 (call site) — same content, line drift from intermediate edits. **Verification**: `pnpm --filter @elearn-studio/authoring-ui exec tsc --noEmit` exit 0; `pnpm lint` 0 errors (2 pre-existing TD-004 warnings in `useComponentProperty.ts` unchanged); no test changes needed (docstrings have no runtime effect). **F3 from audit externo 2026-04-24 — MEDIUM, closed**.

- [x] TD-014.37 — **URL length limit test (F8)** ✅ (2026-04-25). Closes audit-externo F8. **Pure test addition** in `packages/authoring-ui/src/__tests__/simulation/RecorderLauncherDialog.test.tsx` — zero production code changes. Added new describe block `RecorderLauncherDialog — URL length limit (TD-014.37 / F8)` parallel to the existing `title length limit` block (rejected the alternative of merging both into a generic "length limits" block — they cover independent concerns and keeping them parallel matches the per-concern grouping the rest of the file uses; the spec said "new or existing" so the choice was open). Single test pins the boundary at `RecorderLauncherDialog.tsx:62` (`url.length > MAX_RECORDER_URL_LENGTH`): builds an overlong URL of length `MAX + 20` (= `'https://example.com/' + 'a'.repeat(MAX_RECORDER_URL_LENGTH)`), submits, awaits `recorder-dialog-error`, asserts the error text contains the literal limit (`new RegExp(\`${MAX_RECORDER_URL_LENGTH}\`)` matches the "2048" inside the actual copy `"url must not exceed 2048 characters"`) and asserts `mockStart` was NOT called (pins the second half of the contract — the dialog short-circuits before reaching the store call). **Guards against `>` vs `>=` flip**: with `>=`, `length === MAX` would no longer be allowed and the error message would fire on a boundary the rest of the codebase + the backend mirror in `simulation-engine/src/routes/recorder.ts` don't share — silent semantic drift. **Why match the limit number, not the full message**: keeps the test stable across copy rewords ("must not exceed N characters" → "exceeds the N-character limit"); the contract under test is "the limit appears in the error", not "the message uses this exact wording". Updated the file's top-level docstring to list "URL length limit (TD-014.37 — guards against `>` vs `>=` flips)" alongside the other covered concerns. Imported `MAX_RECORDER_URL_LENGTH` alongside the existing `MAX_RECORDER_TITLE_LENGTH` import — single import statement, no module path duplication. **Verification**: file-scoped `vitest run` 19/19 passing (was 18 — +1 new); full authoring-ui vitest **1036/1036** (was 1035 — +1 new); `tsc --noEmit` exit 0; `pnpm lint` 0 errors (2 pre-existing TD-004 warnings unchanged). **F8 from audit externo 2026-04-24 — LOW, closed**.

- [x] TD-014.38 — **Click-outside + isBusy guard tests (F9)** ✅ (2026-04-25). Closes audit-externo F9 and the audit-externo sweep Phase A in full (10/10 findings now closed). **Pure test addition** in `packages/authoring-ui/src/__tests__/simulation/RecorderLauncherDialog.test.tsx` — zero production code changes. Added new describe block `RecorderLauncherDialog — click-outside + isBusy guards (TD-014.38 / F9)` with 3 tests pinning two inline JSX guards: (1) `closes on mouseDown on overlay backdrop (click-outside happy path)` — exercises `RecorderLauncherDialog.tsx:94` (`onMouseDown={e => { if (e.target === e.currentTarget && !isBusy) onClose() }}`); accesses the overlay via `container.firstChild` (the role=dialog form is its child, so the only top-level rendered node is the overlay); fires `mouseDown` directly on it making `target === currentTarget` (testing-library default — see `fireEvent` semantics); inline comment notes that clicking inside the form would have a different target and the guard would short-circuit by design. (2) `does NOT close on Escape while isBusy=true` — sets `useRecorderStore.setState({ isBusy: true })` BEFORE render so the dialog mounts already subscribed to the busy state (avoids any subscription-update timing question between setState and fireEvent in the same tick); fires Escape on `getByRole('dialog')` mirroring the existing happy-path Escape test in render+lifecycle; asserts `onClose` NOT called. (3) `does NOT close on click-outside while isBusy=true` — same setState-before-render pattern + `mouseDown` on overlay; asserts `onClose` NOT called. **Why setState before render**: tested both orderings; setState-after-render works in vitest's environment (Zustand + React 18 commits sync from outside event handlers) but setState-before-render is robust against any future scheduler change and matches the canonical "arrange → act → assert" sequencing. **Why a new dedicated describe block**: the spec said "add three tests" and the existing render+lifecycle block contains the Escape happy path; folding the busy-state negation tests there would split F9 coverage across two locations (the click-outside happy-path lives nowhere in the current file, so 1 of the 3 tests has to be new anyway). New dedicated block keeps F9 traceability in one place — matches the F8/TD-014.37 grouping pattern. Updated the file's top-level docstring bullet from `Escape + click-outside close unless isBusy` to `Escape + click-outside close unless isBusy (TD-014.38 — completes coverage promised in this docstring since TD-014.19)` so the audit trail of "promise → late delivery" is visible from the header. **Verification**: file-scoped `vitest run` 22/22 passing (was 19, +3 new); full authoring-ui vitest **1039/1039** (was 1036, +3 new); `tsc --noEmit` exit 0; `pnpm lint` 0 errors (2 pre-existing TD-004 warnings unchanged). **F9 from audit externo 2026-04-24 — LOW, closed**. **Audit-externo sweep Phase A — COMPLETE (10/10 findings: F1, F2, F3, F5, F6, F7, F8, F9, F10, F11). F4 still folded into TD-014.27.d (OpenAPI regen at block closure).**

**Non-scope / explicitly rejected alternatives:**
- **Reducing user-manual §13 to what is implementable without UI changes** (Route 1 from the 2026-04-24 investigation). Rejected by the owner — keep the documentation ambition; the gap is an implementation debt, not a docs debt.
- **Flagging TD-013.5 as skipped and closing TD-013 without §13** (Route 3). Rejected — the manual would ship with broken placeholders.
- **Introducing new simulation capabilities beyond the recorder + manual-upload paths the manual already names.** Out of scope. Any new feature goes in a separate TD.
- **Rewriting `HotspotCanvas.tsx` on top of a different canvas library (Fabric, PixiJS, plain `<canvas>`).** Konva stays; only `draw-new-rect` mode is added.
- **Dropping Zustand in favour of `useState` for the recorder lifecycle.** Rejected — the existing pattern across simStore / editorStore / phaserSimStore / actionsStore is consistent; the recorder store fits the same mould.

**Dependencies / risk notes:**
- **`VITE_SIMULATION_ENGINE_URL` env var** — default `http://localhost:3002` for local dev. E2E `simulation-recorder.spec.ts` gated by the var being reachable; CI matrix decides whether to spin up the service. If CI does not start `simulation-engine`, the spec auto-skips with a clear annotation.
- **CORS** — `simulation-engine` runs on port 3002; `authoring-ui` on 3000. Verify CORS is permissive for `http://localhost:3000` in dev. If not, add the missing header in `packages/simulation-engine/src/index.ts` as a TD-014.1 output.
- **Live JPEG polling cost** — `RecorderLiveView` polls every 500ms. For a ~50 KB JPEG at 2 req/s this is ~100 KB/s per active viewer. Acceptable for local-only recorder, not multi-tenant. Documented in the architecture page (TD-014.25c).
- **Konva draw-mode + `strictMode` DoubleInvoke** — `react-konva` has had subtle re-render issues in StrictMode. TD-014.15 tests must explicitly assert no duplicated listeners after remount (pattern: assert listener count before + after an unmount cycle).
- **Existing simulation-engine package tests (T023)** — stay untouched. Verify they still pass after any backend change required by TD-014.1 (e.g., CORS header). Rollback is trivial — the backend is mostly frozen in this block.
- **T023 regression risk** — if TD-014.1 discovers a previously-missed backend contract (e.g., `GET /assets?type=image` doesn't exist or has different shape), that opens a sub-investigation and a sub-ticket. Flagged up-front to avoid scope drift.

---

## BACKLOG — Findings from TD-013.5c worked-example build (2026-07-18)

> **Source:** TD-013.5c §17 recomposition — full evidence and repro context in
> `docs/issues/issues-TD-013.md` § "Product/manual findings". Owner directive
> 2026-07-18: file all findings as backlog tasks without hesitation. None are
> blockers for the TD-013 screenshot block.

### TD-015 — Action sequences lost when switching slides (GAP-02 materialised)
> **Source:** TD-013.5c finding 2 | **Status:** FIX SHIPPED 2026-07-18 (severity downgraded — wipe class closed by design) | **Severity:** was CRITICAL

**Delivered (TDD, RED→GREEN):** (1) `EditorCanvas.tsx` selection boundary resolves the persisted widget id like the storage converter (`attributes.id || getId()`) — a divergent component no longer seeds the actions panel with `[]`. (2) `useActionsSave.ts` load-echo suppression — `setWidget` (loading sequences on selection) adopts the baseline and NEVER triggers a save; only a real edit persists. This closes the entire data-loss class: no seeded `[]` can ever be written back by mere selection, whatever seeds it. (3) Tests: 5 new units (3 reproduced the bug pre-fix) in `EditorCanvas.selection.test.tsx` + `hooks/useActionsSave.test.ts`; new E2E `@regression GAP-02.4` (wire → slide switch → return → re-select → cold reload → actions survive). authoring-ui 1044/1044; E2E action-sequence 9/9 + widget-persistence green.

**Honest residual (documented, not blocking):** GAP-02.4 also passes with the fixes reverted (verified under a hard dev-server restart) — in the standard flow GrapesJS re-adopts `attributes.id` as the model id after slide switches, so the ids do not diverge there. The unit tests construct the divergent state directly and prove the old code wiped on it; the exact §17-campaign loss episode remains un-pinned (revised hypothesis: the slide-4 sequence was wired on `BranchingNav` — Escape did not deselect during the build — and the finals looked at `BranchingNote`; plus no-wait save races. The .5c wire-at-capture restructure already made the campaign deterministic). Reopen only if a real-flow repro of id divergence appears.

Sequences wired through the ActionsEditor were gone after navigating to another
slide and back: component ids regenerate on slide reload and the sequences
orphan. A real author who wires actions and navigates away may silently lose
them. This is the e2e skill's GAP-02 persistence gap observed in practice.
Scope: root-cause the id-regeneration vs sequence-keying mismatch (stable
widget ids on the persistence round-trip, or re-keying sequences on load), add
the GAP-02 regression E2E (`action-sequence.spec.ts`: wire → switch → return →
assert), fix. Investigation gate applies (§2.2.2).

### TD-016 — `nav-buttons` renders broken in the editor canvas
> **Source:** TD-013.5c finding 3 | **Status:** ✅ FIX SHIPPED 2026-07-19 | **Severity:** was HIGH

**Root cause** (full trail in `docs/issues/issues-TD-016.md`): the global `component:add` handler in `initEditor.ts` absolutized + made draggable EVERY added component — including the two child `<button>`s of nav-buttons (on creation AND on every loadData). With no left/top both children stacked at the container origin: "Next →" painted over "← Previous". Model/DOM were always correct (probe-verified); pure style clobbering. Editor-only — the runtime player renders from properties. **Fix**: the handler now skips non-top-level components (`component.parent?.() !== editor.getWrapper()` → return); nested children keep their type's layout and `draggable:false`. **Verification**: unit TD-016.1/.2 (RED→GREEN) — authoring-ui 1046/1046; E2E `@regression TD-016` in `grapesjs-integration.spec.ts` (labels + static positions + no-overlap + slide-switch round-trip) — 19/19 with widget-persistence; campaign re-run refreshed the §17 manual captures ("← Previous | Next →" side by side, visually confirmed); tsc 0; lint 0 errors. **TD-016b fix-forward (same day)**: CI caught that centre-clicks now landed on a child button (children flow → occupy the centre) selecting the child instead of the container — `selectable:false, hoverable:false` added to both child-def sites; `nav-buttons-widget.spec.ts` 3/3; contract pinned in `converters.test.ts`; process lesson recorded in issues-TD-016.md (dedicated widget specs belong in the local E2E scope).

The previous-button child renders without its "← Previous" label and overlaps
the Next button. Both the programmatic path and `EditorPage.dragBlockToCanvas`
create the widget identically (`addComponents` by type), so real authors see
this too. Likely the child `content` strings from the type defaults not being
rendered (T634-adjacent). After the fix, re-run `docs:screenshots` — slides
2/3/4 of §17 pick the corrected render automatically.

### TD-017 — Slide-level actions: manual §17 documents a path the product does not expose
> **Source:** TD-013.5c finding 1 | **Status:** ✅ CLOSED 2026-07-19 as a DOCUMENTATION DEFECT (not a product gap) | **Severity:** was MEDIUM

**Verification + archaeology** (full record: ADR `2026-07-19-conditional-navigation-scorm-alignment.md` §Addendum): the runtime executes `enterSlide`/`exitSlide` sequences hosted on ANY widget — `EventDispatcher.fireSlideEvent` scans every widget's sequences by design (docstring explicit; unit-covered since T021; player fires on entry/exit). There is no slide-scope sequence surface anywhere, and the owner's hypothesis was confirmed: `Slide.actions?` survives in the schema as a **fossil field** (present since the initial commit, never read nor written) of a retired slide-level-storage intent — the manual was written against that intent, not the shipped design. **Delivered**: `@deprecated` docstring on `Slide.actions`; manual corrected in all four places (§09 no-selection surface description removed, §10 trigger-families rewritten, §11 three recipes now name a host block + model note, §17 slide-4 hosts the branching on *BranchingNote* + tip pointing to the TD-021 declarative gate); campaign unchanged (its BranchingNote wiring was already the correct pattern). **Slide-scope ActionsPanel surface: closed as not-needed** — would be net-new storage/save/runtime-merge duplicating a working capability; reopen only on real authoring demand.

`17-worked-example.md` §Slide 4 instructs wiring `enterSlide` "on **the slide
itself** (no block selected)", but the ActionsPanel renders an empty state
whenever nothing is selected — slide-level sequences are unreachable from the
UI (the runtime dispatcher DOES fire slide events). Owner decision inside the
task: either build the slide-scope ActionsPanel surface, or correct the manual
to wire slide-entry logic on a designated widget (the §17 campaign currently
uses `BranchingNote`). Until decided, the manual and the product disagree.

### TD-018 — `done-button` label edits do not reflect in the canvas
> **Source:** TD-013.5c finding 4 | **Status:** ✅ FIX SHIPPED 2026-07-19 (v0.5.71) | **Severity:** was MEDIUM

**Root cause**: the T643.1 positive content-restore allowlist in `converters.ts` (`grapesjsFromWidgets`) included `text` and `button` but omitted `done-button` — the edited label saved correctly into `properties.content` (published courses were fine) but every slide reload rebuilt the component without content, so the type default `'✓ Done'` won in the editor. **Fix**: `done-button` added to the allowlist (safe by symmetry with `button` — plain-text single node, not the multi-element HTML that motivated T643.1; consistent with the label-change convention ADR 2026-04-25). **Verification**: unit RED→GREEN in `converters.test.ts` (91/91; suite 1050/1050); E2E `@regression TD-018` in the dedicated `button-widget.spec.ts` (live canvas reflect + slide-switch round-trip — the exact gap) 2/2; campaign refreshed `17-slide-5-final.png`, which now shows "Finish course" (the campaign's own label fill finally survives); tsc 0; lint 0 errors. Test-authoring note: `dragBlockToCanvas` leaves the Blocks tab active — slide navigation in specs must click the Slides tab first (playbook goToSlide pitfall, hit once during test authoring).

Filling "Button Label" on a done-button through the Props panel (committed
with Tab/blur) leaves the canvas rendering the default "✓ Done" after the save
round-trip. Either the caption→content sync or the reload converter drops the
edit. Regular `button` widgets are unaffected (StartBtn's "Start" survives the
same flow). Check the label-change convention ADR
(`decisions/2026-04-25-button-label-change-convention.md`) while fixing.

### TD-019 — `name` trait stripped by the save/reload round-trip
> **Source:** TD-013.5c finding 5 (first documented at TD-013.4 §09) | **Status:** ✅ CLOSED 2026-07-19 (frontend v0.5.72 + backend TD-019b v0.5.73) | **Severity:** was MEDIUM

**Root cause (two-part, the second destructive)**: the loader restored the author name only into `attributes.name` — the component MODEL reverted to the type default ('Button', 'Multiple Choice'…), which (a) the NameField displays (it binds the model), and (b) the save side PREFERS (`c.get('name') ?? attributes.name` — the default is always truthy), so **any save after a reload silently replaced the author's name in the course document with the type default**. The "unnamed widget → type name" semantics are intentional (§09 dropdown); only the author-name restoration was missing. **Fix**: `grapesjsFromWidgets` restores `def.name` (model) alongside `attributes.name` — the same state a freshly-named widget has; the existing save priority becomes correct without changes. **Verification**: unit RED→GREEN in `converters.test.ts` (93/93; suite 1052/1052); E2E `@regression TD-019` in `widget-persistence-across-slides.spec.ts` (NameField keeps the name across a slide round-trip) 3/3; two throwaway probes green, incl. an exact replica of the §17 build sequence (model name survives switch); tsc 0; lint 0.

**Residual → RESOLVED as TD-019b (2026-07-19, backend root cause)**: the §17 campaign's slide-3 finals capture still arrived nameless post-frontend-fix while every direct probe kept the name. A CDP-instrumented repro of the second-course build cracked it: the backend `WidgetSchema` (`backend/api/src/models/Course.ts`) **had no `name` field**, so Mongoose strict mode silently dropped it on every save. The probes survived because T042.5's single-slot `courseCache` served their slide loads from local (named) state; the campaign's second-course flow (renames/addSlide → `bumpCacheVersion`) invalidated the cache and forced nameless server GETs — the frontend fix then faithfully restored *nothing*. **Fix**: `name: String` added to `WidgetSchema` (RED→GREEN regression test `courses.test.ts` "widget name survives the save round-trip"; backend 150/150). **Verification**: instrumented repro shows the MC model name = `Q1Capital` at finals; campaign NameField re-fill retired and the full 55-capture run shows `Q1Capital` organically in `17-slide-3-final.png` (visual check). T-15 (Zustand direct-sync) stays — store staleness, orthogonal to names.

Author-assigned widget names survive as the `[name]` DOM attribute but the
GrapesJS `name` trait (what NameField and WidgetIdParam display) resets to the
type default after a slide reload. Root-cause the converter round-trip so names
persist first-class; then remove the TD-013.4 Zustand direct-sync workaround
and the TD-013.5c NameField re-fill in the campaign.

### TD-021 — Correctness-gated navigation (SCORM-aligned conditional navigation)
> **Source:** Owner request 2026-07-19 (Storm-style "cannot advance until test passed") + TD-017 investigation | **Status:** ✅ SHIPPED 2026-07-19 (v0.5.69) | **Severity:** FEATURE (HIGH value)

Standard verdict (primary sources, see ADR `decisions/2026-07-19-conditional-navigation-scorm-alignment.md`): SCORM 2004 sequencing is inter-SCO only — intra-SCO navigation is explicitly the content's responsibility, and SCORM 1.2 has no sequencing at all; our single-SCO architecture must gate internally (as Storyline/iSpring do). Scope: (a) `scoring.requireCorrect` per question (shared-types); (b) runtime `slideIsComplete` gates on correctness, unlocking when attempts are exhausted (reported failed); (c) **attempts enforcement in the player** — currently NOT implemented (submit hard-disabled after first answer while the default feedback says "Try again" and the authoring UI promises attempts; latent product gap folded in); (d) read `cmi.scaled_passing_score` (2004) / `cmi.student_data.mastery_score` (1.2) as LMS override for passMark; (e) authoring checkbox in the Scoring section; (f) TDD: runtime + panel units, E2E gate flow; (g) ADR + user-guide §08 update + CHANGELOG.

**Delivered 2026-07-19 (TDD end-to-end):** runtime 13 new behavioural tests (RED→GREEN) — gate holds on wrong answers, unlocks on correct or on attempts exhaustion, Submit retryable per attempts (-1 unlimited), LMS threshold override with packaged fallback chain resolved once in init(); suspend v2 extended (c/t fields, legacy inference — resumed learners never re-trapped); authoring checkbox `require-correct-checkbox` in ScoringFeedbackForm (+3 panel tests); E2E `@regression TD-021` green first run. **Verification:** verify:test green all 7 packages (authoring 1049, runtime 278); E2E question-widget + preview-handshake + scorm-export 47/47; campaign refreshed §08 captures (both checkboxes visible; crop-fallback rect re-derived 251×220); tsc -b 0; lint 0 errors. ADR `decisions/2026-07-19-conditional-navigation-scorm-alignment.md` records the standard verdict + multi-SCO non-goal. One flake note: a single verify:test run under heavy load saw 2 harness nulls — non-reproducible (×3 clean since); harness hardened with a render sanity gate for diagnosability.

### TD-020 — Investigate: `editor.select(null)` evaluate dies with "Execution context was destroyed"
> **Source:** TD-013.5c battle note | **Status:** ✅ CLOSED 2026-07-19 — Playwright driver quirk, app exonerated | **Severity:** was LOW (e2e-infra only)

**Verdict (CDP-instrumented repro, 2026-07-19)**: the page's main world is
provably **never destroyed** — a `window.__CTX_TAG` planted before the dying
evaluate is still readable immediately after the error. The failure is a
**stale execution-context handle inside the Playwright driver**, raced by the
GrapesJS canvas-iframe churn (each slide load destroys/recreates the canvas
frame; CDP `Runtime.executionContextCreated/Destroyed` shows the pairs). Not
an app bug; no GrapesJS `select(null)` × autosave interaction involved. The
shipped workarounds were already correct and stand: Escape-key deselect
(fewer evaluates in the churn window) + `retryOnDestroyedContext` (always
safe — the retry runs in the same, live world; rationale in its docstring in
`e2e/utils/worked-example.ts`).

**Upstream filing attempted and DROPPED (2026-07-20)**: to file at
microsoft/playwright (v1.58.2; no matching open issue — #40940/#28572 are
different classes) a reproducible artefact was required. Three standalone
minimal variants (iframe churn via srcdoc / real navigations+reload+jank /
in-flight async evaluates + concurrent locator traffic) scored **0/6000**
spurious failures; a faithful in-app probe replaying the §17 second-course
build to the historical deterministic death point (select(null) after
slide-4 placement) scored **0/10 across 2 runs** — the quirk no longer
reproduces even here. Reading: the TD-019b backend fix removed the
cache-invalidation reload turbulence that raced the driver; the trigger
window has closed. An upstream report with no runnable repro and no longer
reproducible on our side would not survive triage — dropped. **Sentinel**:
the `[docs-screenshots] evaluate hit a destroyed context — retrying once`
warn in `retryOnDestroyedContext` is the canary; if it ever fires again,
capture `DEBUG=pw:protocol` on the spot and revisit the filing.

### TD-014-followup-1 — R-M2: spread-with-null pattern for conditional disabled styles
> **Source:** TD-014.26 reviewer pass (typescript-reviewer, deferred by owner decision) | **Status:** ✅ CLOSED 2026-07-20 (v0.5.74) | **Severity:** was MEDIUM (cosmetic/readability)

The recipe's helper option implemented: `withDisabled(base, isDisabled)` in
`simulationTheme.ts` (returns a new object, never mutates; overlays
`buttons.disabled` only when disabled). All **13 callsites** across the 5
simulation components migrated (the original ticket counted 6 — the pattern
had spread with the recorder work), incl. the two multi-line object-literal
forms and the theme's own JSDoc usage example; `buttons` import dropped where
it became unused (StepForm). TDD: 3 RED→GREEN unit tests in
`simulationTheme.test.ts` (overlay, no-op, immutability). Verification:
`grep 'buttons.disabled : null'` → 0 hits in components; authoring-ui
1055/1055; tsc -b 0; lint 0 errors. Closure note appended to
`docs/issues/issues-TD-014.md`. **Backlog a 0.**
